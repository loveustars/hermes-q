# ⚠️ 3x 网格（clamp 修复前）—— 数字可用，但**指标不可读**

**日期**：2026-09-15
**状态**：结论有效；指标数值因一个**记录层**缺陷而出现经济上不可能的值。

## 这批数据的地位

它跑在**保证金记账缺陷已修复之后**（`e4bdd1e`），所以**策略层面是可信的**：
18 组全部被摧毁 —— 这个结论与修复后的重跑完全一致。

留着它是因为它**记录了一个次级缺陷的原始形态**，而这个形态本身是证据。

## 哪里不可读

破产后曲线里留着**负权益**（交易所不会让你欠钱），导致：

| 指标 | 示例值 | 问题 |
|---|---|---|
| `total_return` | **−205.7%** | 低于 −100% 经济上不可能 |
| `max_dd` | **−109.9%** | 同上 |
| `final_net` | **−10,539** | 负权益 |
| `total_return < −1` 的组数 | **15 / 18** | |
| `final_net < 0` 的组数 | **16 / 18** | |

更严重的是**排名**：脚本按 sharpe 排序，而爆仓组的 sharpe 是假信号
（简单收益率的**均值**可以为正，而复利终值已经归零），于是出现

```
=== top-10 by sharpe ===
  💰 η=0.200 band=0.10 funding=Y  sharpe= 0.892  total= -100.0%   ← 已彻底归零
```

**排行榜第一名是一个本金归零的组合。**

## 修法与对照

修复见 WORK_LOG §15.6，修复后的产物在 `runs/d_param_search_3x/`：
所有爆仓组的 `total_return` 与 `max_dd` 都恰为 **−100%**，
且爆仓组被排除在排名之外（`summary.json` 里 `n_bankrupt=16`）。

**不要引用本目录的 `sharpe` 做任何比较。**
