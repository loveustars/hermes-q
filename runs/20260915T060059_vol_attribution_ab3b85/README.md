# 读这份归因表之前要知道的三件事

**run**：`20260915T060059_vol_attribution_ab3b85`（`vol_attribution.py`，132/160 完成）
**窗口**：77,503 根（`be`/`btc`/`full` 三池统一裁剪）；`be_raw` 未跑完
**复核**：2026-09-15 由委托人独立复核（见 `WORK_LOG.md` §17.11 / §17.12）

---

## 1. ⚠️ `n_funding_events = 0` 且 `n_liquidated > 0` 的行，资金费块无信息量

**强平通道会把若干候选的持仓清得极早**，此后全程空仓 ⇒ 资金费**按构造恒为零**。
这些行里的 `funding_pct_of_initial = 0.0` 是**真值**（不是缺字段），但它
**不能用来论证"资金费无关紧要"**。

已确认属于此类的行（`funding_pct_of_initial = 0.0` 且声明 `engine_funding = True`，共 14 行）：

| 原因 | 行数 | 机制 |
|---|---|---|
| 全现金、从无持仓 | 2 | `exp:flat` |
| k=0.5 假强平**开仓即触发** → 永久空仓 | 7 | `k05_*`；10,000 × 0.5556 ≈ 5,556 ≈ 实测 5,469，与资金费无关（故 `k05_head` ≡ `k05_nofund` 为 5,469） |
| 满额做空在阈值 `(1+k)/(1+m) = +81.8%` 被强平 → 永久空仓 | 5 | `exp:short_all` / `exp:short_mom_720`；BTC 从 2017-11 的 7,676 到 12 月的 13,800 即 +80%，进场数周内触发 |

独立复现（`k1 | exp:short_all`）：`n_trades=2`、`liquidated_legs=2`，权益从 9,763 迅速塌到
**1,166 并长期平在那里**，之后全程空仓 ⇒ 一次资金费都没结算。

**读法**：先看 `n_funding_events` 与 `n_liquidated` 两列再引用任何资金费行。

---

## 2. 本表中混有 61 条**旧 schema** 记录 —— 干净是**运气**，不是设计

判别器：**`n_funding_events is None`**（该列读取时**无默认值**，故缺字段即 None；
而 `funding_paid` 有 `0.0` 默认值，是**不可用**作判别器的）。

```
新记录 engine_funding=False →  14 行
新记录 engine_funding=True  →  89 行
旧记录 engine_funding=False →  61 行      危险组合「旧记录 且 收费」= 0 行
```

61 条旧记录**全部落在资金费关闭的臂上**，那里 `0.0` 本来就是正确值 ⇒ **侥幸未污染**。
**只要有一条落进收费臂，它就会带一个静默 0.0**（"字段不存在"被 `rec.get(..., 0.0)`
变成"资金费为零"），并被当成"这个策略没付资金费"读进结论。

该路径已在 2026-09-15 修复（`CKPT_SCHEMA` / `CKPT_REQUIRED` + `drop_stale_ckpt_records`，
见 `WORK_LOG.md` §17.11）：旧记录会被**丢弃并重跑**，而不是侥幸通过。
**本目录的产生时间早于该修复**，故仍含这 61 条。

---

## 3. 单次运行的数字不是结论

同一候选在 4–8 个"等价"配置下的终值横跨一个数量级乃至 ∞（见 `metrics.json` 的
`variant_sensitivity`）。学习体与买入持有型的 `max/min` 为 `∞`；有限的最大 3.22x。

**本表所有数字都是"某一朵噪声"下的值，只有符号与量级可读。**
`be_raw` 池（故意不裁窗口、用于量化"窗口"这一维扰动）**未跑完**，故窗口敏感度未经独立复现。
