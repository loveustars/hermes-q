# FRAMEWORK_AUDIT.md —— 独立审计：这个框架到底缺什么

**审计员**：独立第三方（未参与本项目任何阶段）
**日期**：2026-09-15
**范围**：只读审计。**未修改任何代码、未改动 `runs/`**；本文件是唯一新增文件。
实验脚本写在 `/tmp/audit{1..9}.py`（临时目录，不入库），关键命令在文中以可执行形式给出。
**方法**：先读代码（`src/agents/online.py`、`src/sim/*`、`src/eval/*`、`src/env/market_view.py`、`src/data/*`），
再用自建 harness 在同一台引擎上复现、隔离、对照。所有数字都是本机实测。
**仓库状态**：`python3 -m pytest tests/ -q` → **158 passed in 1.23s**（复现成功）。
`git status` 仅有 `runs/live/*.json` 的既有改动（前向纸面交易进程写的，与本次审计无关）。

---

## 0. 一句话总判断

> **这个框架缺的不是 bug、也不是防过拟合的装置，而是「相对什么学习、相对什么评判」这一层：
> 它的学习回路只能在一个 18 条规则、全体无正期望的池子里做选择，池内最好的成员就是「一直满仓」
> （等于市场本身）；而它的评估与记账装置既不上报「相对池内最优」的差距、也不做收益归因，
> 还把每一个 1x 结果都压在一个凭空存在的 −44.4% 强平通道上——
> 于是它既无法证明学习有价值，也无法证明没有价值：所有结论都随对照物和强平开关而翻转。**

支撑这句话的两组最硬的数字（同一台引擎、同一套成本、全窗口 77,503 根、1x）：

| 候选 | 净终值 | 成本 | 交易数 | Sharpe | 最大回撤 |
|---|---|---|---|---|---|
| `long_all` 专家（band 0.2 ⇒ 等价于等权买入持有） | **1,525,547** | 249 | 3 | 1.093 | −82.7% |
| 等权三币 逐 bar 再平衡（D 阶段的「公平对照」） | 490,657 | 32,366 | 227,020 | 0.966 | −85.7% |
| **学习体（η=0.20 / band=0.20，D 阶段最优组）** | **200,595** | **91,365** | 5,327 | 0.838 | **−75.5%** |
| 学习体（η=0.01 / band=0.20，≈总是多头） | 72,891 | 142 | 78 | 0.673 | −89.0% |

- 池内 18 条规则里，**只有 beta 型规则（`long_all` / `inv_vol` / `only_BTC` / `only_ETH`）活下来**，
  其余 14 条全部 ≤ 154,003，其中 **12 条 ≤ 2.7 万**（`flat` 10,000、`mom_24` 57、`mom_72` 11,587、
  `mom_168` 27,089、`rev_24` 14、`rev_168` 153、`short_all` 5,865、6 条 `short_mom` 全在 2~31）；
  最好的择时规则 `mom_720` = 154,003，只有买入持有的 1/10。
- **学习体落后池内最优成员 7.6 倍**，同时它的最大回撤是这几个候选里最小的。
  这两个事实同时成立 ⇒ 它不是「没学出 alpha」，它是**在一个不含 alpha 的池子里把一个 beta 暴露稀释了并付了 9 倍于本金的成本**。

---

## 1. 三个最严重的问题

### 【问题一】学习循环的假设空间不含正期望规则，而它的上界从未被测量——「学习没价值」与「池子没价值」被混为一谈

**证据**

1. 同一引擎 / 同一成本下逐个跑 18 条专家规则（`band=0.2`，全窗口）：

   | 规则 | 终值 | 成本 | 规则 | 终值 |
   |---|---|---|---|---|
   | `long_all`（=等权买入持有） | 1,525,547 | 249 | `mom_168` | 27,089 |
   | `inv_vol` | 375,346 | 9,202 | `mom_72` | 11,587 |
   | `mom_720`（最好的择时规则） | 154,003 | **474,376** | `mom_24` | 57 |
   | `short_all` | 5,865 | 249 | `rev_24` / `rev_168` | 14 / 153 |
   | `only_BTCUSDT` | 5,469（↓ 见问题二） | 54 | `short_mom_1..720` | 2 / 3 / 2 / 3 / 3 / 31 |

   内部记账（学习体自己用的那套）也给出同样结论：每 bar 净收益 t 为正的只有 5 条，全是不做择时的
   满仓/单持规则——`mom_720`(2.76)、`long_all`(2.59)、`inv_vol`(2.30)、`only_BTCUSDT`(1.76)、
   `only_ETHUSDT`(1.71)；其余 13 条全为负，做空类（`short_all` + 6 条 `short_mom`）的 t 在 −3.1 ~ −34.1 之间。
   **池内没有一条「择时」规则在扣成本后为正期望；正的几条全是「一直持有」的 beta 表达。**

2. 学习体自己的行为与之一致：终态 `p` 是单点的（η=0.20 组 `p_short_all=1.000`，η=0.01 组 `p_long_all≈0.70~0.74`），
   即它只在选「满仓多」还是「满仓空」，不是在做择时。

3. **学习体的上界（事后最优单专家）从未被计算过。** 全仓库没有一处把「池内最优成员」作为对照物。
   `scripts/d_fair_comparison.py` 的对照物是 3 个固定权重组合；`d_param_search.py` 的对照物是项目旧基准。
   两者都不是「学习体自己的假设空间的上界」。

**为什么严重**
「专家选择型」在线学习（Hedge/MWU）的 regret 界是相对**池内最优成员**的：它数学上不可能超过池内最优。
所以只要池内最优 = 「一直满仓」（=市场），这个架构的天花板就是「市场暴露 − 成本」，
而框架的判定口径（相对市场剥离 beta 后的 alpha）恰好要求它做相反的事。
**结论是一般性的：不改池子，任何 η/band/杠杆/奖励塑形都不会产出正 alpha。**
而框架的 M5/D 阶段花了四轮工作（A/B/C/D）去调「训练目标能不能做空/能不能上杠杆」，
这些都是池内成员的**重新加权**，不改变上界。

**最短验证**（一条命令，约 5 秒；`band=0.2` 时 `long_all` 退化为买入持有）

```python
# 在 python3 里（cwd=项目根）
from src.agents.online import default_experts, HedgeEnsemble
ex = [e for e in default_experts(["BTCUSDT","ETHUSDT","BNBUSDT"]) if e.name=="long_all"][0]
# 把 ex 包成一个 agent：decide(view) -> {s: w}，并与 HedgeEnsemble 一样用 band 过滤（|Δw|₁ < band 则返回 None）
# 过 SimExchange（max_gross=1, allow_short=True, instrument="perp"）⇒ 得到 1,525,547
```

（§附录 A 的第 3 段有可直接复制的完整脚本，同一份 harness 稍作修改即可跑全部 18 条规则。）
补一句：`logs_fair_compare.txt` 里其实已经有「纯 BTC 满仓 99,367 / BTC+ETH 各半 96,933」两行，
两者都被学习体（200,595）打败 —— **那张表缺的只是「池内最优」这一列**，而这一列恰好是 1,525,547。

**可能的反驳**
- *「`long_all` 的 1.53M 是样本内运气（BNB 425 倍），不能当目标。」*
  我同意不能当**目标**，但「上界」这个用途恰恰要求事后最优——它是天花板，不是预期。
  我要说的只是：**框架从未测量天花板，所以它把「在这个池子里学习没有意义」误报成了「学习这件事没有意义」**。
- *「池子是人挑的，挑的规则本来就不该有 alpha。」*
  这正是我的判断，也是问题所在：池子的准入标准里没有任何一条「扣成本后正期望」的检验。
  项目自己已经量化了唯一一条正期望的策略（M10 funding carry：BTC alpha 4.88%/年、t=13.36），
  但那条策略**在结构上不可表达**（见问题一的补充，下文 §1.5）。

**§1.5 补充（这条比上面更硬）：学习回路在结构上无法表达项目自己唯一验证过的边际**

- `src/sim/exchange.py:199` —— `units_n = {s: 0.0 for s in self.symbols}`：**每个标的一个净头寸**。
  `Expert.weights()` 返回每标的的净权重向量。因此一个 delta 中性的双腿仓（现货多 + 永续空）在
  这台引擎里净敞口为 0，**不可表达**。
- 而项目唯一通过完整协议的策略（M10 carry，PLAN §14.4）必须用两条腿：它跑在另一个仿真器
  `src/sim/carry.py`（三桶账户：spot / margin / reserve，模块 docstring 说明了为什么不能复用 SimExchange）。
- `default_experts()`（`src/agents/online.py:296-308`）里**没有任何 carry / basis 规则**；
  funding 只作为 payoff 项加在既有方向性规则上（`online.py:210-223`）。
- 于是 A→B→C→D 四个阶段（做空 → 3x → 逐腿保证金 → 参数重标定）做的，在结构上等价于
  **「把 carry 的来源腿（现货多头）丢掉、只留它的风险腿（永续空头）」**——**这是我的解读，不是项目自述**；
  而这正是 M8/M10 自己证明会亏光的那一半（`short_mom_1` 的每 bar 净收益 t = −34.1，终值 2）。
- 附带：两套保证金模型互不兼容——`carry.py` 用 `initial_margin_ratio=1.0 / maintenance=0.005`，
  `SimExchange` 用 `0.5 / 0.1`（见问题二）。

---

### 【问题二】保证金通道在 1x 下凭空强平（−44.4%，与杠杆无关），band 又阻止重入——D 阶段 18 组、「公平对照」、M5 全部建立在这个通道上

**证据**（解析 + 数值 + 路径三重，详见 §附录 A）

1. 解析式（由 `src/sim/margin.py:98-144` 直接推出）：
   `equity = margin_cash + (p − p₀)·u`，`margin_cash = k·|u|·p₀`，判定 `equity ≤ m·|u|·p`
   ⟹ 多头 `p/p₀ ≤ (1−k)/(1−m)`，空头 `p/p₀ ≥ (1+k)/(1+m)`。
   **`|u|` 两边约掉 ⇒ 阈值与持仓量、与杠杆完全无关，只由 (k, m) 决定。**
   D/C 阶段固定 `k=0.5, m=0.1` ⟹ **多头 −44.44%、空头 +36.36%**。
2. 数值验证（直接调用 `MarginBook`，不依赖解析式）：1x / 2x / 3x 的首次可强平跌幅**都是 44.5%**。
   3x 时 `margin_cash` 被设为 15,000 = **1.5 倍本金**（比账户全部权益还多 50%），这本身就是「公式不是保证金模型」的告示。
3. 路径定位：`only_BTCUSDT`（1x，无借款）**bar 9104 = 2018-11-23 01:00**，BTC 该根 `low=4,239.7`，
   入场价（bar 301 的 open）= 7,676.8，`low/p₀ = 0.5523 ≤ 0.5556` 触发。曲线从索引 8803 起**完全恒定**
   直到末尾（7.9 年）。终值 5,469 vs 关掉保证金 101,251 ⇒ **−94.6%**；
   而 BTC 从入场价到末尾涨到 77,770（10.1 倍）。
4. D 阶段 18 组的 `n_liquidated`：**η=0.20 组 130~216 次**，η=0.05 组 6~9 次，η=0.01 组 4~7 次
   （`runs/d_param_search/all_configs.csv`）。
5. 影响量化（margin ON → OFF）：

   | 候选 | ON | OFF | OFF/ON |
   |---|---|---|---|
   | 学习体 η=0.20 b=0.20 | 200,595 | **325,268** | 1.62 |
   | 被动 EW 逐 bar 再平衡 | 490,657 | 504,717 | 1.03 |
   | 被动 EW 买入持有 | 1,525,547 | 1,580,663 | 1.04 |

   ⇒ 「学习体输给被动等权」的**比值差距从 2.45x 缩到 1.55x**：按比值口径 **~37%** 来自这个通道
   （按对数差距口径 ~51%）。**这条通道选择性地惩罚「会换方向」的策略**（学习体 130 次强平 vs 被动 2~5 次）。

**为什么严重**
这不是记账算错一个数，而是**一个经济上不存在的机制在改写全部 1x 结论**：
1x 满额买入没有借钱，只有标的跌到接近 0 才该归零；而代码给每条腿强加了一个「从锚定价算 −44.4%」的停损，
锚定价只在「从 0 开仓 / 多空翻转」时重置。再叠加 band（`|Δw|₁ < band` 就不发单），
**静态规则被强平后永远不再入场**——`only_BTCUSDT` 那 18.5 倍的差距完全来自这一点
（同一条规则 `band=0.0` 时每 bar 都在发单，强平后立刻重入，终值 99,367 ≈ 正确的买入持有）。
含义：D 阶段「18 组超参搜索」实际是在**一个被停损改写的路径空间**里搜索，
每组的强平次数从 4 到 216 不等；(k, m) 这个从未被讨论过的参数选择，对结果的影响不小于 η。

**最短验证**（两条对照，各 ~3 秒）

```bash
cd /home/nick/workspace/quant && python3 - <<'EOF'
import sys; sys.path.insert(0,'.')
import numpy as np
from src import config as cfgmod; from src.agents.online import default_experts
from src.data import store; from src.sim.costs import CostModel
from src.sim.exchange import SimConfig, SimExchange; from src.sim.margin import MarginConfig
cfg=cfgmod.load("base"); syms=cfg["universe"]["core"]
fr={s:store.load_bars(s,"1h")[["open","high","low","close","volume","quote_volume"]] for s in syms}
idx=None
for f in fr.values(): idx=f.index if idx is None else idx.intersection(f.index)
fr={s:f.loc[idx] for s,f in fr.items()}
ex=[e for e in default_experts(syms) if e.name=="only_BTCUSDT"][0]
class A:
    def __init__(s2): s2.last=None
    def decide(s2,v):
        w=np.asarray(ex.weights(v,syms,1.0),float)
        if s2.last is not None and abs(w-s2.last).sum()<0.2: return None
        s2.last=w; return {s:float(x) for s,x in zip(syms,w)}
for tag,k in [("现状 k=0.5",0.5),("k=1/L（最小修法）",1.0)]:
    r=SimExchange(fr,CostModel(enabled=False),CostModel.from_config(cfg,enabled=True),
        SimConfig(initial_cash=10_000.,warmup=300,max_gross=1.0,max_exposure_per_symbol=1.0,
                  margin=MarginConfig(initial_margin_ratio=k,maintenance_margin_ratio=0.1,topup_trigger_ratio=0.5),
                  allow_short=True,instrument="perp")).run(A())
    print(tag, f"{r.net_equity[-1]:,.0f}", "强平", len(r.liquidated_legs))
EOF
# 期望：k=0.5 → 5,469（1 次强平）；k=1.0 → 101,251（0 次强平）
```

**可能的反驳**
- *「给每个仓位设一个风控止损没什么不对。」* 两个反证：(a) 触发水平 −44.4%（**从锚定价算，与杠杆无关**）
  不对应任何交易所规则；(b) 它不是「策略的决策」，而是记账公式的副作用——策略无法选择不用它，
  也无法在它触发后知道（agent 是**开环**的：不看 fill、不看权益、不看强平）。
- *「1x 不代表没有杠杆——空头腿是卖空来的、3x 是借来的。」* 这条对**空头与杠杆组成立**，我据此做了检验：
  3x 的结论**不受**这个通道影响（η=0.20/0.05 组 margin ON/OFF 都归零，死时原始权益 −794 vs −820），
  所以「3x 全部爆仓」我不能用这条通道去挑战（见 §2 的自我推翻记录）。
  但对**多头 1x**（`only_BTCUSDT`、`long_all`、学习体的大部分时段）没有借款，强平不该存在。
- *「也许 −44.4% 恰好接近现实里 perp 的强平线。」* 现实里 Binance BTC 永续在 1x/全仓下的强平距离是
  ~−90%（维持保证金率 0.4~0.5%），本项目 `carry.py` 用的 `maintenance=0.005` 就是这个量级——
  **同一个仓库里另一处用了对的数量级，而学习线用了错的。**

---

### 【问题三】评估装置与实际决策脱钩：D 阶段没有 holdout / 没有种子 / 样本内选优（违反项目自己的铁律 4、6 与 G2 淘汰门），而协议的判据要么被峰度豁免、要么功效低到分不出 40%/年 与 0

**证据**

1. **D 阶段没有任何样本外纪律**：`scripts/d_param_search.py:13` 明写
   「HoldoutGuard（C/B' baseline 都没用 holdout，D 也不引入）」。
   18 组全部跑全窗口，用**同一份数据的样本内 sharpe** 选最优（`main()` 的 `alive.sort(key=-sharpe)`）。
   这违反 PLAN §1 的铁律 4（样本封存）与铁律 6（≥10 种子、报分布、禁止只报最优）；
   也违反 PLAN.md:184 的 G2 淘汰门原文——**「无显著边际 → 禁止调参，转信号源研究」**。
   PLAN:1161 与 WORK_LOG:476 记录了这个自觉的越界（理由：先让训练目标有能力响应做空）。
   我的判断：那是**能力变更**，不是**新信号源**，而 G2 要防的正是这个。
2. **网格在选「少交易」，不是在选信号**：η=0.20 与 η=0.05 两组的终值对 band 单调递增
   （η=0.20：86,653 → 139,770 → 200,595；η=0.05：53,679 → 81,523 → 95,485），
   最优组落在 `BAND_GRID` 的**最大端点** 0.20。这是「无信号策略」的教科书特征：
   任何交易都在毁灭价值，所以越少交易越好。（η=0.01 组有内点最优 band=0.10，所以不是全组单调。）
3. **协议的功效**（自建 DGP：真 alpha 为常数漂移 + 独立噪声，beta=1，T=77,503）：

   | 真 alpha（年化） | 判「有边际」的比例（n_trials=1） | n_trials=18 |
   |---|---|---|
   | 0% | 0% | 0% |
   | +20% | 7% | 0% |
   | +40% | **35%** | **3%** |
   | +80% | 67% | 18% |
   | +150% | 90% | 53% |

   ⇒ **在 8.8 年小时线上，一个真实的 +40%/年 alpha 只有 35% 概率被判为「有边际」**
   （D 网格规模 18 次试验时 3%）。「无边际」因此**不是「没有边际」的证据**。
4. **协议不漏噪声，但唯一生效的判据没有多重试验校正**：
   对真零边际的动量/随机 on-off 规则做 300 次重复，假阳率 **0~1%**（名义 5%）——
   这一点框架做对了，我的初始怀疑被推翻（见 §2）。
   但 on-off 型策略收益的中位峰度是 14.2（阈值 10）⇒ `alpha_p` 与 `dsr` 被豁免 99~100%，
   **唯一生效的判据是 `alpha_ci`（块自助区间下界 > 0），而它没有任何多重试验惩罚**——
   被豁免掉的 `DSR` 恰恰是项目为自己设计的那个惩罚（铁律 3）。
   实测学习体：峰度 33.5，豁免 DSR/alpha_p，唯一判据失败 → 无边际。
   实测「池内最优」`long_all`@b0.2（相对 EW 再平衡）：alpha +20.63%/年、beta 1.049、t=1.59 → **也判「无边际」**。
   ⇒ 这套装置对「比被动好 3 倍」和「比被动差 3 倍」都给出同一个判定，**没有区分力**。
5. **每条配置只有一条路径，没有种子分布**：脚本无 RNG、延迟抖动默认关闭，18 组各得到一条确定性曲线；
   加上强平次数在组间差 4~216 次，无法区分「超参效应」与「这一条路径 + 强平次数」。
   （铁律 6 要求的「≥10 种子 + 报分布」在这条线上从未执行。）

**为什么严重**
一个功效 3~35% 的判据 + 一个全是样本内极值的网格 = **任何结论都既不可证实也不可证伪**。
这解释了「为什么至今没有产出正结果」的一半：不是说它一定没有边际，而是**它目前的证据结构
不允许「有边际」这个结论出现**，无论真实情况如何。另一半由问题一给出（池子确实没有边际）。

**最短验证**

```bash
# (a) 功效：把 evaluate_strategy 喂给"真 alpha=+40%/年"的合成序列（r = beta*b + idio + drift），
#     跑 40 次重复，数 verdict=="有边际" 的比例 → 会得到 ~35%（n_trials=18 时 ~3%）。
# (b) 样本外：把 d_param_search 的网格改成"前 75% 选参、后 25% 只报不改"，
#     看最优组从 η=0.20/band=0.20 变成谁、sharpe 掉多少（现成的 m11_walkforward.py 有骨架）。
```

**可能的反驳**
- *「协议在假阳方向非常保守（0~1%），所以至少不会自欺。」* 同意，这是它的真实价值；
  但它防的是「相信假东西」，它对「发现真东西」几乎无能力。而项目当前的处境恰恰需要后者。
- *「峰度豁免是刻意的，避免用正态近似。」* 豁免本身没错，**错在豁免之后没有替代判据**。
  `KURT_MAX_WELL_BEHAVED=10`（`protocol.py:469`）意味着：任何 on/off 型策略都自动豁免 DSR——
  即「按试验次数惩罚」这条铁律在最需要它的策略类型上失效。
- *「D 阶段只是参数重标定，不是最终评估。」* 但 D 阶段的结论（`runs/d_param_search/README.md` 的
  「终值 53,679~200,595 / sharpe 0.627~0.838 / 最大回撤 −75.5%~−92.0%」）已经以结论形式进入
  WORK_LOG §15/§16 与 PLAN §21，并被用来支撑「学习体输给零智能」的解读。它事实上已经是评估。

---

## 2. 既有结论里最站不住脚的一条

### ★ 「同口径公平对照：学习体输给零智能等权组合，学习没有增加价值」

（`scripts/d_fair_comparison.py` 的 HYPOTHESIS／`logs_fair_compare.txt`／WORK_LOG §15.2 ②）

**为什么站不住：这条结论完全由「对照物怎么选」决定，而项目自己那份日志里就有反例。**

`logs_fair_compare.txt` 的同一张表：

| 候选 | 终值 | Sharpe | 最大回撤 |
|---|---|---|---|
| **学习体 η=0.20 band=0.20** | **200,595** | **0.838** | **−75.5%** |
| 等权三币 每 bar 再平衡 | 490,657 | 0.966 | −85.7% |
| BTC+ETH 各半 再平衡 | 96,933 | 0.721 | −89.0% |
| 纯 BTC 满仓 | 99,367 | 0.725 | −84.1% |

学习体在这一张表里**打败了 4 个候选里的 2 个（终值、Sharpe、回撤三项全部更好）**，
并且拥有**最好的最大回撤**。唯一赢它的是「等权三币再平衡」——而那一项的收益
按项目自己 README 的说明 **95.6% 来自 BNB（$1.70 → $722.71，425 倍）**。
换句话说：结论的符号由「对照组合里有没有 BNB」决定，去掉一个标的就翻转。

**而「2.4 倍」这个具体数字更是选出来的**，不是上界也不是下界：

| 对照物 | 与学习体的比值 |
|---|---|
| 等权三币 逐 bar 再平衡（项目用的那个） | 2.45x |
| 同上，但关掉强平通道（两个都关） | 1.55x |
| 等权三币**买入持有**（＝池内 `long_all` 在同样 band 下的行为） | 7.61x |
| 同上，关掉强平通道 | 4.86x |

问题在于：那个「公平」对照给**被动方**选了更难的模式（不享有 band、每 bar 再平衡、付 32,366 成本），
给**学习体**则是「有 band、几乎不交易」的模式；同时它给的「公平」只是口径公平，
**对照物的设计（哪些标的、什么再平衡频率、什么 band 语义）从未被论证过**。

**我的反向解读**（比原结论更稳的那一版）：
把这场比较拆开会发现学习体的表现几乎完全是**暴露稀释 **：
- η=0.01 那条（≈总是多头）：对 EW 再平衡基准 **beta=0.861、R²=0.928**——
  它**就是**市场的 86%，残差年化 −16%。它不是「学到了坏信号」，它是**同一个 beta，敞口只有 0.86，还多付成本**。
- η=0.20 那条：beta=0.568、R²=0.390、`mean_gross=0.650`、只有 2.3% 的 bar 真下单、成本 91,365
  （= 初始本金 9.1 倍，占平均权益 53%），BNB 平均权重 0.043（等权应为 0.333）。
- ⇒ 稳健的说法是：「**学习体收敛到一个约 0.57~0.86 倍的市场暴露，并为此付出每年约 6% 权益的成本；
  它的净结果落后于同等暴露的被动持有，差额可分解为敞口缺口 + 成本 + 强平通道三块**」。
  这三块框架一块都算不出来——**它没有收益归因层**。这才是真正的缺口：不是「输了几倍」，而是
  「输在哪儿」无法回答，所以每次换对照物就换一个结论（2.45x / 1.55x / 4.86x / 7.61x）。

**顺带一条同样站不住的解读**：「学习体退化成了 long_all（M5 的 `p_long_all=0.9579`）⇒ 学习失败」。
`long_all` 是**池内事后最优成员**，收敛到它是在这个池子里**唯一正确**的行为。
真正的失败不是「收敛到 long_all」，而是**它并没有真的持续持有 long_all**：
它把权重摊在一堆 on/off 规则上（`mean_gross=0.650`），于是敞口只有 65% 还要付 5,327 笔交易的成本。

### 我原本怀疑、但被自己的实测推翻的三条（如实记录）

1. **「评估协议会让噪声通过」——推翻。** 300 次重复下假阳率 0~1%（名义 5%），协议在假阳方向是保守的。
   我最初怀疑 `alpha_ci` 的块长（默认 block=24）对高持续性策略会低估方差、导致过拒真，实测没有发生。
2. **「3x 全部爆仓也是强平通道造的」——推翻。** `margin ON` 与 `OFF` 的 3x 终值几乎相同
   （η=0.20/band=0.20：ON 归零/off 归零，死时原始权益 −794 vs −820；η=0.01/band=0.03：36 vs 38）。
   3x 的死因是「PnL 三倍化 + 破产闸门」，不是强平阈值。**「3x 不可行」这条结论我无法挑战。**
   （顺带更正一个项目里的说法：由于阈值与杠杆无关，C 阶段「杠杆 ⇒ 强平风险上升」的叙事在代码层面不成立；
   3x 里真正变化的是**亏损幅度**，不是强平距离。）
3. 还有一条我**证伪了自己的推导**：`protocol.py` 的 HAC 标准误在「策略收益 = 基准 + 常数」这种完全共线下
   会崩塌到 1e-18，t 统计量报到 1e4（实测 t=38,410）。这不是现实 DGP（真实策略总有独立噪声），
   但说明 t 统计量没有护栏；如果将来有人把「基准 + 一个缩放系数」这种退化策略喂进去，会得到荒谬的显著性。

---

## 3. 如果只能做一件事

**写一个 ~30 行的脚本 `scripts/pool_ceiling.py`，把「上界」从看不见变成一行数字；跑之前先把保证金参数改成与杠杆一致（`k = 1/max_gross`，一行参数）。**

具体做什么（可执行，约 25 分钟机时）：

1. 复制 `scripts/d_fair_comparison.py` 的 `FixedWeights`，加一个 `OneExpert` 包装器
   （`decide` = `expert.weights(view, syms, max_exposure)` + 与 `HedgeEnsemble` 相同的 band 过滤）；
2. 对 `default_experts(syms)` 的 18 条规则各过一遍 `SimExchange`（同成本、同窗口、同 `max_gross`），
   输出三列：**(a) 池内事后最优**；**(b) 每条规则相对「同 band 的等权买入持有」的差额**；
   **(c) 学习体落在池内第几分位**（净终值与 Sharpe 双口径）；
3. 同时报 **margin ON / OFF 两套**（`MarginConfig(initial_margin_ratio=1.0/max_gross, ...)`），
   让强平通道的贡献直接可见，而不是藏在参数里。

判据（写进 run 的 hypothesis，按铁律 5）：**若学习体不在池内前 30%，就停止参数调整**，
按 G2 转信号源——最直接的一条是把已经验证过的 funding carry 变成一条专家
（需要先让引擎能表达两条腿，见 §1.5），或者接受「这个池子没有 alpha」并关掉这条线。

为什么是这一件：它是**所有其他工作的前置**。修保证金不改上界；调 η/band 不改上界；
写 RL/LLM 反思体不改上界（它们只会更高效地收敛到同一个池内最优 + 过拟合）；
只有先知道上界是多少，才能判断后面任何投入值不值得。而且它的产物本身就是一个**可交付的负结果或正结果**：
如果池内最优 = 买入持有，那么「自学习体要证明的东西」就被精确定义为
「你必须在同一台引擎上，净成本、同 band 语义下，超过等权买入持有」——这是一个可证伪的命题。

---

## 4. 不确定清单（我只有五成把握的判断）

1. **成本模型的绝对水平（12bp/边）我没有独立校准。** 如果真实往返成本是 6bp 而不是 12bp，
   学习体的 91,365 成本会腰斩，很多倍数关系会变（池内最优的位置不会变，它是 249）。
   `fee_taker=0.001` 再打 `bnb_discount=0.75` 用在**永续**上，我怀疑偏乐观（BNB 折扣是现货的机制），
   但没有数据推翻它。**五成。**
2. **「band 网格在选少交易」**：只有 2/3 个 η 组对 band 单调，η=0.01 组的最优在 band=0.10（内点）。
   所以这是「趋势 + 一条例外」，不是定理。**六成。**
3. **学习体 margin ON/OFF 那 1.62 倍（相对缺口 −38%）里，「被停损实现的损失」与「停损后以更好价重入的收益」
   各占多少，我没有分解路径**（只算了净额）。方向不确定。**五成。**
4. **「事后最优成员应当被当作上界来报告」这个做法**本身有前视偏差的味道；
   我坚持它作为「天花板」用途是对的，但如果有人把它当「目标」就会自欺。我在措辞上可能有偏。**五成。**
5. **funding 接入对学习是否有一点点边际**（D 网格里 funding=Y/N 的差在 η=0.05 组很小：81,523 vs 81,615，
   在 η=0.20 组却很大：139,770 vs 92,695）。我倾向于认为那个大差是「强平次数不同」的产物，
   但没有跑对照。**五成。**
6. **数据层**：`configs/base.json` 声明 `validation_sources: ["okx","coinbase"]`，PLAN §5 的 M1 验收标准是
   「三源价差统计报告」，但 `data/manifest.json` 里**只有 binance / binance_funding / binanceperp**，
   `data/raw/` 里没有任何 okx/coinbase 文件 ⇒ 交叉校验看起来从未落地，全部结论基于单一来源。
   我只有五成把握说这是「缺口」而不是「后来决定放弃并已记录」——我在 PLAN 里没找到放弃的记录。
7. **学习线用现货 K 线给永续仓位定价**：`d_param_search.py:124-125` 用 `store.load_bars(s,"1h")`（现货），
   而 `instrument="perp"` 并按永续费率收资金费；`data/raw/binanceperp_*_1h.csv`（61,508 根）就在盘上却没用。
   对收益分解有影响，量级我估计小（基差 0.1% 量级），但没实测。**五成。**

---

## 附录 A —— 用户指定核查：保证金/强平通道（四问四答）

> 用户的假设：「`MarginBook` 的强平触发用 `margin_cash = initial_margin_ratio × |u| × 价格`，
> D/C 阶段固定传 0.5 ⇒ 无条件假设每个仓位 2 倍杠杆；而 `SimExchange` 的现金记账是全额扣现金买入，
> 1x 时 cash 归零、没有隐性借款 ⇒ 实际杠杆由现金余额决定，不是由 `initial_margin_ratio` 决定。」
> **结论：假设成立，且比假设的更严重（阈值与杠杆完全无关）。** 全部实测数字见下。

**问 1：1x 持仓的强平阈值是多少？**
`p/p₀ ≤ (1−k)/(1−m) = (1−0.5)/(1−0.1) = 0.5556` ⇒ **−44.44%**。
数值例：本金 1 万，bar 301（2017-11-18 16:00 UTC）以 open 7,676.81 全额买入 1.3026 BTC；
bar 9104（2018-11-23 01:00）该根 `low=4,239.67`，`4,239.67/7,676.81 = 0.5523 ≤ 0.5556` ⇒ 判强平，
现金回收 `1.3026 × 4,239.67 ≈ 5,523`，扣建仓成本后 ≈ 5,510 —— **实测终值 5,469**（差 0.8%，
来自执行价与成本明细）。方向与量级完全吻合，且**此后 7.9 年曲线恒定**（无持仓、无交易）。
（空头侧：`p/p₀ ≥ (1+k)/(1+m) = 1.3636` ⇒ **+36.36%**。1x 空头的正确量级应接近 +100%。）

**问 2：3x 时同一条件算出的阈值是多少？与 1x 是否同一公式同一组参数？**
**同一个公式、同一组参数、同一个数**。`|u|` 在不等式两边约掉：
`k|u|p₀ + |u|(p−p₀) ≤ m|u|p ⟺ p/p₀ ≤ (1−k)/(1−m)`，**与 |u|、与杠杆无关**。
直接问 `MarginBook`（不依赖解析式）：1x / 2x / 3x 首次判定可强平跌幅**都是 44.5%**。
而 3x 时 `margin_cash` 被设为 0.5×3×本金 = **1.5 倍本金**——比账户全部权益还多 50%，
这就是「它不是一个保证金模型」的直接证据（真实保证金不可能超过账户权益）。
**推论：C/D 阶段把「杠杆 ⇒ 强平风险」当成叙事是不成立的**；3x 的结果差异来自亏损幅度，
不是强平距离（这也是我没能推翻「3x 不可行」这条结论的原因）。

**问 3：正确的做法是什么？**
用户的倾向（「保证金比例应由实际杠杆推出」）**方向正确，但要补两点**：

1. **最小一致化修法（能立刻用，已验证）**：`initial_margin_ratio = 1/L`，其中
   `L = Σ|units_s|·p_s / equity`（在这台引擎里，L 等于目标 gross 暴露）。
   代入原式得 `p/p₀ ≤ (1−1/L)/(1−m)`，把它通分后就是教科书式 `(L−1)/(L(1−m))`：
   L=1 → 0（**永不强平**，需要价格归零）；L=2 → 0.556（−44.4%，正好是这套参数原本想表达的 2x 情形）；
   L=3 → 0.741（−25.9%）。
   **实测验证**：`only_BTCUSDT` 用 `k=1.0, m=0.1` → 终值 **101,251、0 次强平**，
   与完全关掉保证金的结果**逐位一致**。
2. **结构性修法（我更推荐，但改动更大）**：`margin_cash` 从来没有被真正借记过
   （`open_leg` 的 docstring 自己说了：主循环已全额扣现金），所以 `equity()` 算的根本不是账户权益，
   而是「幽灵保证金 + 浮动盈亏」。正确做法是**在账户层面判定**：
   `equity ≤ Σ_s m·|units_s|·p_s` 才强平（标准全仓交叉保证金），
   并删掉 per-leg 逻辑子账户；同时 agent 需要能看到强平与权益（当前是开环的）。
   若保留 per-leg 设计，至少要让 `margin_cash` 真实占用现金，否则 1x 必然出错。
3. 无论选哪种，**必须重跑所有基线**：这个参数一动，1x 结论会整体位移（见下）。

**问 4：影响量化**
- **那个具体路径**：**bar 9104 = 2018-11-23 01:00**（入场 bar 301 = 2017-11-18 的 open = 7,676.8）。
  强平后曲线从索引 8803 起恒定到末尾，**7.9 年空仓**，期间 BTC 从 7,676.8 涨到 77,770（10.1 倍）。
  终值 5,469 vs 关掉保证金 101,251（**−94.6%**）。机制是两层叠加：
  (a) 那个 −44.4% 的伪停损；(b) 强平后 `units=0` 而 agent 无反馈、静态规则的 `|Δw|₁ = 0 < band`
  ⇒ 永不重入（同规则 `band=0.0` 时终值 99,367，证实 (b) 是主因）。
- **对「学习体输给被动等权」的贡献**：
  比值差距 **2.45x → 1.55x**，即**按比值口径 ~37%（按对数差距 ~51%）** 来自这个通道。
  方向相反的一面也要说清楚：这不改变「学习体落后」这个方向，只改变幅度。
  它的机理是**选择性的**：学习体被强平 130 次（D 网格 η=0.20 组 130~216 次），
  而被动组合只有 2~5 次 —— 谁换方向多，谁就被这个通道多惩罚一次。
  用户提到的「200,595 vs 325,268 差 38%」核实为 **+62.2%**（200,595 → 325,268），
  且这 38% 不是学习能力问题，是记账问题。
- 参考：`n_liquidated` 与 `final_net` 的 Spearman 只有 0.145（18 组），
  所以**我不主张**「D 网格在搜索强平次数」；能主张的是这个通道扭曲了**水平**（对最优组 1.62 倍），
  而它的次数在组间差 50 倍，足以污染任何组间比较。

---

## 附录 B —— 证据索引（文件:行号 + 可执行验证）

| 论断 | 代码位置 | 实测证据 |
|---|---|---|
| 更新规则与文档不一致（尺度被横截面 z 化抹掉） | `src/agents/online.py:6`（文档 `exp(η·payoff)`） vs `:232-237`（`z=clip(payoff/std,±5)`） | 实测每 bar Δlog-odds 中位 0.6334、p95 0.9159（η=0.20）⇒ 隐含 z 差中位 3.17 |
| 学习体在头 5 天定死 | `online.py:181,235-237`（无遗忘因子） | `max(p)` 首达 0.5 = bar 104、首达 0.9 = bar 127（T=77,202）；终态熵 0.000 |
| 池内无正期望择时规则 | `online.py:296-308`（18 条规则） | 18 条各自过引擎：除 beta 型外全部 ≤ 154,003，6 条做空全归零；内部 t 统计量见 §1 |
| 池内最优 = 买入持有 | — | `long_all`@band0.2 = 1,525,547（3 笔交易、成本 249） |
| 学习体落后池内最优 7.6 倍 | — | 200,595 vs 1,525,547；成本 91,365 vs 249 |
| 学习体≈稀释的市场暴露 | — | β=0.861/R²=0.928（η=0.01）、β=0.568/R²=0.390（η=0.20）、`mean_gross` 0.650/0.861 |
| 强平阈值与杠杆无关 | `src/sim/margin.py:98-144` | 1x/2x/3x 首次可强平跌幅均 44.5%；`margin_cash` 在 3x 时 = 1.5 倍本金 |
| 伪强平路径 | `src/sim/exchange.py:288-304`（强平判定）+ `:359-360`（入队） | 2018-11-23 01:00（bar 9104）BTC low 4,239.7 / 入场 7,676.8 = 0.5523 |
| 强平后不重入（开环 agent + band） | `exchange.py:303`（`units=0`）+ `online.py:269-272` | `only_BTCUSDT` band0.2 → 5,469 / band0.0 → 99,367 |
| 1x 结果整体被该通道改写 | `scripts/d_param_search.py:64-69`（1x 也开 margin） | `runs/d_param_search/all_configs.csv` 的 `n_liquidated` 130~216（η=0.20 组） |
| 学习回路不可表达双腿 carry | `exchange.py:199`（单净头寸） vs `src/sim/carry.py`（三桶双腿） | M10：BTC alpha 4.88%/t=13.36、ETH 6.25%/t=11.75（PLAN §14.4） |
| D 阶段无 holdout / 无种子 | `scripts/d_param_search.py:12-14` | PLAN §1 铁律 4/6；PLAN.md:184 G2「禁止调参」 |
| 网格最优落在 band 端点 | `scripts/d_param_search.py:53`（`BAND_GRID` 最大 0.20） | η=0.20 与 0.05 两组终值对 band 单调递增 |
| 协议功效低 | `src/eval/protocol.py:509-590` | 真 +40%/年 → 35%（n_trials=1）/ 3%（18） |
| DSR 在 on/off 策略上被豁免 | `protocol.py:469`（`KURT_MAX_WELL_BEHAVED=10`） | 零边际动量规则中位峰度 14.2；学习体 33.5 |
| 假阳率受控（这条是我被推翻的怀疑） | `protocol.py:190-251` | 300 次重复，假阳率 0~1% |
| 数据仅单一来源 | `configs/base.json`（`validation_sources`） | `data/manifest.json` 只有 binance 系列 |

**复现环境**：Python 3.10 / numpy 2.2.6 / pandas 2.3.3；全窗口 = 77,503 根（3 标的交集，
2017-11-06 ~ 2026-09-14）；一次学习体全窗口仿真 ≈ 72 秒。本次审计的全部实验合计约 40 分钟机时。
