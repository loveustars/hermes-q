# LIVE.md —— 前向纸面交易（forward paper trading）

> 状态：**运行中**，起始 2026-09-14 22:40 CST（14:40 UTC）
> 该项目的第一份"未来不可知"的检验。回测里所有防未来函数的努力，
> 在这里由物理事实保证：tick 时刻不存在未来数据。

---

## 1. 这是什么、不是什么

**是**：一个持续运行的纸面组合，用**实时公开行情**推进，逐 8 小时收/付真实资金费，
逐小时记录权益。它回答的问题是"**这套东西在真实时间里往前跑，会不会赚钱、会不会出事**"。

**不是**：不是回测的延长，也不是"验证回测结论"。它的价值恰恰在于**不受我们控制**——
回测里可以反悔（改参数、换窗口、挑种子），这里不能。跑出来的记录是不可编辑的。

## 2. 合规（与项目既定底线一致）

- 只读公开行情端点，白名单在 `src/data/sources.py::ALLOWED_PATHS` 里**代码强制**
- 不接任何交易所账号、不存任何 API key、不调用任何下单端点
  （实测 `/api/v3/order` 会被 `ComplianceError` 拦下）
- `scripts/live_carry.py` **不 import 历史数据层**（`src.data.store`），
  决策只依据 tick 时刻已存在的行情

## 3. 持仓与被检验的配置

| 标的 | 初始本金 | 配置 | 依据 |
|---|---|---|---|
| BTCUSDT | 10,000 | `notional_ratio=0.4, initial_margin_ratio=0.5, rebalance=720h` | M10 网格最优 + G3 封存段判定「有边际」 |
| ETHUSDT | 10,000 | 同上 | 同上 |

结构：现货多 + 永续空，两腿名义额相等（delta 中性）。三桶记账
（现货腿 / 永续保证金 / 备用金），强平判定用**标记价**（与交易所一致）。

## 4. 怎么推进

| 任务 | 频率 | 说明 |
|---|---|---|
| `carry 纸面交易·每小时推进`（cron `0 * * * *`） | 每小时 | 取实时价 → 应用未处理的资金费 → 保证金检查/补保 → 到期再平衡 → 记快照 → 落盘。`no_agent`，静默 |
| `carry 纸面交易·每日摘要`（cron `5 8 * * *` = 00:05 UTC） | 每天 | 读状态文件出摘要 + 异常检查（漏结算 / 推进停摆 / 权益跳变 / 强平） |

Gateway 已装成 systemd 用户服务（`hermes gateway install --start-now --start-on-login`，
已开 linger，登出不影响）。

**幂等是硬要求**：`apply_funding` 只看 `funding_time > last_funding_ms`，
所以 cron 重复触发、或进程重启后重跑，都不会把同一笔资金费算两遍
（`tests/test_live_paper.py::test_funding_is_idempotent` 专门钉这条）。

## 4a. **首次自动结算核验（2026-09-14 16:00 UTC）—— 已通过**

第一个可核验的会计点，四项全部实测通过：

| 项 | 结果 |
|---|---|
| cron 自动触发 | ✅ `2026-09-15 00:00:28` CST 自动跑，无需人工 |
| 代理修复生效 | ✅ 调度器环境里取到了行情（修复前同环境必失败） |
| 资金费金额 | ✅ 与公式 `−perp_units × 结算markPrice × 费率` **误差 0.00e+00** |
| 幂等 | ✅ 重跑一次仍为 1 笔，`funding_total` 不变 |

| 标的 | 费率 | 结算标记价 | 现金流 | 公式复核 |
|---|---|---|---|---|
| BTCUSDT | +0.004220% | 78,543.10 | +0.169052 | +0.169052 |
| ETHUSDT | +0.003649% | 2,507.27 | +0.146448 | +0.146448 |

两笔都是**收到**（正费率 + 空头），与符号约定一致。
`last_funding_ms` 正确推进到 `2026-09-14T16:00:00 UTC`。

> 这一步的意义不在金额（万分之一点七，可以忽略），而在于**整条链路被打通了**：
> 调度器 → 代理 → 实时行情 → 结算记录 → 三桶记账 → 落盘 → 幂等。
> 回测里永远碰不到的就是这些环节。

## 4b. **运维依赖：必须走本地代理**

本机 **DNS 层屏蔽了 `binance.com`**：
- 直连 `curl` 返回 `HTTP 000`、6 毫秒即失败
- `getent hosts github.com` 正常，但 `getent hosts api.binance.com` **失败**
- 必须经本地代理 `127.0.0.1:7897`（实测可用）

而 Hermes gateway 以 systemd 用户服务运行，**它的环境里没有代理变量**。
所以 `~/.hermes/scripts/quant_carry_tick.sh` 里显式导出了 `http_proxy/https_proxy/...`
（默认 `http://127.0.0.1:7897/`，可用 `QUANT_PROXY` 覆盖）。

**2026-09-14 23:00 那次 tick 就是这样失败的**：cron 正常触发、脚本正常找到持仓，
但 `Name or service not known`，一笔行情都没取到。当时的处理是**安静地写了一条
`{"books": []}`**，看起来像"这次没事发生"——这是错的。

已修：现在**一笔行情都取不到就返回非零、并在 ticks.jsonl 里记显式 `error`**，
让 cron 能把它当失败上报出来。数据源中断不该伪装成风平浪静。

> 这个依赖也提示一件事：如果将来真要在境内做实际运营，
> 除了 42 号文的合规问题，**连行情可达性本身都依赖代理**。

## 4c. 路径锚定

`LIVE_DIR` 与 `DEFAULT_LEDGER`（开封账本）都**锚定项目根**，不用相对路径。
早期版本用 `os.path.join("runs", "live")`，隐含假设 CWD 是项目根——
从别的目录跑就 `FileNotFoundError`（实测 `cd /tmp && python3 scripts/live_carry.py --tick` 会崩）。
定时任务长年无人盯着，这类隐含假设必须消掉；而且账本读不到 = **一次性约束直接失效**。

## 4d. 事故记录

### 事故 1：首小时静默失败（2026-09-14 15:00 UTC）

**现象**：cron 正常触发、脚本正常找到持仓，但一笔行情都没取到。
**当时的表现是错的**：安静地写了 `{"books": []}`，看起来像"这次没事发生"。
**根因**：gateway 服务环境无代理变量，而本机 DNS 屏蔽 binance.com。
**修复**：包装脚本显式导出代理；取不到全部行情时返回非零并记显式 `error`。

### 事故 2：代理上游整夜不可用（2026-09-14 17:00 ~ 23:00 UTC 连续 7 小时）

**现象**：连续 **7 次**整点 tick 全部失败，报错
`SSL: UNEXPECTED_EOF_WHILE_READING`（注意：**不是 DNS**，是 TLS 被中途切断）。
时间窗 CST 01:00–07:00 —— 像是有时段性（深夜）。

**影响：零会计误差。** 两笔结算（16:00 与 00:00 UTC）都准时入账，
事件求和与 `funding_total` 分毫不差。原因是两个设计选择：
1. 资金费按**结算时刻记录的标记价**计价，不是按补记时的当前价
2. 查询口径是"自上次已处理以来的全部结算"，所以断流窗口会被下一个成功的 tick 追溯补上

断流窗口 17:00–23:00 里**恰好没有结算点**（结算在 00/08/16），所以连补记都没被触发。
反过来说：**即使窗口跨了结算点，金额也是对的**——这一点由 M10 的幂等设计保证，
不是运气。唯一损失是权益曲线少了 7 小时的粒度（日级快照仍完整）。

**修复**：
- 取数加重试（3 次，退避 2s/4s），吸收秒级抖动。逻辑挪进 `src/live/feed.py` 使其**可测**
  （脚本里的逻辑测不到，重试写错只会表现为"偶尔失败"）
- 日摘要的异常检查**增加"最近 24h 内 error 条数"**这一条：
  原来只看"最后一条 tick 在 2 小时内"，而失败的 tick **也会写进 ticks.jsonl**，
  所以时间戳看起来是新鲜的 ⇒ 7 小时连续失败没被这条判据抓住。
- **小时级中断重试救不了，也不该假装能救**：那种情况就该如实失败并由调度器上报。

### 事故 3：日摘要投递失败（2026-09-15 08:05 CST）

**现象**：摘要任务 `last_status=ok`，但 `last_delivery_error: unknown platform 'webui'`。
**即报告跑出来了、内容正确，但没送到人面前。**
**修复**：`deliver` 从 `origin` 改为 `local`（本会话所在的 WebUI 不是合法投递目标）。
报告落到 `~/.hermes/cron/output/526c739bb321/`，可随时取。

> **教训（三次事故的共同点）**：全都是"看起来正常"的失效——
> 空记录像没事、tick 时间戳新鲜像在跑、任务 status ok 像投递成功。
> **状态字段（status=ok / 时间戳新鲜）不等于效果**，必须去读实际产物。

## 5. 怎么看

> **审计轨迹的读法**：`ticks.jsonl` 里有 2 条 `error=全部标的行情获取失败` 记录
> （**2026-09-14 15:03:26 与 15:04:02 UTC**）是**故意的故障注入测试**——
> 清掉代理变量后运行，用来验证"一笔行情都取不到时必须响亮失败"这条修复是否生效。
> 它们**不是生产故障**，保留在记录里以免抹掉痕迹。
> 真正的生产故障是 **2026-09-14 15:00:14 UTC** 那条（修复前，且当时被静默记成
> `{"books": []}`，看不出是失败）——那条才是要引以为戒的。

```bash
python3 scripts/live_carry.py --status    # 当前状态（含实时价与基差）
python3 scripts/live_carry.py --tick      # 手动推进一次（与 cron 等价）
cat runs/live/BTCUSDT.json               # 完整状态：快照序列 + 事件序列
tail -5 runs/live/ticks.jsonl            # 逐次推进记录
```

## 6. 判定标准（**开始之前就写死**）

预期年化 alpha（依据 G3 封存段）：**BTC 1.50% / ETH 1.33%**。

**按年化波动 0.5%~0.8% 估算的统计功效**（σ 取 0.7%）：
`t ≈ alpha × √T / σ ≈ 2·√T`

| 已运行 | 预期 t | 能说什么 |
|---|---|---|
| 1 个月 | ≈0.6 | 什么都说明不了 |
| 3 个月 | ≈1.0 | 方向性提示 |
| 6 个月 | ≈1.4 | 弱证据 |
| **1 年** | **≈2.0** | 才够格谈"是否显著" |

**所以：至少 1 年才可判断。** 且这个估算偏乐观，两个原因：
1. 假设 alpha 维持 1.4% —— 而它已从全样本的 4.9%/6.3% 衰减到最近的 1.5%/1.3%，
   没有理由认为它停止衰减
2. 分布峰度 44~105（见 PLAN §14.4b），而 t 统计量假设正态 ⇒ **t 值会高估信心**

**因此前向测试的首要产出不是 p 值，而是运行真相**：
资金费有没有算对、状态有没有丢、补保逻辑在真实行情里是否成立、
数据源会不会断。这些是回测**看不到**的东西，也是这套框架真正需要的验证。

## 7. 已知局限（诚实列出）

1. **标记价 vs 成交价**：永续腿按标记价计价与判强平。真实成交会有滑点，
   这里只有一个固定的 `cost_rate=0.0011` 近似。
2. **无部分成交**：再平衡按整笔即时全额成交处理（协议里最后一块已知简化）。
3. **单一交易所**：只用币安公开数据，没有跨交易所对照（回测里 OKX/Coinbase 只做过校验）。
4. **资金费按结算价记账**：用 API 返回的结算时刻 `markPrice`，
   与真实按持仓名义额计价的口径可能有微小差异（<0.1bp 量级）。
5. **不预测费率方向**：策略是"一直持有收 carry"，不择时——这是 M8 的结论
   （择时被切换成本摧毁），不是偷懒。
6. **两本书独立**：BTC 与 ETH 各自 10,000，没有做组合层面的风险预算。

## 8. 如果出事，看哪里

| 症状 | 查 |
|---|---|
| 权益突然跳变 >1% | `runs/live/<SYM>.json` 的 `events`，看是不是补保或再平衡 |
| 资金费长时间不涨 | `last_funding_ms` 是否落后 9 小时以上（说明漏结算） |
| 完全没有新快照 | cron 是否在跑（`hermes gateway status`；看 `ticks.jsonl` 最后时间） |
| 被强平 | `liquidated=true` + `liquidated_at_ms`；这是 G3 未建模尾部风险的真实检验 |
