# WORK_LOG —— 跨 session 交接

**项目**：Hermes-Q（资产无关的自学习交易框架，研究用途，只读公开行情）
**位置**：`/home/nick/workspace/quant`
**最后更新**：2026-09-14
**当前阶段**：M0–M5 全部完成。M5 结论为**无边际**，下一步按淘汰门 G2 转信号源研究。

---

## 一、目标一句话

在扣除真实摩擦成本（含市场冲击）后，在未参与训练的封存数据上，评估智能体是否存在
**可重复、统计显著**的交易边际。产出以框架和评估协议为主，智能体成败其次。

---

## 二、已完成

| 里程碑 | 状态 | 结论 |
|---|---|---|
| M0 骨架与铁律 | ✅ | 冻结配置、实验登记（config hash + 数据版本哈希） |
| M1 数据层 | ✅ | 三源接入，236,383 根 1h，manifest sha256 通过 |
| M2 撮合与成本模型 | ✅ | **换手率一票否决**：日换仓吃掉 95.2% 毛利润，小时换仓账户两个月清零 |
| M3 因果环境 | ✅ | 10/10 测试通过，含未来函数注入与 next-bar-open 成交验证 |
| M4 评估协议 + G1 | ✅ | 首次不通过（100% 假阳性），修掉两个结论级错误后通过（0.0%） |
| M5 在线自学习体 | ✅ | **无边际**：alpha 年化 −13.96%，t=−1.00，p=0.84，DSR 0.70 |
| M6 缺口处理（工程债） | ✅ | 实测 27 处/最长 34 小时；口径敏感性最多 3.1 个本金百分点，零判定翻转 |

---

## 三、M5 的核心结论

在"只用价格数据 + 核心池 BTC/ETH/BNB + 扣成本"的设定下，**自学习体没有显著 alpha**。

最有信息量的一点：**学习体最终把 95.79% 的权重压在 `long_all`（一直满仓等权）上**，
即退化成了最朴素的那个策略，而且还要付 11% 的成本，所以跑输基准（2.5% 成本）。
学习率越大越快收敛到这个结论（专家熵 0.570 → 0.224 → 0.070）。

反过来，**换手控制这一块设计成功了**：学习体年换手 2.0–23.9 次、成本 4.5–29.5%，
而同样没有预测力的随机策略是 2054 次/年、成本 691.8%。
这验证了"学习信号必须用净收益"这条约束是有效的。

---

## 四、M6 缺口处理（工程债已清）

**实测**（对齐后三标的完全一致）：27 处缺口、缺 122 根（0.1574%）、**最长一处 34 小时**
（2018-01-04 起）。BTC 与 ETH 的缺口位置连时间点都相同 ⇒ 交易所级别停机，不是单标的数据问题。

**做法**：标记而非删除（删除会破坏时间轴连续性）。新增 `src/data/quality.py`，
`MarketView.is_gap(t)` 暴露给策略，`SimConfig.gap_policy` 支持 `execute`（默认，贴近现实）与
`skip`（保守对照）。

**敏感性结论**：差异最多 3.1 个本金百分点（只影响高频调仓策略；买入持有与动量 0.000pp），
**零判定翻转** ⇒ 默认 `execute` 保持不变。

**顺带修掉一个错误指标**：第一版用"相对终值"算差异，而小时换仓策略终值 0.98（已归零），
算出 4.45% 的"显著差异"——纯除零噪声。已改为以初始本金的百分点计。

**回归验证**：改动后重跑 `m2_baseline.py`，全部数字与改动前逐项一致
（`bh_BTCUSDT` 101,252 / `buy_hold` 1,580,664 / 动量 148,774 / 冲击表逐项相同）。

---

## 五、踩过的坑（务必先看，全部是"结论级"错误）

1. **收益基数错误**：成本在第一根 bar 扣除，用 `equity[0]` 做收益基数会让净值基数变小，
   净收益百分比反而**高于**毛收益（BNB 毛 45,586% / 净 55,849%）。修复：一律以固定初始本金为基数。
2. **换手统计被截断**：净账本归零后不再交易，换手停止累加（小时换仓策略一度显示 16 次/年，
   真实值 100,266）。修复：换手按**毛账本**统计，成本按净账本统计，并记录归零时刻。
3. **"持有"语义缺失**：agent 返回空 dict 被执行成"清仓"，买入持有曲线是平的。
   契约改为「返回完整目标权重；返回 `None` 表示维持现状」。
4. **向量化快路径不可信**：早期 G1 用近似权重法提速，交叉验证发现与真实撮合器偏差达 134pp。
   已整体废弃——评估器的校准不能建立在近似模型上。
5. **下标错位（最隐蔽）**：策略收益从 warmup 后开始、基准从第 1 根开始，按 `min(len)` 截断对齐，
   导致 `beta` 算成 **0.003**。修复：按时间戳对齐。
6. **基准口径错误**：基准用对数收益均值、策略用简单收益，差半个方差项，
   在 7.7 万根上累积成几千个百分点的虚假 alpha——连"基准对自己"都算出 `beta=1.07`、`alpha=+71%/年`。
   修复：基准改用**同一撮合器、零成本**的等权买入持有组合。
7. **预计算的等价性边界**：滚动统计预计算只在 `t >= n-1` 时与逐根现算等价；
   早期版本没兜住 `t < n-1`，对拍时被抓出 σ 差 6.7e-3。已加回退。

**新增的永久性防线**：`tests/test_protocol.py` 里的
**基准自比基准必须 beta=1、alpha=0、R²=1**。这条测试若早先存在，坑 5 当场就会被抓。

---

## 六、怎么跑

```bash
cd /home/nick/workspace/quant

# 测试
python3 tests/test_causality.py        # 10/10  因果封印与撮合正确性
python3 tests/test_protocol.py         # 13/13  评估协议自洽性
python3 tests/test_data_quality.py     # 11/11  缺口识别与缺口口径

# 数据（可重复执行，只补增量）
python3 scripts/m1_ingest.py

# 成本与基线
python3 scripts/m2_baseline.py
python3 checks_window_sensitivity.py   # 冲击占比的口径敏感性

# 优化正确性对拍（改 MarketView 后必跑）
python3 scripts/verify_precompute.py

# 数据质量诊断
python3 scripts/diag_gaps.py           # 缺口位置与分布
python3 scripts/m6_gap_sensitivity.py  # 缺口口径敏感性（execute vs skip）

# 评估器校准（G1 闸门，约 6 分钟）
python3 scripts/m4_calibrate.py

# 在线自学习体（M5）
python3 scripts/m5_smoke.py            # 冒烟，约 3 秒
python3 scripts/m5_online.py           # 完整评测
```

---

## 七、关键文件位置

```
quant/
├── PLAN.md                       # 设计与里程碑 + 全部实验结果
├── WORK_LOG.md                   # 本文件
├── configs/base.json             # 冻结核心池 + 成本 + 风险 + 评估参数
├── configs/spreads.json          # 实测点差（m1_ingest 生成）
├── src/data/sources.py           # 三源只读适配器 + 合规白名单
├── src/data/store.py             # CSV 落盘 + sha256 manifest
├── src/data/quality.py           # 缺口识别/标记/时间权重
├── src/env/market_view.py        # 因果封印 + 滚动统计预计算 + is_gap
├── src/sim/costs.py              # 手续费 / 点差 / 平方根冲击
├── src/sim/exchange.py           # 双账本撮合仿真
├── src/eval/metrics.py           # 指标（固定本金基数）
├── src/eval/protocol.py          # 时间戳对齐 / PSR / DSR / PBO / alpha 检验
├── src/eval/benchmark.py         # 基准构造（必须与策略同机器同口径）
├── src/eval/holdout.py           # 样本封存守卫（代码级拒绝）
├── src/agents/baselines.py       # L0 基线
├── src/agents/online.py          # M5 在线学习体（11 专家 + Hedge）
├── tests/test_causality.py       # 10/10
├── tests/test_protocol.py        # 13/13
├── tests/test_data_quality.py    # 11/11
└── runs/                         # 实验登记（含 G1/M5/缺口敏感性结果）
```

---

## 八、下一步

**按淘汰门 G2：无显著边际 → 禁止在现有信号上继续调参**（那是过拟合螺旋的入口），
转信号源研究。候选：
- **资金费率与基差**（永续 vs 现货，可观测、可建模，优先级最高）
- **订单簿失衡**（币安公开深度接口可得，与已实现的冲击模型共用数据）
- 链上数据（需第三方源）

其余待办：
1. 1h 数据 27–28 处缺口：回测跨过时 `next-bar` 间隔不是 1 小时，需显式处理或剔除
2. 未实现的成本项：资金费、部分成交队列模型、网络延迟随机扰动
3. 只支持现货多头；做空需接永续并计资金费
4. **协议已知局限**：单持 BNB 在当前窗口 alpha 年化 +13.07%、t=2.30 会被判成接近显著，
   但那是集中度不是技能。协议目前把"押中赢家"也算作 alpha，需在报告里声明。

---

## 九、合规

- 只读公开行情，**不接账号、不存 key、不碰下单接口**（代码级白名单强制，
  非白名单请求抛 `ComplianceError`）
- 不涉及虚拟货币兑换/交易/承销，不触及 2026-02-06 八部委 42 号文禁止的境内业务活动
