# WORK_LOG —— 跨 session 交接

**项目**：Hermes-Q（资产无关的自学习交易框架，研究用途，只读公开行情）
**位置**：`/home/nick/workspace/quant`
**最后更新**：2026-09-15
**当前阶段**：M0–M11 完成。前向纸面交易运行中；**§19 训练目标做空与杠杆路线图
（A/A'/B/B'/C 全部完成；D 阶段规划中）**。

---

## 一、目标一句话

在扣除真实摩擦成本（含市场冲击）后，在未参与训练的封存数据上，评估智能体是否存在
**可重复、统计显著**的交易边际。产出以框架和评估协议为主，智能体成败其次。

---

## 二、已完成

| 里程碑 | 状态 | 结论 |
|---|---|---|
| M0 骨架与铁律 | ✅ | 冻结配置、实验登记（config hash + 数据版本哈希） |
| M1 数据层 | ✅ | 三源接入，236,383 根 1h，manifest sha256 通过 |
| M2 撮合与成本模型 | ✅ | **换手率一票否决**：日换仓吃掉 95.2% 毛利润，小时换仓账户两个月清零 |
| M3 因果环境 | ✅ | 10/10 测试通过，含未来函数注入与 next-bar-open 成交验证 |
| M4 评估协议 + G1 | ✅ | 首次不通过（100% 假阳性），修掉两个结论级错误后通过（0.0%） |
| M5 在线自学习体 | ✅ | **无边际**：alpha 年化 −13.96%，t=−1.00，p=0.84，DSR 0.70 |
| M6 缺口处理（工程债） | ✅ | 实测 BTC/ETH 28 处/BNB 27 处，合计 83 处 378 根（0.1599%）；最长 34 小时；口径敏感性最多 3.1 个本金百分点，零判定翻转 |
| M7 资金费 + 延迟抖动 | ✅ | **发现资金费是摩擦成本的 2627 倍，修正了 M2 的核心结论** |
| M8 carry 可行性侦察 | ✅ | **BTC/ETH 7/7 年为正**；ETH 折算 2x 资本年化 6.80%；择时被成本摧毁 |
| M9 永续数据 + carry 真实成本 | ✅ | 永续 K 线缺口 0；**保证金需求把 6.80% 下调到 3.1%~5.8%** |
| M10 双腿 carry 策略 | ✅ | **项目首个判定「有边际」的策略**（BTC 4.88%、ETH 6.25%，beta≈0）；修掉协议第三条判据的结构性失效 |
| M11 / G3 样本外验证 | ✅ 通过 | 滚动样本外 BTC 7/7 为正、ETH 6/7；**封存段 alpha 降到 1.50% / 1.33%（衰减 3~4 倍）**；BNB 无边际 |

---

## 三、M5 的核心结论

在"只用价格数据 + 核心池 BTC/ETH/BNB + 扣成本"的设定下，**自学习体没有显著 alpha**。

最有信息量的一点：**学习体最终把 95.79% 的权重压在 `long_all`（一直满仓等权）上**，
即退化成了最朴素的那个策略，而且还要付 11% 的成本，所以跑输基准（2.5% 成本）。
学习率越大越快收敛到这个结论（专家熵 0.570 → 0.224 → 0.070）。

反过来，**换手控制这一块设计成功了**：学习体年换手 2.0–23.9 次、成本 4.5–29.5%，
而同样没有预测力的随机策略是 2054 次/年、成本 691.8%。
这验证了"学习信号必须用净收益"这条约束是有效的。

---

## 四、M6 缺口处理（工程债已清）

**实测**（逐标的重新测量，2026-09-14 更正）：

| 标的 | 根数 | 缺口段 | 缺失 | 占比 |
|---|---|---|---|---|
| BTCUSDT | **79,440** | **28** | **128 根** | **0.1611%** |
| ETHUSDT | **79,440** | **28** | **128 根** | **0.1611%** |
| BNBUSDT | 77,503 | 27 | 122 根 | 0.1572% |
| **合计** | — | **83 处** | **378 根** | **0.1599%** |

**最长一处 34 小时间隔（缺 33 根），位置 2018-02-08 01:00 ~ 02-09 09:00。**

> 更正：早期版本写"三标的各 27 处、共缺 122 根（0.1574%）、2018-01-04 起"，
> 那实际是**把 BNB 的数字复制给了三行**，且 **2018-01-04 根本不是缺口**（那天 bar 都在）。
> 详见 PLAN §15.8。对 M6 结论无实质影响。

BTC 与 ETH 的缺口段落连时间点都相同 ⇒ 交易所级别停机，不是单标的数据问题。

**做法**：标记而非删除（删除会破坏时间轴连续性）。新增 `src/data/quality.py`，
`MarketView.is_gap(t)` 暴露给策略，`SimConfig.gap_policy` 支持 `execute`（默认，贴近现实）与
`skip`（保守对照）。

**敏感性结论**：差异最多 3.1 个本金百分点（只影响高频调仓策略；买入持有与动量 0.000pp），
**零判定翻转** ⇒ 默认 `execute` 保持不变。

**顺带修掉一个错误指标**：第一版用"相对终值"算差异，而小时换仓策略终值 0.98（已归零），
算出 4.45% 的"显著差异"——纯除零噪声。已改为以初始本金的百分点计。

**回归验证**：改动后重跑 `m2_baseline.py`，全部数字与改动前逐项一致
（`bh_BTCUSDT` 101,252 / `buy_hold` 1,580,664 / 动量 148,774 / 冲击表逐项相同）。

---

## 五、M7 资金费与延迟抖动（**最重的发现，并修正 M2 结论**）

### 资金费的量级

| 标的 | 条数 | 起点 | 年化均值 | 正费率占比 |
|---|---|---|---|---|
| BTCUSDT | 7,684 | 2019-09-10 | **+11.58%** | 85.8% |
| ETHUSDT | 7,450 | 2019-11-27 | **+13.82%** | 86.3% |
| BNBUSDT | 7,225 | 2020-02-10 | −0.07% | **26.2%** |

永续公共窗口自 2020-02-10 起，57,765 根（6.59 年），覆盖率 100%。

### 成本层级被彻底改写

等权持有三币（6.59 年，持仓 ≈100%，平均权益 88,588）：

- 摩擦成本（手续费+点差+冲击）：**12 USDT**（占平均权益年化 0.002%）
- 资金费（持仓成本）：**32,227 USDT**（占平均权益年化 5.52%）
- **资金费是摩擦成本的 2627 倍**

**⇒ M2 的「换手率一票否决」只对现货成立。** 永续口径下主导成本是**持仓 carry**，
与换手率完全无关：一个从不交易的满仓多头，摩擦成本只 12 USDT，却付 32,227 USDT 资金费。

现货 → 永续的终值影响：等权 −19.2%、动量 −40.8%、随机日换仓 −32.9%、
**单持 BTC −46.0%（Sharpe 从 0.83 掉到 0.02）**。

### 延迟抖动

`latency_bars + Poisson(jitter_mean)`，同种子完全可复现。每档 8 个种子测离散度：

| 策略 | 无抖动 | j=0.5 | j=1.0 | j=2.0 |
|---|---|---|---|---|
| 等权持有三币 | 167,878 | 1.6% | 1.6% | 1.9% |
| 动量（月换仓） | 248,819 | 8.8% | 19.0% | 21.2% |
| 随机（日换仓） | 25,815 | 12.7% | 17.4% | 20.5% |

**⇒ 新增强制协议要求：涉及调仓的结论必须在多个抖动种子下重复。**

### 指向下一阶段的发现

资金费自相关 lag1 = **0.802（BTC）/ 0.787（ETH）/ 0.720（BNB）** ⇒ 高度持续、不是噪声。
且正费率下多头付出、空头收取（单测钉死符号：多头 +10 / 空头 −10，严格对称）。

**⇒ 下一阶段首要候选：资金费 carry。** 收益来源是真实的现金流转移，不是数据挖掘。

---

## 五之二、G3 样本外验证（M11）

### 结论

滚动样本外（1 年样本内 → 半年样本外，各 7 折）：

| 标的 | 样本外年化均值 | 为正折数 | 配置稳定性 |
|---|---|---|---|
| BTC | **+4.64%** | **7/7** | 2 种（nr=0.4、rb=720 一族） |
| ETH | **+5.13%** | **6/7** | 2 种（同族） |
| BNB | −0.85% | 1/7 | 5 种（不稳定） |

样本外与样本内**几乎持平**（BTC 首折 10.10% → 9.70%），没有"选出来就失效"。

封存段一次性开封（2025-01-20 ~ 2026-09-14，1.65 年）：

| 标的 | 封存段年化 | alpha | 自助 95% | 判定 |
|---|---|---|---|---|
| BTC | +1.45% | +1.50% | [+1.16%, +1.85%] | **有边际** |
| ETH | +1.27% | +1.33% | [+0.92%, +1.71%] | **有边际** |
| BNB | −0.08% | −0.03% | [−0.47%, +0.37%] | 无边际 |

### **收益衰减（最重要的事实）**

全样本 4.88% / 6.25% → 滚动样本外 4.64% / 5.13% → **封存段 1.50% / 1.33%**。
最近 1.65 年掉了 **3~4 倍**。滚动样本外没衰减、最近期衰减
⇒ 符合"已知策略被资金压低"的先验。仍为正、区间不跨零，但**幅度已降到 1.3~1.5%**，
是否值得承担运营复杂度是真问题，不能靠"统计显著"回答。

### 工程踩坑

1. **公共时间轴**：早期对现货/永续分别取跨标的交集 → 两条不同索引
   （2017-11 vs 2020-02），却按同一位置区间切 ⇒ 窗口错位 ⇒ 全部强平 ⇒ **折数恒为 0**。
   修复：统一到「所有标的的 (现货 ∩ 永续) 交集」（2020-02-10 起，57,765 根）。
2. **封存守卫按设计拦住了我**：在 `allow=False` 上调 `holdout_slice()` 抛异常。
   A 段用 `allow=False`，B 段另起 `allow=True` 显式开封，写 `runs/_holdout_access.log`。
3. 新增协议函数 `protocol.aggregate_to_clock` / `freq_to_ns`（纯 numpy，不 import pandas），
   配 4 条测试含跨起点对齐。
4. **流程瑕疵（如实记）**：开封实际 **3 次**（每标的一次），严格说打了折扣。

### 待办

统一判定口径（verdict 改为自助区间为主，改了要重跑 G1）；
"开封只允许一次"做成机制约束；补部分成交队列；1.3~1.5% 的商业判断。

---
## 五之三、判定口径统一 + 开封一次性机制（**问题与解决思路**）

详细版在 PLAN §16，这里只留要点。

### 三个问题 → 三个机制

| 问题 | 为什么原来的做法不行 | 落地 |
|---|---|---|
| 判定口径三合一（含 DSR），而 DSR 在峰度 44~105 下不算证据 | G3 三条件恰好同向所以没错，**但那是运气**；下次出现"自助说显著、DSR 说不行"就没有规则可依 | `evaluate_strategy` 分布感知：`KURT_MAX_WELL_BEHAVED=10`，峰度超阈值时 `alpha_p`/`dsr` **仅报告不参与判定**，只留不依赖分布假设的 `alpha_ci`；返回 `criteria`/`binding_criteria`/`waived_criteria`/`failed_criteria`/`verdict_basis` |
| 现金流频率聚合靠人记得 | 忘了就把 t 高估 1.2~1.6 倍 | `evaluate_strategy(..., cashflow_freq="8h")` 自动按时钟桶聚合 + 换算 bars_per_year + 写进 `evaluation_note`；缺索引直接抛错 |
| 开封只靠自觉（G3 实测开了 3 次无人拦） | 铁律 4 当时只是文档里的一句话 | `unseal(purpose, override_reason=None)` + 账本 `runs/_holdout_ledger.json`；不同实例再开同一 seal_id ⇒ 抛 `HoldoutAlreadyUnsealed`；另加**源码级测试**禁止 `scripts/*.py` 直接调裸接口 `holdout_slice()` |

### **意外收获：机制制止了一次不必要的动用**

改完口径后按惯例应"再跑 G3 核对"——那将是第二次开封。但正因为有了账本，
先被迫想清楚"真需要再看一次吗"，于是发现：

**新规则只在退化分支放宽**（非退化分支与旧规则逐字相同），
所以对每个已记录案例可分别按「退化」「未退化」两种分支各算一次判定。
7 个案例**两种分支结论都与原记录一致** ⇒ 不可能翻转 ⇒ **完全不用重读封存段**。

`scripts/verify_verdict_regression.py` 做了这件事，并落了产物。
**0 翻转、0 个"两分支不一致"**（即结论不依赖峰度）。

### G1 重校准：通过，且证明放宽的一支不是漏洞

| 口径 | 窗口 | ② p<0.05 | ③ DSR | ③b 仅自助区间 | ④ 协议判定 | 退化比例 |
|---|---|---|---|---|---|---|
| 毛 | 全窗口 77,503 | 0.0% | 0.0% | **0.0%** | **0.0%** | **100%** |
| 毛 | 仅训练段 58,127 | 0.0% | 0.0% | **0.0%** | **0.0%** | **100%** |
| 净 | 全窗口 | 0.0% | 0.0% | **0.0%** | **0.0%** | 100% |
| 净 | 仅训练段 | 0.0% | 0.0% | **0.0%** | **0.0%** | 100% |

门槛 5% 全过。**关键一行是「退化 100%」**：100 个噪声策略全在退化分支，
判定全程只由自助区间承担，而它 0% 假阳性 —— 回答"豁免两条是不是变松了"：不松。
仅训练段与全窗口一致 ⇒ G1 的 0% 不是靠封存段标定出来的（消除泄漏疑虑）。

### 顺带修掉

1. **G1 窗口与封存段重叠**（轻微泄漏）：G1 用真实行情上的随机权重策略，
   窗口 2023-09-15~2026-09-14 覆盖封存段。加 `--train-only` 开关，两窗口都跑。
2. patch 误删导入（`old_string` 上下文不足，把相邻的 numpy/pandas 导入一起替换了）。
3. f-string 前导空格、嵌套双引号语法错误（改用「」）。
4. 新增的源码级检查第一次跑就报出了残留的直接调用 —— 证明它真会拦人。

### 测试

`tests/test_protocol.py` **32/32**，新增 11 条，其中 3 条是守卫测试：
禁止绕过开封入口 / 退化分布下纯噪声不得通过 / 二次开封必须被拦。

---
## 五之四、前向纸面交易（**运行中**，2026-09-14 起）

详细见 **`LIVE.md`**（判定标准、已知局限、排障表都在那）。

- BTC/ETH 各 10,000 本金，配置 = M10 最优 + G3 判定的「有边际」；每 8h 收资金费、每 720h 再平衡
- 每小时推进（cron `0 * * * *`，no_agent 静默）+ 每天 00:05 UTC 摘要（cron `5 8 * * *`）
- 状态：`runs/live/<SYM>.json`（已入 git，含快照+事件审计轨迹，带裁剪防膨胀）
- 幂等：`funding_time > last_funding_ms`；落盘：先写 .tmp 再 replace

**判定标准（事前写死）**：`t ≈ 2·√T` ⇒ 1 个月无意义、3 个月仅方向提示、**至少 1 年**才够格谈显著。
⇒ 首要产出不是 p 值而是**运行真相**（资金费对不对、状态丢没丢、补保在真实行情里成不成立）。

**启动踩的坑**：① cron 建好但 gateway 没跑 ⇒ "已安排"是假状态，已装 systemd 用户服务
② `time.strftime` 是本地时间却被标成 UTC（差 8 小时，无法对账）
③ 状态文件无界增长 ⇒ 加裁剪（最近 48 条 + 每 UTC 日最后一条）
④ 两处测试期望是我写错的（再平衡费用应精确等于扣费；+24% 打不穿保证金，需约 +49%）

---
## 六、M10 双腿 carry 策略（**首个「有边际」结论，且修掉了协议的一个致命 bug**）

### 6.1 结论

| 标的 | alpha 年化 | beta | t（8h 聚合） | bootstrap 95% | 判定 |
|---|---|---|---|---|---|
| BTCUSDT | **4.88%** | −0.000 | **13.36** | [3.95%, 6.06%] | **有边际** |
| ETHUSDT | **6.25%** | −0.001 | **11.75** | [4.92%, 7.90%] | **有边际** |
| BNBUSDT | 2.80% | −0.000 | 1.12 | [−4.59%, 9.73%] | 无边际 |

**权威 run = `20260914T125453_m10_carry_grid_a0ad13`**。
`20260914T124420_..._4d2315` 是修复判据之前的运行，判全为「无边际」，
两份产物**除 `verdict` 外逐字节相同**（final_equity、funding_income、topup_amount、
t_stat 全部一致）——可用来证明修复只动判据、未动仿真。**别看错那一份。**

run 产物里的 `t_stat = 20.39` 是逐 bar 口径；13.36 是 8h 聚合修正值，
受追踪证据在 `runs/20260914T132343_m10_hac_sensitivity_af9d97/`。
（早期只指 `logs_hac.txt`，但它被 `.gitignore` 排除、不在仓库里 ⇒ 已落成 run 产物。
顺带交叉验证：换成 `protocol.aggregate_to_clock` 后数字逐位一致 13.36/11.75、4.88%/6.25%。）

### 6.1b 判定不能靠 DSR（分布退化）

run 产物 `dsr` 区块暴露 **偏度 7.78 / 峰度 1902.97**。
追查（`scripts/m10_dist_check.py`，受追踪证据在
`runs/20260914T133013_m10_dist_check_47dbe4/`）：

| 标的 | 口径 | 年化波动 | Sharpe | 偏度 | 峰度 |
|---|---|---|---|---|---|
| BTC | 1h | 1.19% | 3.99 | 7.78 | 1902.97 |
| BTC | 8h | 0.54% | **8.81** | 2.85 | 44.25 |
| ETH | 1h | 1.47% | 4.08 | 7.72 | 1025.03 |
| ETH | 8h | 0.80% | **7.44** | 5.38 | 105.12 |

carry 紧对冲 ⇒ 多数 bar 净损益≈0，只有资金费结算/基差跳变时有损益
⇒ 分布退化成「零点尖峰 + 偶尔跳变」⇒ 标准差被压到极小
⇒ **Sharpe 8.81 不可信，DSR=1.0000 不是有效证据**（正态 p 值同样乐观）。

**判定收窄到一条腿**：真正承重的是**截距的块自助区间**（不依赖分布假设）。
结论方向不变，但**不能说"三项判据全过"**。

**协议新增**：现金流频率聚合要扩展到整条链路（Sharpe/DSR/偏度/峰度）；
峰度 > 20 的策略应以**自助区间 + 尾部风险**为主判据。

网格 72 次仿真：存活 68/72，强平仅 4 次且全部在 BNB。
最优配置 `nr=0.4, m=0.5, 再平衡=720`。beta≈0、R²≈0 证实方向中性。

### 6.2 协议的结构性 bug（自 M4 起就存在）

第三条判据 `bootstrap ci_low > 0` 是对 **OLS 残差**做的，而残差均值按构造恒为 0
⇒ **永远不可能通过** ⇒ 任何策略都只能得「无边际」。

症状识别法：bootstrap 区间**不包含点估计**（实测 CI [−0.4%,+0.5%] 而点估计 +4.85%）
——这在数学上不可能，见到就必须查。

修复：对**回归截距本身**做循环块自助（对 (r,b) 成对重采样、每个重采样重跑回归）。
用块级预聚合量加速，O(n_samples × n_blocks)。
守卫测试 `test_evaluate_strategy_can_actually_pass`。
**改了判据必须重跑 G1**：重校准后假阳性率仍 0.0%，未破坏校准。

### 6.3 新协议要求：按现金流频率聚合

资金费 8 小时结算一次，逐 bar 检验把同一笔现金流重复计 8 次 ⇒ t 虚高。
按**绝对时间桶**（不是各自起点的第 k 根）聚合到 8h 后：t 从 20.39→13.36、18.73→11.75，
**虚高约 1.2~1.6 倍**，结论不变。
踩坑：按偏移量聚合成 8h 网格会错开，对齐后不足 10 点、回归退化成 t=0。

### 6.4 再平衡是风控参数

冒烟 [4b]：同样上冲下不再平衡会被强平，每 168 根再平衡则存活
——再平衡按权益缩水自动降杠杆。调参不能只看成本。

---
## 六、M8 资金费 carry 可行性侦察（结构成立）

**符号**：正费率下多头付出、空头收取 ⇒ 空头现金流 `+rate`。窗口 2020-02-10 ~ 2026-09-14（6.60 年）。

| 标的 | 累计 carry | 年化(名义) | 年化@2x资本 | 年化@1.5x | 正年数 | 水下时间 |
|---|---|---|---|---|---|---|
| BTCUSDT | +75.48% | +11.44% | +5.72% | +7.63% | **7/7** | 28.8% |
| ETHUSDT | +89.76% | +13.61% | **+6.80%** | **+9.07%** | **7/7** | 30.2% |
| BNBUSDT | −0.46% | −0.07% | −0.03% | −0.05% | 3/7 | 94.5% |

**含 2022 年熊市仍为正**（BTC +4.16%、ETH +0.79%）⇒ 结构性，不是牛市附属品。

**择时是负收益**：只在费率>0 时持有，切换 964~998 次、成本 212%~220%、净 −120%~−130%。
费率 86% 时间为正，择时省下的远抵不过切换摩擦 ⇒ **连续持有**。

**未计入的风险（必须声明）**：水下时间 28.8%~30.2%；永续空头腿的清算风险；
跨市场划转与对手方风险；**basis P&L 被完全忽略**（只算了资金费现金流），
真正验证需要把两条腿放进撮合器跑（需先采集永续 K 线）。

**必须承认**：carry 是已知的、被广泛交易的策略，不是新发现。价值在于本项目能在扣成本、
可复现、有校准过评估协议的条件下量化它，并说清它还差什么才能可执行。

---

## 七、M9 永续数据与 carry 的真实成本（**下调了 M8 的结论**）

**永续 K 线**：BTC 61,508 根（2019-09-08 起）/ ETH 59,598 / BNB 57,797，**缺口全为 0**。

**基差**：中位 −3.77bp(BTC) / −3.67bp(ETH) / +0.48bp(BNB)，
contango 占比只有 26% / 30% / 51.5% ⇒ **BTC/ETH 大部分时间贴水**。

**二阶 basis P&L**（M8 声明忽略的那一项）：BTC +0.51%/年（占资金费 4.5%）；
**ETH +2.12%/年（占 15.6%，不可忽略）**。但这是首尾单次实现，基差均值回复，**不可外推**。

**挤空风险（永续价滚动最大上冲）**：

| 标的 | 7 日 | 30 日 | 90 日 | 全期 |
|---|---|---|---|---|
| BTCUSDT | 55.1% | **135.5%** | **269.5%** | 3000.9% |
| ETHUSDT | 77.0% | **150.0%** | **321.6%** | 5052.9% |
| BNBUSDT | 192.7% | 796.1% | 1539.1% | 17619.8% |

**保证金需求把收益压下来**（资本回报 = 名义额回报 / (1+m)）：

| 标的 | 撑过 30 日需 m | 年化 | 撑过 90 日需 m | 年化 |
|---|---|---|---|---|
| BTCUSDT | 1.36 | **4.86%** | 2.70 | **3.10%** |
| ETHUSDT | 1.50 | **5.44%** | 3.22 | **3.23%** |
| BNBUSDT | 7.96 | −0.01% | 15.39 | −0.00% |

**⇒ 现实是 3.1%~5.8%，不是 M8 报的 6.80%。**

**carry 的第一号风险不是方向，是永续腿保证金被单独爆掉**：现货腿盈利不能自动
补到永续腿（普通账户），价格最终回归也救不了先被强平的腿。这正是 2021-05、
2022-11 基差交易集体爆仓的机制。⇒ carry 的正确实现必须显式建模保证金账户、
补保规则与强平阈值。**M10 的核心就是这个。**

---

## 八、踩过的坑（务必先看，全部是"结论级"错误）

1. **收益基数错误**：成本在第一根 bar 扣除，用 `equity[0]` 做收益基数会让净值基数变小，
   净收益百分比反而**高于**毛收益（BNB 毛 45,586% / 净 55,849%）。修复：一律以固定初始本金为基数。
2. **换手统计被截断**：净账本归零后不再交易，换手停止累加（小时换仓策略一度显示 16 次/年，
   真实值 100,266）。修复：换手按**毛账本**统计，成本按净账本统计，并记录归零时刻。
3. **"持有"语义缺失**：agent 返回空 dict 被执行成"清仓"，买入持有曲线是平的。
   契约改为「返回完整目标权重；返回 `None` 表示维持现状」。
4. **向量化快路径不可信**：早期 G1 用近似权重法提速，交叉验证发现与真实撮合器偏差达 134pp。
   已整体废弃——评估器的校准不能建立在近似模型上。
5. **下标错位（最隐蔽）**：策略收益从 warmup 后开始、基准从第 1 根开始，按 `min(len)` 截断对齐，
   导致 `beta` 算成 **0.003**。修复：按时间戳对齐。
6. **基准口径错误**：基准用对数收益均值、策略用简单收益，差半个方差项，
   在 7.7 万根上累积成几千个百分点的虚假 alpha——连"基准对自己"都算出 `beta=1.07`、`alpha=+71%/年`。
   修复：基准改用**同一撮合器、零成本**的等权买入持有组合。
7. **符号写反（carry 侦察）**：把"空头收到资金费"写成 `-rate`，整个结论方向颠倒
   （会得出"carry 是负收益"的相反结论）。修复：显式写出符号约定。
8. **预计算的等价性边界**：滚动统计预计算只在 `t >= n-1` 时与逐根现算等价；
   早期版本没兜住 `t < n-1`，对拍时被抓出 σ 差 6.7e-3。已加回退。

**新增的永久性防线**：`tests/test_protocol.py` 里的
**基准自比基准必须 beta=1、alpha=0、R²=1**。这条测试若早先存在，坑 5 当场就会被抓。

---

## 九、怎么跑

```bash
cd /home/nick/workspace/quant

# 测试
python3 tests/test_causality.py        # 10/10  因果封印与撮合正确性
python3 tests/test_protocol.py         # 13/13  评估协议自洽性
python3 tests/test_data_quality.py     # 11/11  缺口识别与缺口口径
python3 tests/test_funding_latency.py  # 20/20  资金费符号与延迟抖动

# 数据（可重复执行，只补增量）
python3 scripts/m1_ingest.py

# 成本与基线
python3 scripts/m2_baseline.py
python3 checks_window_sensitivity.py   # 冲击占比的口径敏感性

# 优化正确性对拍（改 MarketView 后必跑）
python3 scripts/verify_precompute.py

# 数据质量诊断
python3 scripts/diag_gaps.py           # 缺口位置与分布
python3 scripts/m6_gap_sensitivity.py  # 缺口口径敏感性（execute vs skip）

# 资金费与延迟
python3 scripts/m7_ingest_funding.py   # 采集资金费历史
python3 scripts/m7_funding_latency.py  # 资金费量级 + 抖动离散度（约 9 分钟）
python3 scripts/m8_carry_feasibility.py # carry 可行性侦察（快，不跑撮合器）
python3 scripts/m9_ingest_perp.py      # 永续 K 线 + 基差 + 挤空/保证金分析

# 评估器校准（G1 闸门，约 6 分钟）
python3 scripts/m4_calibrate.py

# 在线自学习体（M5）
python3 scripts/m5_smoke.py            # 冒烟，约 3 秒
python3 scripts/m5_online.py           # 完整评测
```

---

## 十、关键文件位置

```
quant/
├── PLAN.md                       # 设计与里程碑 + 全部实验结果
├── WORK_LOG.md                   # 本文件
├── configs/base.json             # 冻结核心池 + 成本 + 风险 + 评估参数
├── configs/spreads.json          # 实测点差（m1_ingest 生成）
├── src/data/sources.py           # 三源只读适配器 + 合规白名单
├── src/data/store.py             # CSV 落盘 + sha256 manifest
├── src/data/quality.py           # 缺口识别/标记/时间权重
├── src/env/market_view.py        # 因果封印 + 滚动统计预计算 + is_gap
├── src/sim/costs.py              # 手续费 / 点差 / 平方根冲击
├── src/sim/funding.py            # 资金费表与结算成本
├── src/sim/exchange.py           # 双账本撮合仿真
├── src/eval/metrics.py           # 指标（固定本金基数）
├── src/eval/protocol.py          # 时间戳对齐 / PSR / DSR / PBO / alpha 检验
├── src/eval/benchmark.py         # 基准构造（必须与策略同机器同口径）
├── src/eval/holdout.py           # 样本封存守卫（代码级拒绝）
├── src/agents/baselines.py       # L0 基线
├── src/agents/online.py          # M5 在线学习体（11 专家 + Hedge）
├── tests/test_causality.py       # 10/10
├── tests/test_protocol.py        # 13/13
├── tests/test_data_quality.py    # 11/11
├── tests/test_funding_latency.py # 20/20
└── runs/                         # 实验登记（含 G1/M5/缺口敏感性结果）
```

---

## 十一、下一步

**按淘汰门 G2：无显著边际 → 禁止在现有信号上继续调参**（那是过拟合螺旋的入口），
转信号源研究。候选：
- **资金费率与基差**（永续 vs 现货，可观测、可建模，优先级最高）
- **订单簿失衡**（币安公开深度接口可得，与已实现的冲击模型共用数据）
- 链上数据（需第三方源）

其余待办：
1. 1h 数据 27–28 处缺口：回测跨过时 `next-bar` 间隔不是 1 小时，需显式处理或剔除
2. 未实现的成本项：资金费、部分成交队列模型、网络延迟随机扰动
3. 只支持现货多头；做空需接永续并计资金费
4. **协议已知局限**：单持 BNB 在当前窗口 alpha 年化 +13.07%、t=2.30 会被判成接近显著，
   但那是集中度不是技能。协议目前把"押中赢家"也算作 alpha，需在报告里声明。

---

## 十二、做空能力扩展（**训练目标改造路线图，A 阶段已完成**）

### 12.1 背景与目标

M5 智能体（`HedgeEnsemble` + 11 个专家）在"只用价格 + 扣成本"设定下被判无边际。
G2 淘汰门之后，重新审视训练目标本身：**当前 `mixed = clip(mixed, 0, None)` 把所有负权重
清零，训练目标只能在 {0, 多} 之间选**，从架构上排除了做空信号。

本路线图目标：让训练目标**能使用杠杆做多做空**，且仿真结果在实盘可复现。
分四阶段（**A、A'、B 已完成**，C、D 规划）：

| 阶段 | 内容 | 风险 | 状态 |
|---|---|---|---|
| **A** 放开做空 | clip(0) → 对称 clip；加做空专家；配置 allow_short | 低 | ✅ 完成 |
| **A'** funding carry 信号 | 把 funding 接进 Hedge payoffs，让 ShortExpert 看到 carry 收益 | 低 | ✅ 完成（但**真实数据几乎无变化**）|
| **B** 加杠杆 | max_gross 1.0 → 3.0；agent/sim 配齐 | 中 | ✅ 完成（**但 Hedge 长期 gross 仍 = 1.0**）|
| **C** 逐腿保证金 + 强平 | 把 `carry.py` 的 margin_cash/reserve 移植到主 sim | **高** | 规划 |
| **D** 参数重标定 | `max_turnover`、`band`、`η` 配合新范围 | 中 | 规划 |

### 12.2 A 阶段变更摘要（**已完成**）

**3 个 commit**：
- `ee87e35` 训练目标: 增加做空专家集合
- `300a652` 训练目标: 放开做空裁剪并配套测试
- `8801252` 做空训练: 补兜底与归一化回归测试

**代码改动**：

| 文件 | 改动 |
|---|---|
| `src/agents/online.py` | 新增 `ShortExpert`（恒为 -1/n）和 `ShortMomentumExpert(24/168)`；`default_experts` 从 11 → 14；`decide()` 的 clip 改为 `[-max_exposure, +max_exposure]`，gross 兜底改用 `Σ\|w\|` |
| `configs/base.json` | `risk` 段加 `allow_short: true` |
| `tests/test_agent_short_capability.py`（新） | 8 条断言：专家存在性、专家提案、端到端 Hedge 学到做空、`_sanitize` 兜底、归一化不变量 |

**关键不变量（被新测试锁死，B 阶段改 max_exposure 时回归会立刻暴露）**：
- 单标的 `|w_s| ≤ max_exposure`
- 总仓位 `Σ\|w_s\| ≤ max_exposure`（等比缩放后）
- 多空等权混合 → `[0, 0]`（保留方向、不翻号）
- `allow_short=False` 时 sim 端仍截断负权重（兜底被覆盖）

**A 阶段的实际能力**（108/108 测试通过）：
- `mixed` 权重范围 `[-1, +1]`（之前 `[0, +1]`）
- 上涨场景 Hedge 学到 `p_long_all` 主导（min_w ≈ +0.11，验证现有 long-only 路径未坏）
- 下跌场景 Hedge 学到 `p_short_all` 主导（终态 p > 2/K，且 min_w < -1e-9）
- **总仓位仍 = 1.0**：做空 0.5 时只能做多 0.5，**离真杠杆还远**

### 12.3 A 阶段没做的事（**这是进入 B 之前必须知道的边界**）

1. **没改 `max_gross_exposure`** —— 还是 1.0。A 只是"放开做空"，没"放大仓位"
2. **没动 sim 层 `_sanitize`** —— 兜底代码 `max(w, 0)` 没改也没删，靠 `allow_short=true` 自然失效
3. **没做保证金/强平** —— sim 里仍然只在 `net_eq < 0.01*initial` 时判定破产，不模拟逐腿爆仓
4. **没改 `risk.max_turnover_roundtrips_per_year: 100`** —— A 不影响换手
5. **没在 LIVE paper 上跑过 A** —— 前向纸面交易还跑着旧 agent，B 阶段才换

### 12.4 B 阶段待办（**启动前要拍板的 3 个分叉**）

1. `max_gross_exposure`：1.0 → **3.0**（你已确认）
2. agent 层 `max_exposure` 与 sim 层 `max_gross` 是否同时改 —— 必须**配齐**，否则任一边是安全网
3. C 阶段（保证金/强平）是否合并到 B 一起做 —— 建议**分开**，风险等级不同

### 12.5 已知坑（务必看）

- **A 没改 base.json 的 `max_gross_exposure`**，所以默认仍是 1.0。要让 A 的"放开做空"真的发挥作用，必须接着做 B 改 3.0
- 旧测试 `test_hedge_ensemble_can_emit_negative_mixed_weights` 用的是**下跌** 600 根合成数据。若改用真实历史数据回归，需重新调阈值（`p > 2/K` 是合成的，真实场景下 Hedge 可能更保守）
- `runs/live/*.json` 没加 `.gitignore`（cron 会自动追加事件，下次 commit 前手动 `git restore`）

### 12.6 A' 阶段：funding carry 信号（**已完成但效果有限**）

**触发**：A 阶段真实数据 baseline 显示 Hedge **完全没学到做空**——下跌段（-41%/-19%）也 0% 做空，只在上涨段 11.1% / 8.6% 短暂做空（短期反转套利）。

**修复**：把 funding carry 接进 `_update_experts` 的 payoffs，让 short 持仓的专家能从 8h 资金费里收到 carry（BTC/ETH 年化 +5~8%）。

**实现**：
- `HedgeEnsemble(funding=...)` 新增可选 `FundingTable` 参数
- `decide(view)` 查上一根 bar 时刻的 funding rate，写进 fr_arr
- payoffs[k] += `target @ fr_arr`（短 carry 对 short 持仓是 +payoff）

**测试新增 2 条**（`test_short_expert_activated_by_positive_funding_carry` / `test_funding_none_keeps_pure_price_signal`），全 110 条通过。

**真实数据发现**（A' vs A 几乎无变化）：

| 指标 | A（无 funding）| A'（有 funding）|
|---|---|---|
| 做空 bar 占比 | 3.9% | 4.0% |
| 终态 p_short_all | 0 | 0 |
| 段 3/5 下跌做空 | 0% | 0% |

**根本原因**：1h 价格波动（σ ≈ 0.4%）>> 8h funding 收益（1bp），Hedge 在 1h 时间尺度上把 funding 当噪声过滤。**这是 D 阶段（参数重标定）的目标**。

### 12.7 B 阶段：3x 杠杆（**先 no-op，后修成真生效**）

**B 阶段首次跑**（2026-09-15 上半段，no-op 状态）：

| 指标 | A'（1x）| B 首次（3x）|
|---|---|---|
| 净终值 | 49,778 | **49,778** |
| max(Σ\|w\|) | 1.00 | **1.00** |
| 终态 p_long_all | 0.9945 | 0.9945 |

**核心发现**：
- **B 首次是 no-op**——`max(Σ\|w\|)=1.0` 而非 3.0，杠杆上限完全没用满
- 根因：`LongExpert` / `InverseVolExpert` 等专家的"满仓值"= `1/n`，**写死**在专家代码里
- 要让 B 阶段真生效，必须改专家"满仓值"或让 HedgeEnsemble 把 `max_exposure` 注入专家 proposal

### 12.7.1 B' 阶段：专家满仓值与 max_exposure 解耦（**已完成**）

**改动**：
- `Expert.weights(view, symbols, gross_target=1.0)` 加 `gross_target` 参数（向后兼容）
- `LongExpert` / `MomentumExpert` / `ReversalExpert` 满仓 = `gross_target / n`
- `ShortExpert` / `ShortMomentumExpert` 满仓 = `-gross_target / n`
- `SingleAssetExpert` 满仓 = `gross_target`
- `InverseVolExpert` 满仓 = `gross_target`（保留比例）
- `HedgeEnsemble.decide()` 把 `self.max_exposure` 传给所有 expert
- 测试 5 条新加，119/119 通过

**B' 真实数据发现（杠杆真生效，且 sim 失真暴露）**：

| 指标 | A'（1x）| B（首次 3x）| **B'（3x 解耦后）**|
|---|---|---|---|
| 净终值 | 49,778 | 49,778 | **-18（破产）**|
| 毛终值 | 52,249 | 52,249 | **-17** |
| 成交笔数 | 1,884 | 1,884 | **7,647**（4x）|
| 换手 / 本金 | 67.5x | 67.5x | **306.6x** |
| 累计成本 | 727 | 727 | **9,539**（13x）|
| **max(Σ\|w\|)** | 1.00 | 1.00 | **3.00** ✅ |
| **max(w)** | 1.00 | 1.00 | **3.00** ✅ |
| 段 1（+42% 涨）做空 | 11.1% | 11.1% | 8.8% |
| 段 1 max gross | 1.00 | 1.00 | **3.00** |
| 破产 | 否 | 否 | **是，bar 1,420**（2018-01 暴跌段）|
| 终态 p_long_all | 0.9945 | 0.9945 | 0.8754 |
| 终态 p_inv_vol | 0.0055 | 0.0055 | 0.1246 |

**B' 阶段的两大发现**：
1. **杠杆真生效了**——`max(Σ\|w\|)=3.0`、单标 `max(w)=3.0`，满仓 +3x 满仓 1.5x/标的
2. **sim 失真暴露**——bar 1,420 触发了 `insolvent_at`（2018-01 暴跌段），但 sim 没真爆仓，agent 继续 3x 仓位交易，**net_eq 持续走到 -18**。这正是 PLAN §19.6 警告的"C 阶段没做 → sim 失真"。

**这正是 C 阶段必须做的原因**：当前 B' 的 alpha **完全是 sim 估值**。实盘上 bar 1,420 的 -40% 暴跌会让 3x 多头爆仓，账户归零，不会再继续交易。

### 12.8 C 阶段（**已完成**）：逐腿保证金 + 强平

C 阶段分 4 个子阶段（C1 / C2 / C3 / C4），全部完成：

**C1 阶段**：
- `src/sim/margin.py`（新文件）：MarginConfig + MarginBook + MarginLeg
- 接口：open_leg / close_leg / topup_to / liquidate / is_liquidatable / needs_topup
- 17 条单元测试，102/102 通过

**C2 阶段**：
- `SimConfig` 加 `margin: MarginConfig | None = None`（默认 None，向后兼容）
- `SimConfig` 加 `max_exposure_per_symbol: float = 3.0`（per-symbol 杠杆上限）
- `_sanitize` 加 per-symbol 限幅（|w_s| ≤ max_exposure_per_symbol）
- `SimExchange` 主循环建仓/平仓时同步调 MarginBook
- 关键决策：**C2 阶段 open_leg 只记账不操作 cash**（因为 SimExchange 已全额扣 cash 买币）
- 2 个集成测试：建仓/平仓时 margin_cash 同步、margin=None 时行为不变
- 136/136 通过

**C3 阶段**：
- `SimResult` 加 `liquidated_legs: list[symbol]` 字段
- `SimExchange` 主循环 step 1.5：每根 bar 用 high 价做压力测试，触发强平
- 强平语义：`release = max(0, margin_cash - penalty)`，退还 margin_cash 本金到 cash_pool
- **关键设计决定**：强平后**允许**重新开仓（discard liquidated 标记），模拟"爆仓后账户归零重建"
- 2 个集成测试：做空 + 价涨触发强平、价格平稳时无强平
- 140/140 通过

**C4 阶段**：
- 新增 `scripts/c_margin_baseline.py`
- 真实数据：3x 杠杆 + initial=0.5 / maint=0.1 强平线

**C 阶段真实数据发现**：

| 指标 | B'（3x 不强平）| **C（3x + 逐腿强平）**|
|---|---|---|
| 净终值 | -18（破产）| **15**（强平后回升）|
| 毛终值 | -17 | -16 |
| 累计成本 | 9,539 | **14,679**（1.5x）|
| 成交笔数 | 7,647 | 7,831 |
| **liquidated_legs** | （空）| **6 个（BNB ×5 + ETH ×1）**|
| 破产 | bar 1,420 | bar 1,420 |
| max(Σ\|w\|) | 3.00 | 3.00 |

**C 阶段核心发现**：
- **逐腿强平真的触发了**（6 次，主要是 BNB）—— sim 失真被强平阻止
- 但 **BTC 1 次都没强平**（Hedge 长期 99% 压在 long_all，但 BTC 暴跌段被 long_all 救下）
- 净终值 15 vs B' -18：**强平阻止了 BNB 持续亏损**（回升 33）
- 但累计成本从 9,539 涨到 14,679（多 5,140）—— 强平后重建仓 + 多次补保的成本

**C 阶段没解决的事**：
- **B' 暴露的"BTC 跌 33% 净资产归零"**：sim 现在 bar 1,420 触发了**破产**（整体 net_eq<1%），但单标的 BTC 1 次都没强平（因为 BTC 实际只跌到 77000，距 -33% 很远）
- **真正问题**：Hedge 长期 99% long_all，**根本不会分散**到做空，杠杆放大了 99% 多头
- **D 阶段（参数重标定）** 才是解决这个问题的关键：让 funding 信号可见、让短动量专家真正学到做空

### 12.10 D 阶段（**已完成**）：参数重标定与 cost 模型 bug 发现

D 阶段跑 18 组（η × band × funding，3×3×2）真实数据搜索，1x 杠杆。

**D 阶段两个核心发现**：

**发现 1：3x 杠杆下参数救不了 sim 失真**
- D1 阶段（3x 杠杆，9 组 + 3 组）12 组全部破产（终值 ≤ 104）
- funding 接入 vs 不接入差别微小（mean sharpe 1.087 vs 1.083）
- η=0.20 + 小 band 触发了 p_short_all=1.0（Hedge 100% 做空），但 max_dd 溢出 1.1e27%
- **结论**：3x 杠杆 sim 失真下参数搜索没用，必须先修 sim

**发现 2：1x 杠杆下 cost 模型对早期 bar 计算异常**
- D2 阶段（1x 杠杆，18 组）跑出 sharpe 0.7-1.9 的"好"结果
- 但 cost_paid.sum() 9 亿 vs final_net 5.5 亿（cost > net_equity 的 1.6x）
- 5000 根跑：成本 595 亿 / 换手 79 万 = **7,545,571 bp**（正常应 ~30 bp）
- **根因**：bar 5000 时 BTCUSDT period_notional=14.7M USDT，bar 10000 时=4.6M（早期市场薄）
  → notional/pn 大 → impact 公式给出"冲击极大"
- 这是**CostModel 在早期市场薄、notional 大的情况下虚高冲击成本**的 bug
- **D 阶段真正贡献**：不是找到好超参，而是**通过参数搜索暴露 sim bug**

**D 阶段 18 组 top-10 by sharpe**（1x 杠杆）：

| η | band | funding | sharpe | total_ret | p_short | p_long |
|---|---|---|---|---|---|---|
| 0.05 | 0.03 | Y | 1.924 | 5,544,682% | 0.000 | 0.995 |
| 0.05 | 0.03 | N | 1.879 | 4,201,493% | 0.000 | 0.989 |
| 0.20 | 0.10 | Y | 1.480 | 739,301% | 1.000 | 0.000 |
| 0.20 | 0.20 | N | 1.454 | 745,886% | 1.000 | 0.000 |
| 0.20 | 0.10 | N | 1.189 | 32,025% | 1.000 | 0.000 |
| 0.20 | 0.20 | Y | 1.160 | 35,813% | 1.000 | 0.000 |

**注意**：这些 "total_ret" 是 sim 计算的，**含 cost 模型 bug**，不可信。

**D 阶段没完成的事**：
- ~~**修 CostModel**：早期 bar 的 impact 需要按 bar 时刻的实际市场规模校准（不能固定用早期 pn 当基准）~~
  ⚠️ **这个诊断是错的，已于 2026-09-15 更正 —— 见 §14。**
  实测 `period_notional` 本来就是逐 bar 更新的（随市场增长 44.8~260 倍），
  从未"固定用早期 pn 当基准"。真正的根因在**保证金模块**，不在成本模型。
- 修完 sim bug 后**重跑 D 阶段**（见 §14.5 的复测结果）
- 修完 cost 后**接 3x 杠杆 + 完整评估协议**（DSR / bootstrap CI）—— 这是真正判断"有边际"的关键

### 12.9 进入 C 之前必须解决的 B 阶段遗留问题

1. ~~**专家"满仓值"与 max_exposure 解耦**~~（**B' 阶段已完成**，见 §12.7.1）
2. **C 阶段（逐腿保证金/强平）必须先于"实盘用"**：B' 真实数据已证明 sim 失真（bar 1,420 触发破产后 agent 继续 3x 仓位交易到 -18，**实盘上不会发生**——3x 多头在 -40% 暴跌段必爆仓）
3. **D 阶段（参数重标定）必须做**：A' 已证明 funding 信号在 1h 时间尺度上被噪声过滤，需要 D 阶段重新选 `η` / `band` 才能让 funding 可见

---

---

## 十三、合规

- 只读公开行情，**不接账号、不存 key、不碰下单接口**（代码级白名单强制，
  非白名单请求抛 `ComplianceError`）
- 不涉及虚拟货币兑换/交易/承销，不触及 2026-02-06 八部委 42 号文禁止的境内业务活动


---

## 十四、修 bug：D 阶段结论的根因是**保证金模块**，不是成本模型（2026-09-15）

**结论先行**：§12.10 把 D 阶段的病态归因于"CostModel 在早期薄市场虚高冲击成本"，
**这个诊断是错的**。真正的元凶是 C 阶段新写的 `src/sim/margin.py`，
它有**三个缺陷**，其中一个是会把任何"杠杆 + 做空 + 保证金"的回测变成**印钞机**。
成本模型只是**报信的**——它忠实地对一笔 1.86 亿的异常委托算了冲击。

### 14.1 证据链（复现 → 隔离 → 定位）

**第一步：复现。** 用 D 阶段同一组配置（`eta=0.20, band=0.10, max_exposure=3.0,
MarginConfig(0.5, 0.1, 0.5), allow_short=True, instrument="perp"`）跑全窗口：

| 进度 | bar | 日期 | 权益 | 累计成本 |
|---|---|---|---|---|
| 5% | 3,860 | 2018-04 | 881 | — |
| 10% | 7,720 | 2018-09 | 206 | 13,395 |
| 25% | 19,300 | 2020-01 | **−0** | **1,243,431,399** |
| 50% | 38,601 | 2022-04 | **17,989,991** | 3,534,287,698 |

权益归零的同时累计成本 12.4 亿 —— **经济上不可能**。单笔名义额最大 **1.86 亿**
（本金 1 万），`part = Q/V` 最高 **6.1**（下单量是市场小时成交量的 6 倍）。

**第二步：隔离。** 同一套数据、同一个 agent，只开关保证金：

| 配置 | 终值 | 最大权益 | 总成本 | 冲击截断 | 强平 |
|---|---|---|---|---|---|
| 1x 无保证金 | 150,692 | 337,065 | 66,928 | 0 | 0 |
| 3x 无保证金 | −498 | 297,184 | 24,881 | 0 | 0 |
| **3x 有保证金** | −726,760 | **210,645,829** | **35,955,131,411** | 379 | 434 |

**爆炸只在启用保证金时出现。** 这就排除了成本模型和主循环。

**第三步：定位。** 逐行读 `margin.py` + 在 `liquidate` 上插桩，找到三处缺陷。

### 14.2 缺陷 1（致命）：强平时凭空造钱

`open_leg` **只记账不扣现金**（注释理由："SimExchange 已全额扣 cash 买币"），
但 `liquidate` 却把 margin_cash **退给现金池**：

```python
# 初版（错）
release = max(0.0, leg.margin_cash - penalty)
cash_pool[0] += release          # ← 看起来在退钱
units_n[s] = 0.0                 # ← 仓位真的被清零了
```

**而且这行"退钱"其实是空操作**：调用点传的是 `[cash_n]` —— 一个装了浮点数**副本**
的列表，改 `cash_pool[0]` 改不到局部变量 `cash_n`。所以实际发生的是：

1. `units_n[s] = 0.0` —— 仓位**真的**消失了
2. 没有任何现金对冲

**对空头（units<0）这就是凭空造钱**：一笔负的负债消失，权益立刻
`+|units| × 价格`。策略于是"学会"故意让空头被强平 —— 434 次强平 ×
每次本金量级 → 权益冲到 2.1 亿。

**修复**：强平 = **在强平价上强制平仓**，与主循环"全额扣现金买资产"的口径一致：

```python
realized = units * exit_price - penalty      # units 带符号，多空自动处理
cash_pool[0] += realized
```

并且**调用点必须用返回值**：`cash_n += self.margin_book.liquidate(...)`。
修完后强平前后权益连续（无罚金时严格相等）—— 这条已写成单测
`test_liquidation_is_cash_neutral_when_no_penalty` 钉住。

### 14.3 缺陷 2：权益口径错，空头一开仓就被强平

初版 `equity = margin_cash + units × price`（仓位的**绝对市值**）。
对空头这在开仓瞬间就是 `0.5N − N = −0.5N`，**恒为负**。代入参数得
"空头在 `p ≥ 0.4 × p_open` 即强平" —— 几乎任何价格都满足。

**修复**：改用**相对建仓价的浮动盈亏** `(price − entry_price) × units`
（新增 `MarginLeg.entry_price`）。修正后 2x 杠杆下：

| 方向 | 强平线 | 含义 |
|---|---|---|
| 多头 | 价跌到 66.67（−33%） | 合理 |
| 空头 | 价涨到 120（+20%） | 合理 |

且**建仓价处两个方向都不触发**（初版空头在这里恒为 true）。

顺带在主循环补了**多空翻转重锚**：`old_units * want_n < 0` 时重新 `open_leg`，
否则翻转后仍拿旧方向的建仓价算盈亏（符号反了）。

### 14.4 缺陷 3：压力测试用错不利价

初版对所有方向都用 `high` 判定。但**多头的不利价是 `low`** ——
用 high 会让多头看起来更安全，所以多头几乎从不会被强平
（与"1x 有保证金强平 206 次但全是空头"的观测一致）。

**修复**：新增 `MarginBook.adverse_price(units, low, high)`，多头取 low、空头取 high。

**附带**：`topup_to` 初版只加 `margin_cash` 不动现金（同类造钱问题）。
主循环并未调用它（只有单测在用），但既然它经 `equity()` 影响强平判定，
一并改成**真实从现金池扣款**、以可用现金为上限。

### 14.5 修复后的复测（同一套数据、同一个 agent）

| 配置 | 终值 | 最大权益 | 总成本 | 冲击截断 | 强平 | 破产 |
|---|---|---|---|---|---|---|
| 1x 有保证金 | 92,695 | 211,452 | 41,917 | **0** | 206 | 否 |
| 2x 有保证金 | 26,265 | 573,060 | 68,412 | **0** | 205 | 否 |
| 3x 有保证金 | −90 | 131,228 | 15,582 | **0** | 217 | **是** |
| 3x 无保证金 | −498 | 297,184 | 24,881 | **0** | 0 | 是 |

**两个可判读的信号**：
1. **杠杆现在单调了**：1x > 2x > 3x（3x 归零）。这符合"策略无边际 ⇒
   杠杆只放大成本"的预期，是修好后的合理结果；修复前是 2.1 亿的假象。
2. **冲击截断归零**：规模正常时平方根律的护栏根本用不上，
   说明它确实是"第二道防线"而非调参旋钮。

**上表是全窗口（77,503 根）的隔离实验。** 另有一个**可重跑的**对照脚本
`scripts/fix_margin_verify.py`（默认前 25,000 根，病态在 bar ~19,300 已出现），
它把**初版实现内联保存**，所以"修复前"的数字永远不会因代码改动而失传：

| 杠杆 | 实现 | 终值 | 最大权益 | 总成本 | 强平 | 冲击截断 | 破产 |
|---|---|---|---|---|---|---|---|
| 1x | **初版** | 41,564,729 | **1,082,371,904** | **57,642,481,825** | 1933 | 1057 | 否 |
| 1x | 修复后 | 18,497 | 34,259 | 4,782 | 88 | 0 | 否 |
| 2x | 初版 | −9,957,247 | 18,393,468 | 52,399,044 | 142 | 2 | 是 |
| 2x | 修复后 | 10,574 | 67,341 | 9,746 | 83 | 0 | 否 |
| 3x | 初版 | −13,105,320 | 11,260,031 | 45,606,303 | 156 | 1 | 是 |
| 3x | 修复后 | 446 | 96,466 | 10,887 | 80 | 0 | 否 |

最大权益差 **31,000 倍**、总成本差 **1.2 亿倍**。守恒不变量：单根最大权益变化
14,517，宽松上界 1,127,780 → **通过**。

关键观察：**初版在 1x 下就已经爆炸了**（10.8 亿）。因为缺陷 2 让空头一开仓就被强平，
缺陷 1 让每次强平都造钱 —— 两者相乘与杠杆倍数无关，**1x 同样中招**。
所以"只有 3x 才有问题"是错的，1x 的结论也必须重跑。

证据：`runs/20260915T035051_fix_margin_verify_d0497d/`（`metrics.json` 含
`continuity_ok` 与 36 项 `grid.*` 对照数字）。

### 14.6 被污染的结论（需要重跑）

- **§12.10 的 D 阶段 18 组全部作废**：那些 `total_ret`（5,544,682% / 4,201,493% …）
  是印钞机的产物，不是策略表现。
- **B' / C 阶段的杠杆结论同样不可信**：它们和 D 阶段共用这条病态记账路径。
- **⚠️ 1x 也中招，不只是 3x**：对照脚本实测**初版在 1x 下最大权益就到 10.8 亿**
  （见 §14.5 表）。原因是缺陷 2 让空头一开仓就被强平、缺陷 1 让每次强平都造钱，
  两者相乘**与杠杆倍数无关**。所以"1x 受影响较小"这个直觉是错的，
  任何启用 `MarginConfig` 且 `allow_short=True` 的结果都必须重跑。
- **不受影响**：M0–M11 的 carry 线（用的是 `src/sim/carry.py`，没有 MarginBook）；
  前向纸面交易（用的是冻结配置，无保证金、无做空）。

### 14.7 两个流程性发现（比这次的 bug 更值得记）

**发现 A：测试里出现了"让断言去迁就 bug"。**
`test_is_liquidatable_uses_high_price_threshold` 原本带着这样的注释：

```python
# high=99: equity=5000+(-100)*99=-4900, maint=0.5*100*99=4950 → -4900 ≤ 4950 → 强平
# 做空 + 价跌也强平（开仓时 margin_cash 不足以覆盖）—— 修正断言
assert book.is_liquidatable(...), "做空 100 单位价不变/跌都会强平"
```

作者**观察到空头在盈利（价跌）时也被强平**，然后去改断言匹配这个行为。
另一条 `test_liquidate_releases_margin_cash_to_cash_pool` 直接断言
`cash == 15_000` —— **把凭空造钱写成了正确行为**。还有测试用了**负价格**。

⇒ **教训**：当一条断言需要写 "—— 修正断言" 这种说明才能成立时，
先怀疑**代码**，而不是先改断言。已随修复重写全部相关断言（本文件 §14 说明保留）。

**发现 B：测试跑法有洞，覆盖率被高估。**
7 个测试文件里 **2 个没有 `main()` 块**（`test_agent_short_capability.py`、
`test_margin_book.py`，都是 pytest 风格），用
`for t in tests/*.py; do python3 $t; done` 跑会**静默跳过它们，且 exit 0**。

⇒ **标准跑法改为此命令**（已写入 §14.8）：

```bash
python3 -m pytest tests/ -q
```

其中 `test_agent_short_capability.py` 覆盖 C 阶段集成（强平 × 主循环），
正是本次 bug 所在的层面 —— **"测过了"和"跑过了"在那两个文件上完全是两件事**。

### 14.8 复现与验证命令

```bash
cd /home/nick/workspace/quant

# 单元测试 —— 必须用 pytest 跑法（见 §14.7 发现 B：两个文件没有 main()，
# 用 `python3 tests/x.py` 会静默跳过且 exit 0）
python3 -m pytest tests/ -q                      # 期望 156 passed

# 复现"修复前 vs 修复后"的对照（初版实现内联保留在脚本里，永不失传）
python3 scripts/fix_margin_verify.py             # 默认前 25,000 根，约 3 分钟
python3 scripts/fix_margin_verify.py --bars 10000  # 更快

# 资金守恒不变量（补上 C 阶段缺失的覆盖层）
python3 -m pytest tests/test_sim_conservation.py -q

# D 阶段重跑（约 21.5 分钟；1x 与 3x 用环境变量区分，不复制脚本）
python3 scripts/d_param_search.py                          # 1x → runs/d_param_search
D_LEVERAGE=3.0 D_OUT=d_param_search_3x python3 scripts/d_param_search.py
```

对照证据：`runs/20260915T035051_fix_margin_verify_d0497d/metrics.json`
（含 `continuity_ok` 与 36 项 `grid.*`）。
作废的旧 D 结果：`runs/d_param_search_INVALIDATED_margin_bug/`（含 README 说明为何不可引用）。

---

## 十五、D 阶段重跑（修复后，2026-09-15）

**触发**：§14 修掉保证金记账缺陷后，D 阶段全部数字作废，必须重跑。

**命令**：`python3 scripts/d_param_search.py`（`LEVERAGE = 1.0`，即 D2 的配置）
**窗口**：77,503 根（2017-11-06 ~ 2026-09-14），3 标的，18 组（η 3 × band 3 × funding 2）
**耗时**：1,290.3s（21.5 分钟，每组约 72s —— 脚本文档里写的"3-5 分钟"是错的）
**产物**：`runs/d_param_search/{all_configs.csv,summary.json}`

### 15.1 结果

| 项 | 数值 |
|---|---|
| 终值区间 | **53,679 ~ 200,595**（本金 10,000） |
| sharpe 区间 | 0.627 ~ **0.838** |
| 最大回撤区间 | **−75.5% ~ −92.0%** |
| 基准 buy_hold 净终值 | **1,584,313** |
| **跑赢基准的组数** | **0 / 18** |

**最优组**：η=0.20、band=0.20、funding=Y —— sharpe 0.838、总收益 +1,907.5%、终值 200,595。

### 15.2 判读

> **⚠️ 修正说明（2026-09-15 同日）**：本节初稿把结论写成"18 组全部跑不过基准"，
> 那句话**具有误导性**，已更正。原因是基准 `equal_weight_buyhold` 用的是
> `BuyHold()`（**只交易一次、不再平衡**）且**零成本**，而策略每根 bar 都在按 band
> 再平衡并付真实成本。两者不可直接比。下面的对照表才是有意义的比较。

**① 基准的 158 倍有 95.6% 来自 BNB**

| 标的 | 起点 | 终点 | 倍数 | 对基准的贡献 |
|---|---|---|---|---|
| BTCUSDT | 7,215.54 | 77,770.01 | 10.8x | 2.4% |
| ETHUSDT | 293.20 | 2,512.72 | 8.6x | 1.9% |
| **BNBUSDT** | **1.70** | **722.71** | **425.1x** | **95.6%** |

而 BNB 恰恰是本项目 M9 自己判定**出局**的标的（波动过大、需 8 倍保证金）。
所以"基准 = 158 倍"实际上等价于"买 BNB 不动 = 425 倍"。

**② 公平对照：学习体输给零智商的等权再平衡组合**

同样引擎、同样成本、同样 band 语义，只换决策：

| 组合 | 终值 | 成本 |
|---|---|---|
| 买入持有（**零成本 + 不再平衡**，= 脚本的基准口径） | 1,584,313 | 0 |
| **等权三币，每 bar 再平衡** ← 公平对照 | **490,657** | 32,366 |
| **★ 学习体 η=0.20 band=0.20（最优组）** | **200,595** | **91,365** |
| 纯 BTC 满仓 | 99,367 | 61 |
| BTC+ETH 各半再平衡 | 96,933 | 5,393 |

**结论**：
- 学习体**输给"等权三币每 bar 再平衡"2.4 倍**（20.1 万 vs 49.1 万），
  而后者零智能、无学习、无预测。
- 同时它的成本是对方的 **2.8 倍**（91,365 vs 32,366）——换手高得多，没换到收益。
- 那些"看起来不如买入持有"的差距，**大部分是再平衡拖累而非选股/择时能力**：
  同一套再平衡逻辑、零智能，就能拿到 49.1 万。

**③ 学习体回避了 BNB——这是跑不过基准的真正原因**

最优组的实际平均权重：

| 标的 | 平均权重 | 平均 \|权重\| | 做多占比 | 做空占比 |
|---|---|---|---|---|
| BTC | **+0.267** | 0.449 | 68.9% | 29.2% |
| ETH | **+0.119** | 0.304 | 64.9% | 33.8% |
| BNB | **+0.043** | 0.229 | 61.0% | **35.6%** |

BNB 的平均权重只有 **0.043**（等权应为 0.333），且 35.6% 的时间在做空它。
**它系统性地压低了那个 425 倍赢家的敞口。** 这与"买入持有基准 95.6% 靠 BNB"
是同一件事的两面：基准赢在不问价格地拿着 BNB，学习体输在一直在削它。

（这不构成"应该满仓 BNB"的建议——那是事后之明。它说明的是：
**这个对比度量的是 BNB 敞口，不是学习能力**；要评估学习能力必须用 ② 那种同口径对照。）

**④ 全部 18 组的区间**（供参考，均已按上面口径重新理解为"相对公平对照"）

| 项 | 数值 |
|---|---|
| 终值区间 | 53,679 ~ **200,595** |
| sharpe 区间 | 0.627 ~ **0.838** |
| 最大回撤区间 | **−75.5% ~ −92.0%** |

**回撤 −75.5% ~ −92.0% 这一条本身就足以否决**：一个回撤 75% 以上的组合，
sharpe 0.838 不能作为"有边际"的证据（任何真实策略都不该有这种形态）。

**⑤ bug 的影响与做空占比严格对应** —— 对 §14 根因诊断最有力的独立验证：

| 做空 bar 占比 | 组数 | 旧/新 终值倍数（中位） |
|---|---|---|
| 0%（几乎不做空） | 6 | **1.0x** |
| 3.9%（偶尔做空） | 6 | **25.0x** |
| 33.8%（经常做空） | 6 | **281.6x** |

**越常做空，旧数字被夸得越狠**，机制正是 §14.2/§14.3 描述的
（缺陷 2 让空头一开仓就被强平、缺陷 1 让每次强平都造钱）。
反过来说，**不做空的配置新旧几乎一样（1.0x）**——若 bug 另有原因，不该有这种对应。

**⑥ 参数搜索实际只有 1 个自由度**：sharpe 与 total_return 的 **Spearman = 1.0000**
（完全同序），所以"按 sharpe 排"和"按总收益排"是同一张表。

**⑦ funding 接入仍近乎无效**：mean sharpe 0.723(Y) vs 0.705(N)、
mean total 955.5% vs 803.6%。与 A' 阶段"1h 尺度上资金费远小于价格波动"一致。

**⑧ 学习体依然是双峰**：低 η(0.01) 全压 `long_all`（做空 0%）；
高 η(0.20) 全压 `short_all`（做空 33.8%，强平 160~216 次）。**没有中间态。**

**⑨ 本次重跑不等于走完评估协议**：本脚本按设计**不跑** DSR / bootstrap /
purged K-fold，也没有开封封存段。上面的结论是**描述性的**，
不构成"该策略无边际"的协议判定——但 ④ 的回撤形态已足以让人不急于走协议。

### 15.3 被本次重跑推翻/搁置的结论

- **D1 的"3x 下 12 组全部破产，参数搜索救不了 sim 失真"不可信**：
  那是 bug 之下的结论，而它正是把整个 D 阶段推向 1x 的依据。
  3x 重跑：`runs/d_param_search_3x/`（`D_LEVERAGE=3.0 D_OUT=d_param_search_3x`）。
- **§12.10 的全部数字作废**，已归档为 `runs/d_param_search_INVALIDATED_margin_bug/`。

### 15.4 顺带修掉的两个报告层 bug

`scripts/d_param_search.py` 的两处**只影响落盘、不影响计算**的问题：

1. `"best_by_sharpe": rows[0] if not rows else None` —— **条件写反**，
   非空时反而给 `None`，所以该字段**恒为 null**（旧产物里也是 null，不是新引入的）。
   已改为在**按 sharpe 排序之后、按 total_return 重排之前**取，并新增
   `best_by_total_return` 字段。
2. 死代码 `LEVERAGE = 3.0` 紧跟着被 `LEVERAGE = 1.0` 覆盖（已删）。

### 15.5 公平对照（受追踪证据）

上表的"等权再平衡"一行初稿来自临时脚本，按项目规矩不能算证据。
已固化为 `scripts/d_fair_comparison.py`（走 `Run` 登记），产物：
`runs/20260915T042959_d_fair_comparison_337208/`。

| 组合 | 终值 | sharpe | 最大回撤 | 成本 | 成交笔数 |
|---|---|---|---|---|---|
| 买入持有（零成本 + 不再平衡，**不可比**） | 1,584,313 | — | — | 0 | — |
| **等权三币 每 bar 再平衡** | **490,657** | **0.9661** | −85.7% | **32,366** | 227,020 |
| BTC+ETH 各半再平衡 | 96,933 | 0.7210 | −89.0% | 5,393 | 145,012 |
| 纯 BTC 满仓 | 99,367 | 0.7246 | −84.1% | 61 | 4 |
| **★ 学习体 η=0.20 band=0.20** | **200,595** | **0.8382** | −75.5% | **91,365** | 5,327 |

**学习体在收益、sharpe、成本三个维度上全部输给"零智能的等权再平衡"**
（终值 0.41 倍、sharpe 0.838 vs 0.966、成本 2.8 倍）。
这是比"跑不过买入持有"更强、也更干净的陈述——因为它排除了再平衡拖累的干扰。

假设按 `falsified_by` 被证伪（见 run 的 `notes.md`）；该对照是**事后补充**的，
不是事前预注册，已在 run 的 `hypothesis.note` 里如实标注。

### 15.6 副作用修复：破产时的**记录语义**

3x 网格暴露出一个次级缺陷：破产后曲线里留着**负权益**，导致下游指标
出现经济上不可能的值 —— `total_return = −205.7%`、`max_dd = −109.9%`。

**更糟的是排名**：脚本按 sharpe 排序，而爆仓组的 sharpe 在数学上失去意义
（简单收益率的**均值**可以为正，而复利终值已经归零），于是出现了

```
=== top-10 by sharpe ===
  💰 η=0.200 band=0.10 funding=Y  sharpe= 0.892  total= -100.0%   ← 已爆仓
     η=0.010 band=0.10 funding=N  sharpe= 0.138  total= -100.0%   ← 已爆仓
```

**"排行榜第一名是彻底归零的组合"**。两处修法：

1. `src/sim/exchange.py`：破产闸门触发时**权益归零而非负值**
   （交易所不会让你欠钱，强平的意义就在于此）；原始盯市值另存
   `SimResult.bankrupt_equity_raw` 供排查"本来跌到多深"。
2. `scripts/d_param_search.py`：**爆仓组不参与排名**，单独列出并只报
   "死时原始权益 / 换手 / 强平次数"；`summary.json` 增加 `n_alive` / `n_bankrupt`。

新增 2 条测试：`test_bankruptcy_records_zero_not_negative_equity`（曲线不得为负）
与 `test_bankruptcy_keeps_raw_overshoot_for_diagnosis`（诊断信息不得被抹掉）。

---

## 十六、3x 重跑：**18 组全部被摧毁**（2026-09-15）

**为什么必须重跑**：D1 阶段那句"3x 下 12 组全部破产，参数搜索救不了 sim 失真"
是**在记账缺陷之下**得出的，而它正是把整个 D 阶段推向 1x 的依据。
如果 3x 的结论只是 bug 的产物，那么"降杠杆到 1x"这个决策就建立在沙子上。

**命令**：`D_LEVERAGE=3.0 D_OUT=d_param_search_3x python3 scripts/d_param_search.py`
**产物**：`runs/d_param_search_3x/`
**耗时**：约 7 分钟（3x 的组死得早，曲线被截断 ⇒ 比 1x 的 21.5 分钟快）

### 16.1 结果

| 结局 | 组数 |
|---|---|
| 爆仓（权益归零、破产闸门中止） | **16 / 18** |
| 未触发闸门，但终值 36~37（本金 10,000，即 **−99.6%**） | 2 / 18 |
| **实质存活** | **0 / 18** |

那 2 组"未爆仓"的是 η=0.010、band=0.03 的 N/Y 两行。它们只是**死得慢一点**：
终值 36 和 37，等于全损。所以正确的说法是 **18/18 全部被摧毁**。

**⇒ D1 的结论在正确记账下依然成立。** 降杠杆到 1x 不是被 bug 误导的决定。

### 16.2 3x 是怎么死的：不是方向错，是成本+强平

几个可读的信号：
- **换手爆炸**：η=0.20 band=0.03 的组做了 **38,718~44,244 笔**（1x 同组才 5,000~25,000 笔）
- **强平密集**：同组 **200~234 次**逐腿强平（1x 同组 160~217 次）
- **死时原始权益为负**：最差一组跌到 **−10,539**（1.05 倍本金），
  说明单根 bar 内亏掉的不止全部权益 —— 这正是 §15.6 那个 clamp 修复要处理的情形

**funding 开关对它毫无差别**：多组的 N/Y 两行数字**完全相同**
（−10,539 / −2,132 / −1,555 各自两行一致）。再次印证资金费在 1h 尺度上可忽略。

### 16.3 一个必须记住的教训：**爆仓组的 sharpe 是假信号**

修复前的 3x 网格给出了这样的"排行榜"：

```
=== top-10 by sharpe ===
  💰 η=0.200 band=0.10 funding=Y  sharpe= 0.892  total= -100.0%   ← 已彻底归零
     η=0.010 band=0.10 funding=N  sharpe= 0.138  total= -100.0%   ← 已彻底归零
```

**第一名是一个本金归零的组合。** 原因是 sharpe 用简单收益率的均值/标准差
计算，而**均值可以为正、复利终值却已经归零**（单期收益低于 −100% 时二者脱钩）。
这不是小数点问题，是"用一个失效统计量给已死账户排名"。

修法见 §15.6：曲线归零 + 爆仓组不参与排名 + funding 对比只用存活组。
**任何按风险调整收益排序的策略搜索，都必须先排除已爆仓的候选**，
否则搜索会自动偏向"在爆掉之前波动很大"的配置。

---

## 十七、独立审计的核验 + 两个新发现的记账缺口（2026-09-15）

**背景**：`FRAMEWORK_AUDIT.md` 是一份独立审计（由另一个 agent 写，见提交 `a516b46`）。
本节是**我对它的实质性断言做的独立复核**，外加复核过程中**新发现的两个缺口**。
受追踪证据：`runs/20260915T055158_audit_verify_419fbe/`（脚本 `scripts/audit_verify.py`）。

### 17.1 审计断言：精确复现 ✓

| 断言 | 复核结果 |
|---|---|
| 清算事件在 **bar 9104 = 2018-11-23 01:00**，BTC `low = 4,239.67` | **逐位一致** |
| 强平阈值解析式 `p/p0 ≤ (1−k)/(1−m)` | `k=0.5 → 0.5556`（−44.4%）；`k=1/3 → 0.7407`（−25.9%）；`k=1.0 → 0`（永不强平） |
| `exchange.py:199` 每标的只有一个净头寸 ⇒ 双腿 delta 中性不可表达 | 确认：`units_n = {s: 0.0 for s in self.symbols}` |

**一条审计没挖到的强旁证**：`src/sim/carry.py` 用的**正是正确参数** ——
`initial_margin_ratio = 1.0`、`maintenance_margin_ratio = 0.005`。代入公式得阈值 0，
对 1x 完全正确。**项目在 M10（唯一通过完整协议的那条线）已经把参数写对了**，
是 D 阶段另起炉灶时传了 `0.5 / 0.1`。

**一处未能复现（已解释）**：审计报 `only_BTCUSDT` 强平后终值 5,469，我复现得 99,367。
差异**完全由 band 解释** —— 审计用的 agent 带 band 且**开环**（不知道自己被强平，
一直重发同一目标 ⇒ 被 band 滤掉 ⇒ 永不重入）；我的定权重 agent 无 band、每 bar 重发。
**机制成立，幅度取决于 band。**

### 17.2 新缺口 ①：**引擎从不结算资金费**（9 个调用点里 8 个）

`SimExchange.__init__(..., funding: FundingTable | None = None)` —— **可选参数，默认 None**。
未传时 `use_funding=False`，**资金费现金流恒为 0**。

实测调用点：

| 脚本 | 是否给引擎传 funding |
|---|---|
| `m7_funding_latency.py` | ✓ 传了 |
| `m4_calibrate.py`（G1）/ `m5_online.py`（学习体）/ `m11_walkforward.py`（G3） | ✗ |
| `a_short_baseline.py` / `b_leverage_baseline.py` / `d_param_search.py` | ✗ |
| `d_fair_comparison.py`（我写的）| ✗ |

**后果（3 标的，margin k=1.0）**：

| 策略 | 引擎不结算 | 引擎结算 | 变化 | 资金费现金流 |
|---|---|---|---|---|
| 被动等权 | 501,687 | **280,415** | **−44.1%** | −98,551 |
| 只持 BTC | 100,757 | **44,701** | **−55.6%** | −28,872 |
| 学习体 η=.2 b=.2 | 233,380 | **142,010** | **−39.1%** | −96,754 |

**⇒ 整条训练线的绝对值虚高约 2 倍。**
项目在 M7 阶段就测出"资金费是摩擦成本的 **2627 倍**"，**却从没把它接进训练线的引擎**——
用自己的最大成本项做回测却不计它。而且 funding 只进了 agent 的内部收益估计
（`HedgeEnsemble(funding=...)`），形成"**agent 以为有成本、引擎从不收费**"的错位。

**次序未翻转**：被动/学习体 2.15x（OFF）→ 1.97x（ON）。
所以相对结论比绝对值稳，但**所有绝对水平都要打折**。

### 17.3 新缺口 ②：**学习体对配置是混沌敏感的**

同一个"2 标的、η=0.20、band=0.20"的策略：

| 差异来源 | 结果 |
|---|---|
| 取数窗口 79,440 根 → 29,502；77,503 根 → 108,420 | **仅差 1,937 根（2.4%）⇒ 终值差 3.68 倍** |
| 引擎 funding OFF → 518,328；ON → 108,420 | **差 4.78 倍** |
| 而同样开关加在**固定权重**（η=0）上 | 13,641 → 11,823，仅 **−13.3%** |

**所以资金费的经济效应是 −13.3%（固定权重上量的），而同一个开关加在真学习体上是
4.78 倍 ⇒ 学习动态把它的影响放大约 4.2 倍。**
乘法权重更新 + band 让路径对任何微小扰动都敏感。

**最干净的隔离（2×2，`scripts/funding_2x2_isolation.py`）** —— 让**引擎的资金费规则
保持不变**（都收费），只改 **agent 知不知道资金费**，于是两格的差异**只能来自决策路径**：

| 引擎 / agent 是否知道资金费 | 终值 | 资金费现金流 |
|---|---|---|
| 不收费 / 不知道 ← **项目现状** | **145,721** | 0 |
| 收费 / 不知道 | 81,399 | 20,812 |
| **收费 / 知道 ← 应当如此** | **29,502** | 12,724 |
| 不收费 / 知道 — 对照 | 49,932 | 0 |

- **纯决策路径效应**（第 2 行 vs 第 3 行，引擎规则相同、只有 agent 的信息不同）：
  **2.76 倍**（1/0.3624）
- **纯经济效应**（η=0 的固定权重组合，见上）：**−13.3%**
- **项目现状 vs 应有状态**：145,721 → 29,502 = **虚高 4.94 倍**

**⇒ 学习动态把资金费的影响放大约 4.4 倍**：固定权重下只有 −13.3% 的经济效应，
而真学习体上放大到 2.76 倍（决策路径）叠加成 4.94 倍（总偏差）。

第 4 格（49,932）与更早的独立脚本 `_verify_probe.py` 逐位相同 ⇒ 该量级经两次独立测量。

**⇒ 项目里所有关于这个学习体的单次数值都不是结论**，必须报分布。
这也是对"测量上界"的直接威胁：如果策略本身是混沌的，任何单路径归因都是在
拟合一朵具体的噪声。

### 17.4 我自己在这一轮犯的三个错（如实记录）

1. **用机制推演代替测量。** 我预测"学习体做空多 ⇒ 应当收资金费"，并据此推断
   漏结算资金费会**翻转**次序（有利于被动组合）。实测**否掉了方向**：学习体付了
   **96,754**，与永远满仓的被动组合（98,551）同一量级——因为它**平均持仓是净多头**
   （+0.267/+0.119/+0.043），做空占的是**时间比例**而非平均仓位。次序没有翻转。
2. **在自己查"漏传 funding"的测试脚本里，自己漏传了 funding。**
   第一版混沌判据（`η=0 → 摆幅 1.0000x`）因此是**我自己的 bug 造出来的**，
   不是发现。修正后 η=0 的摆幅是 0.867x，结论从"纯混沌"改为"经济效应 −13.3%，
   学习动态放大约 6 倍"。
3. **在自己的对照实验里复用了有状态的 agent。** `audit_verify.py` 初版对每个策略
   连跑两次（引擎 funding OFF/ON）却复用同一个 `HedgeEnsemble` 实例。该对象维护
   `self.p` / `self.hold` / `self.eq` / `self.prev_close` / `self.last_target` / `self.log`
   （`online.py:181-192`）跨 bar 累积，且 `_update_experts` 就地改 `self.p` ⇒
   第二次运行**不是从初始权重出发**。被污染的只有 ② 组学习体的 ON 值
   （124,717 → 修正为 **142,010**）；OFF 值来自第一次运行（实例是新的）故未受影响。
   修法：把 agent 写成工厂 `lambda: HedgeEnsemble(...)`，每次运行新建。
   受污染的那份产物已加 README 标注（**不删**）。

**由此暴露一个应当写进项目坑清单的设计缺口**：`SimExchange.run()` **不重置 agent**。
所以"agent 跨运行复用"是一个**静默**错误——不报错、不崩、数字看起来完全正常，
只是悄悄变成了另一条策略路径。建议 `run(agent)` 在入口处或文档里强制这一点
（例如断言 agent 是"新鲜的"，或提供 `agent.reset()` 并由引擎调用）。

**这三条与 §14.7 的教训同源：状态字段 ≠ 效果，机制推演 ≠ 测量。**
`audit_verify.py` 内置的判据这次自己说了"η=0 摆幅也远离 1.0 ⇒ 另有渠道"，
把 2 号错误当场暴露出来——**把判据写进脚本比"记得去查"可靠**；
而 3 号错误是它**没有**判据的地方漏出来的，说明判据要覆盖到"实验卫生"本身。

### 17.5 附加核验：**引擎的资金费算术是正确的**（并发现一条建模缺口）

审查 `pool_ceiling` 的表时发现一处反常：funding ON 时 `long_all` 只跌 0.4%，
而同样 100% 多头、gross 同为 1.0 的 `only_BTCUSDT` 却破产归零。
独立复跑（`scripts/verify_funding_math.py`）后**算术全部对上**：

| 核验项 | 结果 |
|---|---|
| 费率量纲 | BTC 每 8h 均值 0.000106 × 1095 = **11.58%/年** ✓ 与 M7 数据吻合 |
| 手算 vs 引擎 | 手算 **21,797** vs 引擎 **24,269**，比值 **1.11**（差异来自建仓 bar/units 细节）⇒ **无计费 bug** |
| 逐年分解 | 2019: 262 / 2020: 2,555 / **2021: 17,871** / 2022: 1,110 |
| 恒等式 | `long_all` 1,580,663 − 6,206.4 = **1,574,456.6**，与实测终值**逐位吻合** |

**"反常"的解释**：2021 一年付的资金费（17,871）**超过初始本金**——按当年平均名义额
约 58,600 反推，年化费率约 **30%**（牛市多头付高费率，合理）。
而 `long_all` 的终值 **95.6% 来自 BNB（费率≈0）**，所以资金费对它的占比极小。
**不对称是真实的、经济上有意义的，不是 bug。**

**由此得到一条建模缺口（该进坑清单）**：引擎允许现金**无下限变负**来付资金费，
等于向交易所借**无息贷款**。这使买持类策略的资金费成为**纯线性扣减、不吃复利**
（上式逐位吻合即其证据）。现实中会触发追保/强平，成本被复利放大 ⇒
**对买持类策略，引擎低估了资金费**。`only_BTCUSDT` 是靠最终的"权益 ≤ 0 破产闸门"
才被抓到，而不是靠保证金钱。
反向的一侧：资金费表只有 **7,684 条 / 8.7 年**（≈2.4 次/天，而应为 3 次/天），
**约 20% 结算缺失** ⇒ 这一侧又是低估的反面。两者方向相反，**净偏差未量化**。

### 17.6 修正后的公平对照 + 两个 subagent 的产出（2026-09-15）

**（a）用 k=1/L 重跑 `d_fair_comparison`**（`runs/20260915T065628_d_fair_comparison_e7e3b9`，
93 秒）。原产物（`runs/20260915T042959_...`）是在 k=0.5 下跑的，已加 README 标注
"只应作为错误参数的证据留存、不应被任何结论引用"：

| 候选（同引擎 / 同成本 / 同 band 语义） | k=0.5（旧，含假强平） | k=1.0（修正后） |
|---|---|---|
| 等权三币 每 bar 再平衡 | 490,657 | **501,687** |
| BTC+ETH 各半 再平衡 | — | 99,025 |
| 纯 BTC 满仓 | — | 100,757 |
| **学习体 η=0.20 band=0.20** | 200,595 | **233,380** |

**次序未翻转**（501,687 vs 233,380 = 2.15x）："学习体跑不过等权再平衡组合"
**对参数修正是稳的**。修正使学习体 +16.3%、被动 +2.2%（假强平通道**只打击会调仓的
候选**，学习体 mean_gross 只有 0.65 且频繁调仓，是被打击最重的一类）。

**残余污染被干净量化了**：学习体在"有保证金账本 + k=1.0"下 233,380，
而在"完全没有保证金账本"下 325,268 ⇒ **这 28% 的差就是下面这个未修缺陷的残余污染**。

**第四个缺陷（A 与 B 各自独立发现，只诊断未修）**：主循环只在**穿越零点**
（空→有仓 / 有仓→空 / 多空翻转）时才把保证金账本与当前持仓对齐。
仓位被**调大或调小但不穿零**时，`margin_cash` / `entry_price` 仍锚在旧仓位上，
于是权益公式 `margin_cash + (p − p₀)·|u_现|` 不再等于 `|u_现|·p`
⇒ **连 k=1（本该零阈值、永不强平）也能凭空触发强平**。
实测 `inv_vol` 这条逐 bar 再平衡的只做多规则在 k=1 下被强平，
终值 327,938（带账本）vs 456,157（关账本）= **−28%**。

**⇒ k = 1/L 只修掉了一半。** A 新增了 `stale_anchor_bars` / `n_liquidated_stale`
两个诊断计数把它变成可见数字（`pool_ceiling` 的表里全是 0，因为那些候选不调仓）；
修它要动 `exchange.py` 的同步路径（每次成交都重锚），**留作下一轮**。
在此之前，**任何逐 bar 再平衡的候选其结论都带着这 28% 量级的通道污染**。

`config.json` 记录了 `margin.derived_from = "leverage=1.0"` 与
`long_liquidation_ratio = 0.0` ⇒ 产物自描述 ✓

**（b）两个 subagent 的产出**（`runs/20260915T055621_pool_ceiling_bd5ebf`、
`runs/20260915T060059_vol_attribution_ab3b85`）：

**`pool_ceiling`（池子上界）**：`long_all` 在四个 margin/funding 臂里都是池内最优，
且与基准 `ew_buyhold` **逐位相同**（`abs_diff = 0.0`）⇒ **池子的上界就是"零智能买入持有"**。
`n_pool_rules_beating_ew_buyhold = 0`（full 池 18 条规则全没跑赢）。
学习体**在池内排第 3/18、胜过 94%。**

**`vol_attribution`（收益归因层，132/160 完成）**：β 匹配对照 + 四路分解
（`G_exp` 敞口 / `G_trade` 成本 / `G_fund` 资金费 / `G_liq` 强平），
分解残差 **1.78e-15**（数值上精确闭合）✓；归零候选按要求置空 vol/sharpe 且不参与排名 ✓。

**它给出的、与"学习体无价值"相反的结果（我已独立复现）**：

| 池 | 学习体 vs 等权再平衡基准 | 学习体 vs β 匹配被动 |
|---|---|---|
| `full`（含 BNB） | — | — |
| `be`（去 BNB） | **2.63x（赢）** | **1.78x（赢）** |
| `btc`（单币） | 唯一存活者（其余全归零） | — |

**`H2_leader_flips_without_BNB = flips: true`**：去掉 BNB 后池内最优从 `ew_bh`
变成 `exp:mom_720`（3.86x）⇒ **"最优是买入持有"确实由 BNB 驱动**。

**我独立复现了它这个结论所依赖的分母**：`be` 池等权再平衡（收资金费）= **41,248**
（它报 41,247.85）⇒ 比值 2.629x 与它的 2.6285 **一致**。

**⇒ 关键结论：翻转需要两个条件同时满足 —— 去掉 BNB **且** 正确结算资金费。**
因为资金费对"永远满仓"是被动组合的重罚、对低敞口常做空的学习体是轻罚；
而含 BNB 时被动组合终值的 95.6% 靠零费率的 BNB 撑着，资金费伤不到它。
**所以项目至今所有"学习体无价值"的结论，都是在"含 BNB + 不计资金费"这个特定
组合下得出的。**

**审查判定**：179 passed（含新加的 21 条 `test_margin_leverage_semantics.py`）；
无调试残留；未越界（只动 `runs/` `scripts/` `tests/` 与 `src/sim/margin.py`）；
事前假说与可证伪判据齐备；B 的 H1 被它**自己的判据**否掉后如实上报 ✓

### 17.7 审查发现的第四个缺口：**DSR 的试验次数用的是"目录个数"，不是"试验次数"**

`src/registry/runs.py:95`：

```python
def trial_count() -> int:
    """已登记的实验次数 —— DSR 的多重试验惩罚要用它。"""
    return sum(1 for d in os.listdir(runs_dir)
               if os.path.isdir(...) and not d.startswith("_"))
```

**文档说"已登记"，实现是"`runs/` 下的目录个数"**（只排除 `_` 前缀）。实测：

```
trial_count() = 55        而真正有 meta.json 的登记试验 = 44     虚增 11（20%）
```

虚增来源分三类，**两类是多算、一类根本不是试验**：

| 类别 | 例子 | 问题 |
|---|---|---|
| 空目录 | `20260915T062445_...`、`20260915T062340_...`、`b_leverage_baseline`、`c_margin_baseline` | 0 文件，被放弃的 scratch |
| **不是试验** | `runs/live/` | 纸面交易状态目录，被算成一次试验 |
| **同一试验的归档/重跑** | `d_param_search_3x`、`d_param_search_3x_preclamp`、`d_param_search_INVALIDATED_margin_bug` | 同一个网格，应算 1 次而算成 3 次 |

**使用点只有一处**：`scripts/m5_online.py:121` 的 `total_trials = trial_count() + n_trials_this`
→ 喂给 `expected_max_sharpe(sr_variance, n_trials)` → 决定 DSR 门槛。

**实测影响**（`sr_variance` 任取，相对变化一致）：

| n | DSR 门槛（`sr_variance=0.2`） | 相对 n=44 |
|---|---|---|
| 44（真实登记） | 0.9959 | — |
| **55（当前实际）** | **1.0342** | **+3.85%** |
| 158（继续累积后） | 1.2019 | **+20.8%** |

**⇒ M5 的 DSR 结果不可复现，而且随时间单调变严。** 同一份代码、同一份数据，
今天重跑与当时重跑会得到不同的门槛；每多一个 Run 目录就再严一点。
**做家务（删/归档目录）会静默改变一个统计校正的值。**

**修法**（任一）：
1. 维护**显式试验账本**（append-only，记下真正测过的假说数）；
2. 只统计**有 `meta.json` 的登记 run**（当前 44，且能排除 `live/` 与空目录）；
3. 由**实验设计**显式传入 `n_trials`（网格大小）。

项目里已有正确做法的先例：`pool_ceiling` 把自己的试验数写进了 `config.json`。

**✅ 已修（2026-09-15，按用户选定的"正解"）**：

1. **`configs/base.json` 新增 `eval.cumulative_trials = 77`** + 同处的
   `cumulative_trials_note`（口径、构成、已知下界、维护规则全部写明）。
   构成：m5 `3η×3band=9` + 5 基线 = 14；m10/m11 同一网格 `3×3×3` = 27
   （walk-forward 是同一批配置的再评估，不重复计）；`d_param_search` 18(1x)+18(3x) = 36。
   **已知为下界**：`pool_ceiling` 72 与 `vol_attribution` ≤241 未计入
   （它们是"对固定规则集做归因/稳健性分析"，不是为挑选配置而搜索）；
   若日后把它们也定义为搜索，必须上调此值。
2. **`scripts/m5_online.py`**：删掉 `from src.registry.runs import ..., trial_count`，
   改为读 `cfg["eval"]["cumulative_trials"]`；**缺该字段则直接 `SystemExit`**
   （不允许退回目录数推断）。运行时打印试验数口径。
3. **`src/registry/runs.py::trial_count()`** 的文档改为**警告**：说明它数的是目录个数
   而非登记数、不可复现、会让门槛随目录累积变严，**不得用于 DSR**；保留仅供诊断。
   并指向两处正确用法（`m11_walkforward` 的 `n_trials=len(GRID)`、本处配置）。

**新增回归测试 `tests/test_trial_count_reproducibility.py`**（6 条，全套 **185 passed**）：

| 测试 | 守什么 |
|---|---|
| 配置里存在且为正整数 | 字段不许丢 |
| 必须带非空的构成说明（≥40 字且含分项数字） | 防止又变成一个无从审计的魔法常数 |
| **AST 级**检查 m5 不得调用 `trial_count()` | 注释里的 `trial_count()` 不算（避免误伤说明文字） |
| m5 必须显式读 `eval.cumulative_trials` | 口径不许回退 |
| `expected_max_sharpe` 对同参数是纯函数、且随 n 单调不减 | 门槛自身的性质 |
| 目录变动不得移动门槛 | **核心回归** |

**"测试这个测试"**：临时把 `trial_count()` 调用加回 m5 ⇒ `test_m5_does_not_call_trial_count`
**当场失败**（不是空测试）；还原后全绿。

**可复现性实证**（改动前无法通过、改动后会通过）：

```
声明值 n = 77
  变动前: trial_count()=56   门槛=1.089966
  变动后: trial_count()=61   门槛=1.089966      ← 造 5 个目录
  ⇒ 目录数变了 5，DSR 门槛变化 +0.000000
```

即**做家务不再改变一个统计校正的值**。附带证据：本函数在本次会话期间自己从 **55 漂到 56**。

### 17.8 两个 subagent 产物的复审（通知触发，2026-09-15 晚）

一条后台通知（`proc_0c267c021a0a`）暴露了 **B 早先版本**的一次崩溃：

```
vol_attribution.py:574  "funding_paid": rec["funding_paid"]  →  KeyError: 'funding_paid'
```

**提交的版本已修好**（第 599/627/799 行都是 `rec.get("funding_paid", 0.0)`）⇒ 交付代码无此
bug、无需改。**但它暴露了我审查的真实缺口：我只跑了测试套件、没有端到端跑它的脚本。**
现已补做（`runs/_smoke_vol_attribution`，`_` 前缀避免污染 `trial_count`）。

**报告与实际不符两处**（`runs/20260915T064200_vol_attribution_c9c280`）：
1. **B 的 `REPORT.md` §8 把 `config.json` / `meta.json` 列进产物清单，实际两者都不存在**
   ⇒ 这份"权威 run"**没有走项目的 Run 登记**。假说与判据确实记在 `metrics.json` 里
   （`H1_*`/`H2_*`/`status: complete`/`n_done: 135`），但**引擎设置只在报告正文里、非机器可读**。
2. 同一份报告把 `n_done` 写作 135/141，而 `metrics.json` 的 `n_planned = 160`（另一份是 241）。
   计划数在不同 run 之间不一致，没有单一权威值。

**B 报告的质量另说，是好的**：它有一份**示范性的不确定清单**（8 条），其中
- 明确说"委托人实测的窗口 79,440 vs 77,503 差 3.6 倍**我未能独立复现**，只能引用、不能背书" ✓
- 主动声明"`full` 池那两个显著正 α 未做 DSR 校正，**我不主张它是 α**" ✓
- 明确说"强平通道的正确修法我没做，超出我的文件权限" ✓
- 自证清单可复跑：BM1 对自身回归 β=1.000000 / α=−1.47e-15 bp / R²=1.000000、
  四块可加性 1.8e-15、`long_all ≡ ew_buyhold` |Δ|=0.0 ✓
- 且**主动与我的实测交叉对齐**（280,415 / 142,010 / −96,754 / 44,701 全部逐位一致）✓

**它独立复现并推广了我关心的锚点缺陷**，推广式为
`p/p₀ ≤ (1 − a/p₀)/(1 − m)`，`a = k·p₀·|units_anchor|/|units_now|`
⇒ **持仓涨大时阈值上移：越赚越容易被强平**。实测 BTC 在 `p/p₀ = 0.757`（预测 0.7578）、
ETH 在 0.679 被强平，阈值从 −44.4% 挪到 −24.3%。并给出 `G_liq`：`be|hedge` = **−64.3%**（37 次）、
`full|hedge` = −33.5%、`btc|hedge` = **+1.0%（对空头为正）** ⇒ **通道是双向的：
它选择性惩罚换方向的策略、补贴做空**，"bug 让一切变差"不成立。

**它还测出 `m` 也必须一起修**：`be|hedge` 在 `k=1,m=0.1` 是 108,420，而 `k=1,m=0.005`（`carry.py` 的量级）
是 **149,054（+37%）**；`m=0.1` 给空头强加 `p/p₀ ≥ (1+k)/(1+m) = +81.8%` 的停损，
`m=0.005` 推到 +99.5%。⇒ **只改 `k` 不够，`m` 同样是人为量级。**

### 17.9 审查中确认的第五个缺口：**"成本是主要摩擦项"在永续口径下方向错了**

B 的四块分解在**被动型候选上一致显示**：

| 候选 | 交易成本 `G_trade` | 资金费 `G_fund` |
|---|---|---|
| `be|ew_rebal`（基准自己） | −11.1% | **−87.6%** |
| `be|exp:inv_vol` | −2.1% | **−104.7%** |

**资金费比交易成本大一个数量级**（8/40 行 `|G_fund| > |G_trade|`，被动型普遍如此）。
**而项目在 M7 之后的叙事一直把"成本"当作主要摩擦项** —— 在**现货**口径下那是对的，
在**永续**口径下主项是资金费。这与 §17.2（引擎根本没结算资金费）是同一个认知错位的两面。

### 17.10 审查发现的第三个"看起来配好了、其实没接线"缺口：**`risk.allow_short` 是装饰性的**

- `SimConfig` **没有 `from_config`**（实测 `grep` 无此方法），只能逐字段构造
- **没有任何代码读 `cfg["risk"]["allow_short"]`**

⇒ 那个配置项**没有任何效果**。真正生效的是脚本里显式传的 `SimConfig(allow_short=...)`。

实测各脚本：

| 显式传了 `allow_short=True`（能做空） | 没传 ⇒ `_sanitize` 把空头截成 0 |
|---|---|
| `c_margin_baseline` / `d_param_search` / `d_fair_comparison` / `m7_funding_latency` / `pool_ceiling` / `vol_attribution` | **`a_short_baseline`** / `b_leverage_baseline` / `m4_calibrate` / `m5_online` / `m11_walkforward` |

**最严重的一条**：`scripts/a_short_baseline.py:73` 用
`SimConfig(initial_cash=10_000.0, warmup=300)`（既无 `instrument` 也无 `allow_short`）
⇒ `exchange.py:175-176` 的 `if not self.cfg.allow_short: w = max(w, 0.0)`
把**全部空头截成 0**。**这个"验证 A 阶段放开做空"的基线，实际跑的是纯多头。**

而 A 阶段的验收标准写的是"`min_w < -1e-9`（证明真在做空）" —— 那是**agent 输出的
`mixed` 权重**，不是**引擎执行的仓位**。所以它验证的是"agent 能提出负权重"，
**从未验证"引擎执行了负权重"**。这正是缺口所在。

**同类缺口的第三次出现**（前两次：`funding=` 漏传 8/9 处；`initial_margin_ratio`
硬编码）——共同形态是：**配置/接口允许某个能力，但实际调用链没有把它接通，
而"看起来是通的"**（配置里有、签名上有默认值）。建议的修法方向：
`SimConfig` 加 `from_config(cfg)` 并**从配置读 `risk.allow_short`**，
或在构造处**断言配置与参数一致**（配置说 true 而参数是 False 就报错）。

**影响范围（已逐项查清，不要夸大）**：

| 结论 | 是否受影响 | 依据 |
|---|---|---|
| A 阶段"**Hedge 完全没学到做空**"（下跌段 0%、做空占比 3.9%） | **不受影响** | 那是 **agent 侧**指标（`mixed` 权重），与引擎是否执行无关 |
| A' 阶段"funding carry **效果有限**"及其诊断（1h 波动 >> 8h funding） | **不受影响** | 同上，是 agent 的行为描述 |
| `a_short_baseline.py` 作为一个"**做空**基线"的结果 | **无效** | 它跑的是纯多头，从未执行做空 |
| `b_leverage_baseline.py` 作为"**杠杆**基线" | **有效**（杠杆经由负现金实现，不依赖 `allow_short`） | `max_gross=3` 生效 |
| `b_leverage_baseline.py` 作为"**做空 + 杠杆**"的证据 | **无效** | 空头被截断 |
| C / D 阶段（`c_margin_baseline` / `d_param_search` / `pool_ceiling`）的做空 | **有效** | 三者都显式传了 `allow_short=True`；D 网格 `short%` 到 33.9%、`p_short_all=1.000` 即证据 |
| M5 / M11（`m5_online` / `m11_walkforward`） | **不受影响** | 那是 long-only 时代的设计，本就该只做多 |

⇒ **净结论：这个缺口只让 A / B 两个"基线"名不副实，没有污染 C/D 及更早的结论。**
但必须记下来——因为**"做空在 A 阶段就跑过了"这个印象是错的**，
而它本可以用来质疑 A' 阶段那句"效果有限"。

### 17.11 审查发现的第六个缺口：**断点续跑把"缺字段"变成了"资金费为零"**（已修）

一条后台通知（`proc_50bbfc25bf28`）报 `vol_attribution.py:620` 的 `KeyError: 'funding_paid'`。
**行号与先前那次（574）不同**，说明那是**陈旧进程在跑更早版本的文件** ⇒ 该崩溃本身无需处理。
**但它把两条我审查时没走到的路径摆到了台面上**：

1. **`assemble()` 从已存在的检查点恢复**这条路径我一次都没测过 —— 我的冒烟测试是**全新运行**
   （无 ckpt），所以从不经过 `rows_from_ckpt`。
2. 顺着它查出真问题：**旧检查点的记录里没有 `funding_paid`**，
   而读取端为防御写成 `rec.get("funding_paid", 0.0)` ⇒
   **"字段不存在"被静默变成"资金费恰好是 0.0"**，在归因表里与
   "这个策略真的没付过资金费"**无法区分**。

**实测证据**（拿真实旧 ckpt `/tmp/vol_ckpt.json`，64 条、无 `funding_paid`，恢复跑）：

| 候选 | arm | 净终值 | `funding_pct_of_initial`（修复前） | 真值 |
|---|---|---|---|---|
| `btc_bh` | 资金费 ON | 0 | **0.0** ✗ | 242.7% |
| `btc_rebal` | 资金费 ON | 44,701 | 288.7 ✓（新记录） | 288.7% |

⇒ 旧记录的 0.0 与"资金费开关关闭"那一臂的 0.0 **在外观上完全相同**。
`G_fund` 退化成 `nan`（分解有守卫，这一侧没骗人，但那一侧的 0.0 骗了）。
**这比崩溃更危险：崩溃会停，静默错值会进结论。**

**修法（已实施）**：
- 新增 `CKPT_SCHEMA = 3` / `CKPT_REQUIRED = ("funding_paid", "n_funding_events", "bankrupt_at")`；
- 抽出**纯函数** `drop_stale_ckpt_records(ckpt)`：丢弃 schema 不匹配的记录并返回键名；
- 主流程在续跑前调用它，并**大声报出丢弃条数、原因与样例键**；
- `save_ckpt()` 写入 `__schema__` 标记（键解析要求 3 段，故标记不会被当成记录）。

修复后的实测输出：

```
断点：/tmp/vol_ckpt_compat2.json（已有 0 条，迁移 61 条，schema None → 3）
⚠️ 丢弃 64 条旧版本记录（缺少 (...funding_paid, n_funding_events, bankrupt_at) 中至少一项）
   —— 它们**会被重跑**。沿用会把『资金费』静默写成 0.0，与『真的没收过资金费』无法区分。
```

修复后表里 **0.0 只出现在资金费确实关闭的那一臂**，开启的臂显示 242.7% / 288.7% 真值。

**新增回归测试 `tests/test_vol_attribution_ckpt.py`**（8 条，全套 **193 passed**）：
旧记录被丢弃且返回键名 / 合规记录不被误删 / schema 标记不被误删 /
非 dict 值不崩 / **必需字段必须含 `funding_paid`** / **守卫必须真被 `main` 调用**（AST 级）/
脚本必须有 `__main__` 守卫（否则导入即跑实验）/ 把这次的真实旧 ckpt 固化成一条实证检查。

**"测试这个测试"**：摘掉守卫调用 ⇒ `test_guard_is_actually_wired_into_main` **失败**；
从 `CKPT_REQUIRED` 拿掉 `funding_paid` ⇒ **2 条失败**；还原后全绿。

### 17.12 对已提交产物的复核：**警报是误报，但它挖出了一处"靠运气干净"**（2026-09-15）

`ab3b85` 的完成通知到达后，我按 §17.11 的判据复核了它已提交的归因表，
发现 **14 行「声明引擎收资金费（`engine_funding=True`）却 `funding_pct_of_initial=0.0`」**。
其中若干行跑满 **77,201 根全窗口**、持仓策略，却记 **`n_funding_events=0`** ⇒ 看上去是静默错值。

**逐条追查后：不是 bug，14 行的 0 都是真值。**

| 原因 | 行数 | 机制 |
|---|---|---|
| 全现金、从无持仓 | 2 | `exp:flat` |
| k=0.5 假强平**开仓即触发** → 永久空仓 | 7 | `k05_*`；10,000 × 0.5556 ≈ 5,556 ≈ 实测 5,469，与资金费无关（故 `k05_head` ≡ `k05_nofund`） |
| 满额做空在阈值 `(1+k)/(1+m) = +81.8%` 被强平 → 永久空仓 | 5 | `exp:short_all` / `exp:short_mom_720`；BTC 从 2017-11 的 7,676 到 12 月的 13,800 就是 **+80%**，进场数周内即触发 |

独立复现确认（用脚本自己的候选工厂）：`k1 | exp:short_all` → `n_trades=2`、`liquidated_legs=2`、
权益从 9,763 迅速塌到 **1,166 并长期平在那里**，之后全程空仓 ⇒ 一次资金费都没结算 ✓

**⇒ 归因表干净，但干净的原因需要记录**（下条）。

**它挖出的一处真问题：`ab3b85` 里混着 61 条旧 schema 记录**（标记 = `n_funding_events is None`，
因为该列读取时**无默认值**，正是我刻意留的"新旧记录判别器"）：

```
新记录 engine_funding=False →  14 行
新记录 engine_funding=True  →  89 行
旧记录 engine_funding=False →  61 行   ← 危险组合「旧记录 且 收费」= 0 行
```

**⚠️ 更正（同日晚，复核 `c9c280` 时发现）：此处我先前写的"靠运气干净"是错的。**
旧记录**不可能**落进收费臂，原因是**结构性的**：旧版脚本**根本没有"引擎资金费"这一维**，
它写出的变体名只有 `OFF` / `k1` / `k05`，而迁移映射把这三个**全部**指向资金费**关闭**的变体：

```
rename = {"OFF": "C_nofund", "k1": "k1_noengfund", "k05": "k05_nofund"}
ENGINE_VARIANTS["C_nofund"]    = ("OFF", False, ...)   engine_funding = False
ENGINE_VARIANTS["k1_noengfund"] = ("k1",  False, ...)   engine_funding = False
ENGINE_VARIANTS["k05_nofund"]   = ("k05", False, ...)   engine_funding = False
```

⇒ **危险组合按构造不可能出现，且旧记录的 `funding_pct_of_initial = 0.0` 是真值**
（旧脚本确实不结算资金费）。`ab3b85` 与 `c9c280` 两个产物都因此**在数值上正确**。

**但残留一个真问题（`c9c280` 独有，3 行）**：`k1m005` 这个变体名**不在迁移映射里**，
于是那 3 条记录以旧名存活、`ENGINE_VARIANTS.get("k1m005") = (None, None)`：

| variant | candidate | 净终值 | `funding_pct_of_initial` | `engine_funding` | `n_funding_events` |
|---|---|---|---|---|---|
| `k1m005` | `exp:long_all` | 88,161 | 0.0 | **None** | None |
| `k1m005` | `exp:short_all` | 0 | 0.0 | **None** | None |
| `k1m005` | `exp:short_mom_1` | 2 | 0.0 | **None** | None |

**数字对（88,161 正是"资金费 OFF"下的值），但标签错**：`k1m005` 不是真实存在的臂，
读者可能把它当成"k=1、m=0.005 且**收**资金费"的那一档 —— 实际它是旧脚本在**不收**资金费下跑的。
`engine_funding = None` 是唯一诚实的提示（"这一行的资金费口径未知"）。

**这 3 条已被 §17.11 的守卫覆盖**：实测它们只有 **13 个字段**、既无 `funding_paid` 也无
`n_funding_events` ⇒ `drop_stale_ckpt_records` 会**丢弃并重跑**它们。
`c9c280` 是修复**之前**产生的，所以保留了它们。

**附带一条应写进归因表读法的告诫**：**强平通道会把若干候选的持仓清得极早**，
使它们的资金费/分解读数**按构造恒为零** ⇒ 任何涉及这些候选的"资金费对比"都是空洞的。
读 `attribution_*.json` 时应先看 `n_funding_events` 与 `n_liquidated`：
**`n_funding_events = 0` 且 `n_liquidated > 0` 的行，其资金费块无信息量**，
不应被拿来论证"资金费无关紧要"。
