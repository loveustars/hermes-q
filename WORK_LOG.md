# WORK_LOG —— 跨 session 交接

**项目**：Hermes-Q（资产无关的自学习交易框架，研究用途，只读公开行情）
**位置**：`/home/nick/workspace/quant`
**最后更新**：2026-09-15
**当前阶段**：M0–M11 完成。前向纸面交易运行中；**§19 训练目标做空与杠杆路线图
（A/A'/B/B'/C 全部完成；D 阶段规划中）**。

---

## 一、目标一句话

在扣除真实摩擦成本（含市场冲击）后，在未参与训练的封存数据上，评估智能体是否存在
**可重复、统计显著**的交易边际。产出以框架和评估协议为主，智能体成败其次。

---

## 二、已完成

| 里程碑 | 状态 | 结论 |
|---|---|---|
| M0 骨架与铁律 | ✅ | 冻结配置、实验登记（config hash + 数据版本哈希） |
| M1 数据层 | ✅ | 三源接入，236,383 根 1h，manifest sha256 通过 |
| M2 撮合与成本模型 | ✅ | **换手率一票否决**：日换仓吃掉 95.2% 毛利润，小时换仓账户两个月清零 |
| M3 因果环境 | ✅ | 10/10 测试通过，含未来函数注入与 next-bar-open 成交验证 |
| M4 评估协议 + G1 | ✅ | 首次不通过（100% 假阳性），修掉两个结论级错误后通过（0.0%） |
| M5 在线自学习体 | ✅ | **无边际**：alpha 年化 −13.96%，t=−1.00，p=0.84，DSR 0.70 |
| M6 缺口处理（工程债） | ✅ | 实测 BTC/ETH 28 处/BNB 27 处，合计 83 处 378 根（0.1599%）；最长 34 小时；口径敏感性最多 3.1 个本金百分点，零判定翻转 |
| M7 资金费 + 延迟抖动 | ✅ | **发现资金费是摩擦成本的 2627 倍，修正了 M2 的核心结论** |
| M8 carry 可行性侦察 | ✅ | **BTC/ETH 7/7 年为正**；ETH 折算 2x 资本年化 6.80%；择时被成本摧毁 |
| M9 永续数据 + carry 真实成本 | ✅ | 永续 K 线缺口 0；**保证金需求把 6.80% 下调到 3.1%~5.8%** |
| M10 双腿 carry 策略 | ✅ | **项目首个判定「有边际」的策略**（BTC 4.88%、ETH 6.25%，beta≈0）；修掉协议第三条判据的结构性失效 |
| M11 / G3 样本外验证 | ✅ 通过 | 滚动样本外 BTC 7/7 为正、ETH 6/7；**封存段 alpha 降到 1.50% / 1.33%（衰减 3~4 倍）**；BNB 无边际 |

---

## 三、M5 的核心结论

在"只用价格数据 + 核心池 BTC/ETH/BNB + 扣成本"的设定下，**自学习体没有显著 alpha**。

最有信息量的一点：**学习体最终把 95.79% 的权重压在 `long_all`（一直满仓等权）上**，
即退化成了最朴素的那个策略，而且还要付 11% 的成本，所以跑输基准（2.5% 成本）。
学习率越大越快收敛到这个结论（专家熵 0.570 → 0.224 → 0.070）。

反过来，**换手控制这一块设计成功了**：学习体年换手 2.0–23.9 次、成本 4.5–29.5%，
而同样没有预测力的随机策略是 2054 次/年、成本 691.8%。
这验证了"学习信号必须用净收益"这条约束是有效的。

---

## 四、M6 缺口处理（工程债已清）

**实测**（逐标的重新测量，2026-09-14 更正）：

| 标的 | 根数 | 缺口段 | 缺失 | 占比 |
|---|---|---|---|---|
| BTCUSDT | **79,440** | **28** | **128 根** | **0.1611%** |
| ETHUSDT | **79,440** | **28** | **128 根** | **0.1611%** |
| BNBUSDT | 77,503 | 27 | 122 根 | 0.1572% |
| **合计** | — | **83 处** | **378 根** | **0.1599%** |

**最长一处 34 小时间隔（缺 33 根），位置 2018-02-08 01:00 ~ 02-09 09:00。**

> 更正：早期版本写"三标的各 27 处、共缺 122 根（0.1574%）、2018-01-04 起"，
> 那实际是**把 BNB 的数字复制给了三行**，且 **2018-01-04 根本不是缺口**（那天 bar 都在）。
> 详见 PLAN §15.8。对 M6 结论无实质影响。

BTC 与 ETH 的缺口段落连时间点都相同 ⇒ 交易所级别停机，不是单标的数据问题。

**做法**：标记而非删除（删除会破坏时间轴连续性）。新增 `src/data/quality.py`，
`MarketView.is_gap(t)` 暴露给策略，`SimConfig.gap_policy` 支持 `execute`（默认，贴近现实）与
`skip`（保守对照）。

**敏感性结论**：差异最多 3.1 个本金百分点（只影响高频调仓策略；买入持有与动量 0.000pp），
**零判定翻转** ⇒ 默认 `execute` 保持不变。

**顺带修掉一个错误指标**：第一版用"相对终值"算差异，而小时换仓策略终值 0.98（已归零），
算出 4.45% 的"显著差异"——纯除零噪声。已改为以初始本金的百分点计。

**回归验证**：改动后重跑 `m2_baseline.py`，全部数字与改动前逐项一致
（`bh_BTCUSDT` 101,252 / `buy_hold` 1,580,664 / 动量 148,774 / 冲击表逐项相同）。

---

## 五、M7 资金费与延迟抖动（**最重的发现，并修正 M2 结论**）

### 资金费的量级

| 标的 | 条数 | 起点 | 年化均值 | 正费率占比 |
|---|---|---|---|---|
| BTCUSDT | 7,684 | 2019-09-10 | **+11.58%** | 85.8% |
| ETHUSDT | 7,450 | 2019-11-27 | **+13.82%** | 86.3% |
| BNBUSDT | 7,225 | 2020-02-10 | −0.07% | **26.2%** |

永续公共窗口自 2020-02-10 起，57,765 根（6.59 年），覆盖率 100%。

### 成本层级被彻底改写

等权持有三币（6.59 年，持仓 ≈100%，平均权益 88,588）：

- 摩擦成本（手续费+点差+冲击）：**12 USDT**（占平均权益年化 0.002%）
- 资金费（持仓成本）：**32,227 USDT**（占平均权益年化 5.52%）
- **资金费是摩擦成本的 2627 倍**

**⇒ M2 的「换手率一票否决」只对现货成立。** 永续口径下主导成本是**持仓 carry**，
与换手率完全无关：一个从不交易的满仓多头，摩擦成本只 12 USDT，却付 32,227 USDT 资金费。

现货 → 永续的终值影响：等权 −19.2%、动量 −40.8%、随机日换仓 −32.9%、
**单持 BTC −46.0%（Sharpe 从 0.83 掉到 0.02）**。

### 延迟抖动

`latency_bars + Poisson(jitter_mean)`，同种子完全可复现。每档 8 个种子测离散度：

| 策略 | 无抖动 | j=0.5 | j=1.0 | j=2.0 |
|---|---|---|---|---|
| 等权持有三币 | 167,878 | 1.6% | 1.6% | 1.9% |
| 动量（月换仓） | 248,819 | 8.8% | 19.0% | 21.2% |
| 随机（日换仓） | 25,815 | 12.7% | 17.4% | 20.5% |

**⇒ 新增强制协议要求：涉及调仓的结论必须在多个抖动种子下重复。**

### 指向下一阶段的发现

资金费自相关 lag1 = **0.802（BTC）/ 0.787（ETH）/ 0.720（BNB）** ⇒ 高度持续、不是噪声。
且正费率下多头付出、空头收取（单测钉死符号：多头 +10 / 空头 −10，严格对称）。

**⇒ 下一阶段首要候选：资金费 carry。** 收益来源是真实的现金流转移，不是数据挖掘。

---

## 五之二、G3 样本外验证（M11）

### 结论

滚动样本外（1 年样本内 → 半年样本外，各 7 折）：

| 标的 | 样本外年化均值 | 为正折数 | 配置稳定性 |
|---|---|---|---|
| BTC | **+4.64%** | **7/7** | 2 种（nr=0.4、rb=720 一族） |
| ETH | **+5.13%** | **6/7** | 2 种（同族） |
| BNB | −0.85% | 1/7 | 5 种（不稳定） |

样本外与样本内**几乎持平**（BTC 首折 10.10% → 9.70%），没有"选出来就失效"。

封存段一次性开封（2025-01-20 ~ 2026-09-14，1.65 年）：

| 标的 | 封存段年化 | alpha | 自助 95% | 判定 |
|---|---|---|---|---|
| BTC | +1.45% | +1.50% | [+1.16%, +1.85%] | **有边际** |
| ETH | +1.27% | +1.33% | [+0.92%, +1.71%] | **有边际** |
| BNB | −0.08% | −0.03% | [−0.47%, +0.37%] | 无边际 |

### **收益衰减（最重要的事实）**

全样本 4.88% / 6.25% → 滚动样本外 4.64% / 5.13% → **封存段 1.50% / 1.33%**。
最近 1.65 年掉了 **3~4 倍**。滚动样本外没衰减、最近期衰减
⇒ 符合"已知策略被资金压低"的先验。仍为正、区间不跨零，但**幅度已降到 1.3~1.5%**，
是否值得承担运营复杂度是真问题，不能靠"统计显著"回答。

### 工程踩坑

1. **公共时间轴**：早期对现货/永续分别取跨标的交集 → 两条不同索引
   （2017-11 vs 2020-02），却按同一位置区间切 ⇒ 窗口错位 ⇒ 全部强平 ⇒ **折数恒为 0**。
   修复：统一到「所有标的的 (现货 ∩ 永续) 交集」（2020-02-10 起，57,765 根）。
2. **封存守卫按设计拦住了我**：在 `allow=False` 上调 `holdout_slice()` 抛异常。
   A 段用 `allow=False`，B 段另起 `allow=True` 显式开封，写 `runs/_holdout_access.log`。
3. 新增协议函数 `protocol.aggregate_to_clock` / `freq_to_ns`（纯 numpy，不 import pandas），
   配 4 条测试含跨起点对齐。
4. **流程瑕疵（如实记）**：开封实际 **3 次**（每标的一次），严格说打了折扣。

### 待办

统一判定口径（verdict 改为自助区间为主，改了要重跑 G1）；
"开封只允许一次"做成机制约束；补部分成交队列；1.3~1.5% 的商业判断。

---
## 五之三、判定口径统一 + 开封一次性机制（**问题与解决思路**）

详细版在 PLAN §16，这里只留要点。

### 三个问题 → 三个机制

| 问题 | 为什么原来的做法不行 | 落地 |
|---|---|---|
| 判定口径三合一（含 DSR），而 DSR 在峰度 44~105 下不算证据 | G3 三条件恰好同向所以没错，**但那是运气**；下次出现"自助说显著、DSR 说不行"就没有规则可依 | `evaluate_strategy` 分布感知：`KURT_MAX_WELL_BEHAVED=10`，峰度超阈值时 `alpha_p`/`dsr` **仅报告不参与判定**，只留不依赖分布假设的 `alpha_ci`；返回 `criteria`/`binding_criteria`/`waived_criteria`/`failed_criteria`/`verdict_basis` |
| 现金流频率聚合靠人记得 | 忘了就把 t 高估 1.2~1.6 倍 | `evaluate_strategy(..., cashflow_freq="8h")` 自动按时钟桶聚合 + 换算 bars_per_year + 写进 `evaluation_note`；缺索引直接抛错 |
| 开封只靠自觉（G3 实测开了 3 次无人拦） | 铁律 4 当时只是文档里的一句话 | `unseal(purpose, override_reason=None)` + 账本 `runs/_holdout_ledger.json`；不同实例再开同一 seal_id ⇒ 抛 `HoldoutAlreadyUnsealed`；另加**源码级测试**禁止 `scripts/*.py` 直接调裸接口 `holdout_slice()` |

### **意外收获：机制制止了一次不必要的动用**

改完口径后按惯例应"再跑 G3 核对"——那将是第二次开封。但正因为有了账本，
先被迫想清楚"真需要再看一次吗"，于是发现：

**新规则只在退化分支放宽**（非退化分支与旧规则逐字相同），
所以对每个已记录案例可分别按「退化」「未退化」两种分支各算一次判定。
7 个案例**两种分支结论都与原记录一致** ⇒ 不可能翻转 ⇒ **完全不用重读封存段**。

`scripts/verify_verdict_regression.py` 做了这件事，并落了产物。
**0 翻转、0 个"两分支不一致"**（即结论不依赖峰度）。

### G1 重校准：通过，且证明放宽的一支不是漏洞

| 口径 | 窗口 | ② p<0.05 | ③ DSR | ③b 仅自助区间 | ④ 协议判定 | 退化比例 |
|---|---|---|---|---|---|---|
| 毛 | 全窗口 77,503 | 0.0% | 0.0% | **0.0%** | **0.0%** | **100%** |
| 毛 | 仅训练段 58,127 | 0.0% | 0.0% | **0.0%** | **0.0%** | **100%** |
| 净 | 全窗口 | 0.0% | 0.0% | **0.0%** | **0.0%** | 100% |
| 净 | 仅训练段 | 0.0% | 0.0% | **0.0%** | **0.0%** | 100% |

门槛 5% 全过。**关键一行是「退化 100%」**：100 个噪声策略全在退化分支，
判定全程只由自助区间承担，而它 0% 假阳性 —— 回答"豁免两条是不是变松了"：不松。
仅训练段与全窗口一致 ⇒ G1 的 0% 不是靠封存段标定出来的（消除泄漏疑虑）。

### 顺带修掉

1. **G1 窗口与封存段重叠**（轻微泄漏）：G1 用真实行情上的随机权重策略，
   窗口 2023-09-15~2026-09-14 覆盖封存段。加 `--train-only` 开关，两窗口都跑。
2. patch 误删导入（`old_string` 上下文不足，把相邻的 numpy/pandas 导入一起替换了）。
3. f-string 前导空格、嵌套双引号语法错误（改用「」）。
4. 新增的源码级检查第一次跑就报出了残留的直接调用 —— 证明它真会拦人。

### 测试

`tests/test_protocol.py` **32/32**，新增 11 条，其中 3 条是守卫测试：
禁止绕过开封入口 / 退化分布下纯噪声不得通过 / 二次开封必须被拦。

---
## 五之四、前向纸面交易（**运行中**，2026-09-14 起）

详细见 **`LIVE.md`**（判定标准、已知局限、排障表都在那）。

- BTC/ETH 各 10,000 本金，配置 = M10 最优 + G3 判定的「有边际」；每 8h 收资金费、每 720h 再平衡
- 每小时推进（cron `0 * * * *`，no_agent 静默）+ 每天 00:05 UTC 摘要（cron `5 8 * * *`）
- 状态：`runs/live/<SYM>.json`（已入 git，含快照+事件审计轨迹，带裁剪防膨胀）
- 幂等：`funding_time > last_funding_ms`；落盘：先写 .tmp 再 replace

**判定标准（事前写死）**：`t ≈ 2·√T` ⇒ 1 个月无意义、3 个月仅方向提示、**至少 1 年**才够格谈显著。
⇒ 首要产出不是 p 值而是**运行真相**（资金费对不对、状态丢没丢、补保在真实行情里成不成立）。

**启动踩的坑**：① cron 建好但 gateway 没跑 ⇒ "已安排"是假状态，已装 systemd 用户服务
② `time.strftime` 是本地时间却被标成 UTC（差 8 小时，无法对账）
③ 状态文件无界增长 ⇒ 加裁剪（最近 48 条 + 每 UTC 日最后一条）
④ 两处测试期望是我写错的（再平衡费用应精确等于扣费；+24% 打不穿保证金，需约 +49%）

---
## 六、M10 双腿 carry 策略（**首个「有边际」结论，且修掉了协议的一个致命 bug**）

### 6.1 结论

| 标的 | alpha 年化 | beta | t（8h 聚合） | bootstrap 95% | 判定 |
|---|---|---|---|---|---|
| BTCUSDT | **4.88%** | −0.000 | **13.36** | [3.95%, 6.06%] | **有边际** |
| ETHUSDT | **6.25%** | −0.001 | **11.75** | [4.92%, 7.90%] | **有边际** |
| BNBUSDT | 2.80% | −0.000 | 1.12 | [−4.59%, 9.73%] | 无边际 |

**权威 run = `20260914T125453_m10_carry_grid_a0ad13`**。
`20260914T124420_..._4d2315` 是修复判据之前的运行，判全为「无边际」，
两份产物**除 `verdict` 外逐字节相同**（final_equity、funding_income、topup_amount、
t_stat 全部一致）——可用来证明修复只动判据、未动仿真。**别看错那一份。**

run 产物里的 `t_stat = 20.39` 是逐 bar 口径；13.36 是 8h 聚合修正值，
受追踪证据在 `runs/20260914T132343_m10_hac_sensitivity_af9d97/`。
（早期只指 `logs_hac.txt`，但它被 `.gitignore` 排除、不在仓库里 ⇒ 已落成 run 产物。
顺带交叉验证：换成 `protocol.aggregate_to_clock` 后数字逐位一致 13.36/11.75、4.88%/6.25%。）

### 6.1b 判定不能靠 DSR（分布退化）

run 产物 `dsr` 区块暴露 **偏度 7.78 / 峰度 1902.97**。
追查（`scripts/m10_dist_check.py`，受追踪证据在
`runs/20260914T133013_m10_dist_check_47dbe4/`）：

| 标的 | 口径 | 年化波动 | Sharpe | 偏度 | 峰度 |
|---|---|---|---|---|---|
| BTC | 1h | 1.19% | 3.99 | 7.78 | 1902.97 |
| BTC | 8h | 0.54% | **8.81** | 2.85 | 44.25 |
| ETH | 1h | 1.47% | 4.08 | 7.72 | 1025.03 |
| ETH | 8h | 0.80% | **7.44** | 5.38 | 105.12 |

carry 紧对冲 ⇒ 多数 bar 净损益≈0，只有资金费结算/基差跳变时有损益
⇒ 分布退化成「零点尖峰 + 偶尔跳变」⇒ 标准差被压到极小
⇒ **Sharpe 8.81 不可信，DSR=1.0000 不是有效证据**（正态 p 值同样乐观）。

**判定收窄到一条腿**：真正承重的是**截距的块自助区间**（不依赖分布假设）。
结论方向不变，但**不能说"三项判据全过"**。

**协议新增**：现金流频率聚合要扩展到整条链路（Sharpe/DSR/偏度/峰度）；
峰度 > 20 的策略应以**自助区间 + 尾部风险**为主判据。

网格 72 次仿真：存活 68/72，强平仅 4 次且全部在 BNB。
最优配置 `nr=0.4, m=0.5, 再平衡=720`。beta≈0、R²≈0 证实方向中性。

### 6.2 协议的结构性 bug（自 M4 起就存在）

第三条判据 `bootstrap ci_low > 0` 是对 **OLS 残差**做的，而残差均值按构造恒为 0
⇒ **永远不可能通过** ⇒ 任何策略都只能得「无边际」。

症状识别法：bootstrap 区间**不包含点估计**（实测 CI [−0.4%,+0.5%] 而点估计 +4.85%）
——这在数学上不可能，见到就必须查。

修复：对**回归截距本身**做循环块自助（对 (r,b) 成对重采样、每个重采样重跑回归）。
用块级预聚合量加速，O(n_samples × n_blocks)。
守卫测试 `test_evaluate_strategy_can_actually_pass`。
**改了判据必须重跑 G1**：重校准后假阳性率仍 0.0%，未破坏校准。

### 6.3 新协议要求：按现金流频率聚合

资金费 8 小时结算一次，逐 bar 检验把同一笔现金流重复计 8 次 ⇒ t 虚高。
按**绝对时间桶**（不是各自起点的第 k 根）聚合到 8h 后：t 从 20.39→13.36、18.73→11.75，
**虚高约 1.2~1.6 倍**，结论不变。
踩坑：按偏移量聚合成 8h 网格会错开，对齐后不足 10 点、回归退化成 t=0。

### 6.4 再平衡是风控参数

冒烟 [4b]：同样上冲下不再平衡会被强平，每 168 根再平衡则存活
——再平衡按权益缩水自动降杠杆。调参不能只看成本。

---
## 六、M8 资金费 carry 可行性侦察（结构成立）

**符号**：正费率下多头付出、空头收取 ⇒ 空头现金流 `+rate`。窗口 2020-02-10 ~ 2026-09-14（6.60 年）。

| 标的 | 累计 carry | 年化(名义) | 年化@2x资本 | 年化@1.5x | 正年数 | 水下时间 |
|---|---|---|---|---|---|---|
| BTCUSDT | +75.48% | +11.44% | +5.72% | +7.63% | **7/7** | 28.8% |
| ETHUSDT | +89.76% | +13.61% | **+6.80%** | **+9.07%** | **7/7** | 30.2% |
| BNBUSDT | −0.46% | −0.07% | −0.03% | −0.05% | 3/7 | 94.5% |

**含 2022 年熊市仍为正**（BTC +4.16%、ETH +0.79%）⇒ 结构性，不是牛市附属品。

**择时是负收益**：只在费率>0 时持有，切换 964~998 次、成本 212%~220%、净 −120%~−130%。
费率 86% 时间为正，择时省下的远抵不过切换摩擦 ⇒ **连续持有**。

**未计入的风险（必须声明）**：水下时间 28.8%~30.2%；永续空头腿的清算风险；
跨市场划转与对手方风险；**basis P&L 被完全忽略**（只算了资金费现金流），
真正验证需要把两条腿放进撮合器跑（需先采集永续 K 线）。

**必须承认**：carry 是已知的、被广泛交易的策略，不是新发现。价值在于本项目能在扣成本、
可复现、有校准过评估协议的条件下量化它，并说清它还差什么才能可执行。

---

## 七、M9 永续数据与 carry 的真实成本（**下调了 M8 的结论**）

**永续 K 线**：BTC 61,508 根（2019-09-08 起）/ ETH 59,598 / BNB 57,797，**缺口全为 0**。

**基差**：中位 −3.77bp(BTC) / −3.67bp(ETH) / +0.48bp(BNB)，
contango 占比只有 26% / 30% / 51.5% ⇒ **BTC/ETH 大部分时间贴水**。

**二阶 basis P&L**（M8 声明忽略的那一项）：BTC +0.51%/年（占资金费 4.5%）；
**ETH +2.12%/年（占 15.6%，不可忽略）**。但这是首尾单次实现，基差均值回复，**不可外推**。

**挤空风险（永续价滚动最大上冲）**：

| 标的 | 7 日 | 30 日 | 90 日 | 全期 |
|---|---|---|---|---|
| BTCUSDT | 55.1% | **135.5%** | **269.5%** | 3000.9% |
| ETHUSDT | 77.0% | **150.0%** | **321.6%** | 5052.9% |
| BNBUSDT | 192.7% | 796.1% | 1539.1% | 17619.8% |

**保证金需求把收益压下来**（资本回报 = 名义额回报 / (1+m)）：

| 标的 | 撑过 30 日需 m | 年化 | 撑过 90 日需 m | 年化 |
|---|---|---|---|---|
| BTCUSDT | 1.36 | **4.86%** | 2.70 | **3.10%** |
| ETHUSDT | 1.50 | **5.44%** | 3.22 | **3.23%** |
| BNBUSDT | 7.96 | −0.01% | 15.39 | −0.00% |

**⇒ 现实是 3.1%~5.8%，不是 M8 报的 6.80%。**

**carry 的第一号风险不是方向，是永续腿保证金被单独爆掉**：现货腿盈利不能自动
补到永续腿（普通账户），价格最终回归也救不了先被强平的腿。这正是 2021-05、
2022-11 基差交易集体爆仓的机制。⇒ carry 的正确实现必须显式建模保证金账户、
补保规则与强平阈值。**M10 的核心就是这个。**

---

## 八、踩过的坑（务必先看，全部是"结论级"错误）

1. **收益基数错误**：成本在第一根 bar 扣除，用 `equity[0]` 做收益基数会让净值基数变小，
   净收益百分比反而**高于**毛收益（BNB 毛 45,586% / 净 55,849%）。修复：一律以固定初始本金为基数。
2. **换手统计被截断**：净账本归零后不再交易，换手停止累加（小时换仓策略一度显示 16 次/年，
   真实值 100,266）。修复：换手按**毛账本**统计，成本按净账本统计，并记录归零时刻。
3. **"持有"语义缺失**：agent 返回空 dict 被执行成"清仓"，买入持有曲线是平的。
   契约改为「返回完整目标权重；返回 `None` 表示维持现状」。
4. **向量化快路径不可信**：早期 G1 用近似权重法提速，交叉验证发现与真实撮合器偏差达 134pp。
   已整体废弃——评估器的校准不能建立在近似模型上。
5. **下标错位（最隐蔽）**：策略收益从 warmup 后开始、基准从第 1 根开始，按 `min(len)` 截断对齐，
   导致 `beta` 算成 **0.003**。修复：按时间戳对齐。
6. **基准口径错误**：基准用对数收益均值、策略用简单收益，差半个方差项，
   在 7.7 万根上累积成几千个百分点的虚假 alpha——连"基准对自己"都算出 `beta=1.07`、`alpha=+71%/年`。
   修复：基准改用**同一撮合器、零成本**的等权买入持有组合。
7. **符号写反（carry 侦察）**：把"空头收到资金费"写成 `-rate`，整个结论方向颠倒
   （会得出"carry 是负收益"的相反结论）。修复：显式写出符号约定。
8. **预计算的等价性边界**：滚动统计预计算只在 `t >= n-1` 时与逐根现算等价；
   早期版本没兜住 `t < n-1`，对拍时被抓出 σ 差 6.7e-3。已加回退。

**新增的永久性防线**：`tests/test_protocol.py` 里的
**基准自比基准必须 beta=1、alpha=0、R²=1**。这条测试若早先存在，坑 5 当场就会被抓。

---

## 九、怎么跑

```bash
cd /home/nick/workspace/quant

# 测试
python3 tests/test_causality.py        # 10/10  因果封印与撮合正确性
python3 tests/test_protocol.py         # 13/13  评估协议自洽性
python3 tests/test_data_quality.py     # 11/11  缺口识别与缺口口径
python3 tests/test_funding_latency.py  # 20/20  资金费符号与延迟抖动

# 数据（可重复执行，只补增量）
python3 scripts/m1_ingest.py

# 成本与基线
python3 scripts/m2_baseline.py
python3 checks_window_sensitivity.py   # 冲击占比的口径敏感性

# 优化正确性对拍（改 MarketView 后必跑）
python3 scripts/verify_precompute.py

# 数据质量诊断
python3 scripts/diag_gaps.py           # 缺口位置与分布
python3 scripts/m6_gap_sensitivity.py  # 缺口口径敏感性（execute vs skip）

# 资金费与延迟
python3 scripts/m7_ingest_funding.py   # 采集资金费历史
python3 scripts/m7_funding_latency.py  # 资金费量级 + 抖动离散度（约 9 分钟）
python3 scripts/m8_carry_feasibility.py # carry 可行性侦察（快，不跑撮合器）
python3 scripts/m9_ingest_perp.py      # 永续 K 线 + 基差 + 挤空/保证金分析

# 评估器校准（G1 闸门，约 6 分钟）
python3 scripts/m4_calibrate.py

# 在线自学习体（M5）
python3 scripts/m5_smoke.py            # 冒烟，约 3 秒
python3 scripts/m5_online.py           # 完整评测
```

---

## 十、关键文件位置

```
quant/
├── PLAN.md                       # 设计与里程碑 + 全部实验结果
├── WORK_LOG.md                   # 本文件
├── configs/base.json             # 冻结核心池 + 成本 + 风险 + 评估参数
├── configs/spreads.json          # 实测点差（m1_ingest 生成）
├── src/data/sources.py           # 三源只读适配器 + 合规白名单
├── src/data/store.py             # CSV 落盘 + sha256 manifest
├── src/data/quality.py           # 缺口识别/标记/时间权重
├── src/env/market_view.py        # 因果封印 + 滚动统计预计算 + is_gap
├── src/sim/costs.py              # 手续费 / 点差 / 平方根冲击
├── src/sim/funding.py            # 资金费表与结算成本
├── src/sim/exchange.py           # 双账本撮合仿真
├── src/eval/metrics.py           # 指标（固定本金基数）
├── src/eval/protocol.py          # 时间戳对齐 / PSR / DSR / PBO / alpha 检验
├── src/eval/benchmark.py         # 基准构造（必须与策略同机器同口径）
├── src/eval/holdout.py           # 样本封存守卫（代码级拒绝）
├── src/agents/baselines.py       # L0 基线
├── src/agents/online.py          # M5 在线学习体（11 专家 + Hedge）
├── tests/test_causality.py       # 10/10
├── tests/test_protocol.py        # 13/13
├── tests/test_data_quality.py    # 11/11
├── tests/test_funding_latency.py # 20/20
└── runs/                         # 实验登记（含 G1/M5/缺口敏感性结果）
```

---

## 十一、下一步

**按淘汰门 G2：无显著边际 → 禁止在现有信号上继续调参**（那是过拟合螺旋的入口），
转信号源研究。候选：
- **资金费率与基差**（永续 vs 现货，可观测、可建模，优先级最高）
- **订单簿失衡**（币安公开深度接口可得，与已实现的冲击模型共用数据）
- 链上数据（需第三方源）

其余待办：
1. 1h 数据 27–28 处缺口：回测跨过时 `next-bar` 间隔不是 1 小时，需显式处理或剔除
2. 未实现的成本项：资金费、部分成交队列模型、网络延迟随机扰动
3. 只支持现货多头；做空需接永续并计资金费
4. **协议已知局限**：单持 BNB 在当前窗口 alpha 年化 +13.07%、t=2.30 会被判成接近显著，
   但那是集中度不是技能。协议目前把"押中赢家"也算作 alpha，需在报告里声明。

---

## 十二、做空能力扩展（**训练目标改造路线图，A 阶段已完成**）

### 12.1 背景与目标

M5 智能体（`HedgeEnsemble` + 11 个专家）在"只用价格 + 扣成本"设定下被判无边际。
G2 淘汰门之后，重新审视训练目标本身：**当前 `mixed = clip(mixed, 0, None)` 把所有负权重
清零，训练目标只能在 {0, 多} 之间选**，从架构上排除了做空信号。

本路线图目标：让训练目标**能使用杠杆做多做空**，且仿真结果在实盘可复现。
分四阶段（**A、A'、B 已完成**，C、D 规划）：

| 阶段 | 内容 | 风险 | 状态 |
|---|---|---|---|
| **A** 放开做空 | clip(0) → 对称 clip；加做空专家；配置 allow_short | 低 | ✅ 完成 |
| **A'** funding carry 信号 | 把 funding 接进 Hedge payoffs，让 ShortExpert 看到 carry 收益 | 低 | ✅ 完成（但**真实数据几乎无变化**）|
| **B** 加杠杆 | max_gross 1.0 → 3.0；agent/sim 配齐 | 中 | ✅ 完成（**但 Hedge 长期 gross 仍 = 1.0**）|
| **C** 逐腿保证金 + 强平 | 把 `carry.py` 的 margin_cash/reserve 移植到主 sim | **高** | 规划 |
| **D** 参数重标定 | `max_turnover`、`band`、`η` 配合新范围 | 中 | 规划 |

### 12.2 A 阶段变更摘要（**已完成**）

**3 个 commit**：
- `ee87e35` 训练目标: 增加做空专家集合
- `300a652` 训练目标: 放开做空裁剪并配套测试
- `8801252` 做空训练: 补兜底与归一化回归测试

**代码改动**：

| 文件 | 改动 |
|---|---|
| `src/agents/online.py` | 新增 `ShortExpert`（恒为 -1/n）和 `ShortMomentumExpert(24/168)`；`default_experts` 从 11 → 14；`decide()` 的 clip 改为 `[-max_exposure, +max_exposure]`，gross 兜底改用 `Σ\|w\|` |
| `configs/base.json` | `risk` 段加 `allow_short: true` |
| `tests/test_agent_short_capability.py`（新） | 8 条断言：专家存在性、专家提案、端到端 Hedge 学到做空、`_sanitize` 兜底、归一化不变量 |

**关键不变量（被新测试锁死，B 阶段改 max_exposure 时回归会立刻暴露）**：
- 单标的 `|w_s| ≤ max_exposure`
- 总仓位 `Σ\|w_s\| ≤ max_exposure`（等比缩放后）
- 多空等权混合 → `[0, 0]`（保留方向、不翻号）
- `allow_short=False` 时 sim 端仍截断负权重（兜底被覆盖）

**A 阶段的实际能力**（108/108 测试通过）：
- `mixed` 权重范围 `[-1, +1]`（之前 `[0, +1]`）
- 上涨场景 Hedge 学到 `p_long_all` 主导（min_w ≈ +0.11，验证现有 long-only 路径未坏）
- 下跌场景 Hedge 学到 `p_short_all` 主导（终态 p > 2/K，且 min_w < -1e-9）
- **总仓位仍 = 1.0**：做空 0.5 时只能做多 0.5，**离真杠杆还远**

### 12.3 A 阶段没做的事（**这是进入 B 之前必须知道的边界**）

1. **没改 `max_gross_exposure`** —— 还是 1.0。A 只是"放开做空"，没"放大仓位"
2. **没动 sim 层 `_sanitize`** —— 兜底代码 `max(w, 0)` 没改也没删，靠 `allow_short=true` 自然失效
3. **没做保证金/强平** —— sim 里仍然只在 `net_eq < 0.01*initial` 时判定破产，不模拟逐腿爆仓
4. **没改 `risk.max_turnover_roundtrips_per_year: 100`** —— A 不影响换手
5. **没在 LIVE paper 上跑过 A** —— 前向纸面交易还跑着旧 agent，B 阶段才换

### 12.4 B 阶段待办（**启动前要拍板的 3 个分叉**）

1. `max_gross_exposure`：1.0 → **3.0**（你已确认）
2. agent 层 `max_exposure` 与 sim 层 `max_gross` 是否同时改 —— 必须**配齐**，否则任一边是安全网
3. C 阶段（保证金/强平）是否合并到 B 一起做 —— 建议**分开**，风险等级不同

### 12.5 已知坑（务必看）

- **A 没改 base.json 的 `max_gross_exposure`**，所以默认仍是 1.0。要让 A 的"放开做空"真的发挥作用，必须接着做 B 改 3.0
- 旧测试 `test_hedge_ensemble_can_emit_negative_mixed_weights` 用的是**下跌** 600 根合成数据。若改用真实历史数据回归，需重新调阈值（`p > 2/K` 是合成的，真实场景下 Hedge 可能更保守）
- `runs/live/*.json` 没加 `.gitignore`（cron 会自动追加事件，下次 commit 前手动 `git restore`）

### 12.6 A' 阶段：funding carry 信号（**已完成但效果有限**）

**触发**：A 阶段真实数据 baseline 显示 Hedge **完全没学到做空**——下跌段（-41%/-19%）也 0% 做空，只在上涨段 11.1% / 8.6% 短暂做空（短期反转套利）。

**修复**：把 funding carry 接进 `_update_experts` 的 payoffs，让 short 持仓的专家能从 8h 资金费里收到 carry（BTC/ETH 年化 +5~8%）。

**实现**：
- `HedgeEnsemble(funding=...)` 新增可选 `FundingTable` 参数
- `decide(view)` 查上一根 bar 时刻的 funding rate，写进 fr_arr
- payoffs[k] += `target @ fr_arr`（短 carry 对 short 持仓是 +payoff）

**测试新增 2 条**（`test_short_expert_activated_by_positive_funding_carry` / `test_funding_none_keeps_pure_price_signal`），全 110 条通过。

**真实数据发现**（A' vs A 几乎无变化）：

| 指标 | A（无 funding）| A'（有 funding）|
|---|---|---|
| 做空 bar 占比 | 3.9% | 4.0% |
| 终态 p_short_all | 0 | 0 |
| 段 3/5 下跌做空 | 0% | 0% |

**根本原因**：1h 价格波动（σ ≈ 0.4%）>> 8h funding 收益（1bp），Hedge 在 1h 时间尺度上把 funding 当噪声过滤。**这是 D 阶段（参数重标定）的目标**。

### 12.7 B 阶段：3x 杠杆（**先 no-op，后修成真生效**）

**B 阶段首次跑**（2026-09-15 上半段，no-op 状态）：

| 指标 | A'（1x）| B 首次（3x）|
|---|---|---|
| 净终值 | 49,778 | **49,778** |
| max(Σ\|w\|) | 1.00 | **1.00** |
| 终态 p_long_all | 0.9945 | 0.9945 |

**核心发现**：
- **B 首次是 no-op**——`max(Σ\|w\|)=1.0` 而非 3.0，杠杆上限完全没用满
- 根因：`LongExpert` / `InverseVolExpert` 等专家的"满仓值"= `1/n`，**写死**在专家代码里
- 要让 B 阶段真生效，必须改专家"满仓值"或让 HedgeEnsemble 把 `max_exposure` 注入专家 proposal

### 12.7.1 B' 阶段：专家满仓值与 max_exposure 解耦（**已完成**）

**改动**：
- `Expert.weights(view, symbols, gross_target=1.0)` 加 `gross_target` 参数（向后兼容）
- `LongExpert` / `MomentumExpert` / `ReversalExpert` 满仓 = `gross_target / n`
- `ShortExpert` / `ShortMomentumExpert` 满仓 = `-gross_target / n`
- `SingleAssetExpert` 满仓 = `gross_target`
- `InverseVolExpert` 满仓 = `gross_target`（保留比例）
- `HedgeEnsemble.decide()` 把 `self.max_exposure` 传给所有 expert
- 测试 5 条新加，119/119 通过

**B' 真实数据发现（杠杆真生效，且 sim 失真暴露）**：

| 指标 | A'（1x）| B（首次 3x）| **B'（3x 解耦后）**|
|---|---|---|---|
| 净终值 | 49,778 | 49,778 | **-18（破产）**|
| 毛终值 | 52,249 | 52,249 | **-17** |
| 成交笔数 | 1,884 | 1,884 | **7,647**（4x）|
| 换手 / 本金 | 67.5x | 67.5x | **306.6x** |
| 累计成本 | 727 | 727 | **9,539**（13x）|
| **max(Σ\|w\|)** | 1.00 | 1.00 | **3.00** ✅ |
| **max(w)** | 1.00 | 1.00 | **3.00** ✅ |
| 段 1（+42% 涨）做空 | 11.1% | 11.1% | 8.8% |
| 段 1 max gross | 1.00 | 1.00 | **3.00** |
| 破产 | 否 | 否 | **是，bar 1,420**（2018-01 暴跌段）|
| 终态 p_long_all | 0.9945 | 0.9945 | 0.8754 |
| 终态 p_inv_vol | 0.0055 | 0.0055 | 0.1246 |

**B' 阶段的两大发现**：
1. **杠杆真生效了**——`max(Σ\|w\|)=3.0`、单标 `max(w)=3.0`，满仓 +3x 满仓 1.5x/标的
2. **sim 失真暴露**——bar 1,420 触发了 `insolvent_at`（2018-01 暴跌段），但 sim 没真爆仓，agent 继续 3x 仓位交易，**net_eq 持续走到 -18**。这正是 PLAN §19.6 警告的"C 阶段没做 → sim 失真"。

**这正是 C 阶段必须做的原因**：当前 B' 的 alpha **完全是 sim 估值**。实盘上 bar 1,420 的 -40% 暴跌会让 3x 多头爆仓，账户归零，不会再继续交易。

### 12.8 C 阶段（**已完成**）：逐腿保证金 + 强平

C 阶段分 4 个子阶段（C1 / C2 / C3 / C4），全部完成：

**C1 阶段**：
- `src/sim/margin.py`（新文件）：MarginConfig + MarginBook + MarginLeg
- 接口：open_leg / close_leg / topup_to / liquidate / is_liquidatable / needs_topup
- 17 条单元测试，102/102 通过

**C2 阶段**：
- `SimConfig` 加 `margin: MarginConfig | None = None`（默认 None，向后兼容）
- `SimConfig` 加 `max_exposure_per_symbol: float = 3.0`（per-symbol 杠杆上限）
- `_sanitize` 加 per-symbol 限幅（|w_s| ≤ max_exposure_per_symbol）
- `SimExchange` 主循环建仓/平仓时同步调 MarginBook
- 关键决策：**C2 阶段 open_leg 只记账不操作 cash**（因为 SimExchange 已全额扣 cash 买币）
- 2 个集成测试：建仓/平仓时 margin_cash 同步、margin=None 时行为不变
- 136/136 通过

**C3 阶段**：
- `SimResult` 加 `liquidated_legs: list[symbol]` 字段
- `SimExchange` 主循环 step 1.5：每根 bar 用 high 价做压力测试，触发强平
- 强平语义：`release = max(0, margin_cash - penalty)`，退还 margin_cash 本金到 cash_pool
- **关键设计决定**：强平后**允许**重新开仓（discard liquidated 标记），模拟"爆仓后账户归零重建"
- 2 个集成测试：做空 + 价涨触发强平、价格平稳时无强平
- 140/140 通过

**C4 阶段**：
- 新增 `scripts/c_margin_baseline.py`
- 真实数据：3x 杠杆 + initial=0.5 / maint=0.1 强平线

**C 阶段真实数据发现**：

| 指标 | B'（3x 不强平）| **C（3x + 逐腿强平）**|
|---|---|---|
| 净终值 | -18（破产）| **15**（强平后回升）|
| 毛终值 | -17 | -16 |
| 累计成本 | 9,539 | **14,679**（1.5x）|
| 成交笔数 | 7,647 | 7,831 |
| **liquidated_legs** | （空）| **6 个（BNB ×5 + ETH ×1）**|
| 破产 | bar 1,420 | bar 1,420 |
| max(Σ\|w\|) | 3.00 | 3.00 |

**C 阶段核心发现**：
- **逐腿强平真的触发了**（6 次，主要是 BNB）—— sim 失真被强平阻止
- 但 **BTC 1 次都没强平**（Hedge 长期 99% 压在 long_all，但 BTC 暴跌段被 long_all 救下）
- 净终值 15 vs B' -18：**强平阻止了 BNB 持续亏损**（回升 33）
- 但累计成本从 9,539 涨到 14,679（多 5,140）—— 强平后重建仓 + 多次补保的成本

**C 阶段没解决的事**：
- **B' 暴露的"BTC 跌 33% 净资产归零"**：sim 现在 bar 1,420 触发了**破产**（整体 net_eq<1%），但单标的 BTC 1 次都没强平（因为 BTC 实际只跌到 77000，距 -33% 很远）
- **真正问题**：Hedge 长期 99% long_all，**根本不会分散**到做空，杠杆放大了 99% 多头
- **D 阶段（参数重标定）** 才是解决这个问题的关键：让 funding 信号可见、让短动量专家真正学到做空

### 12.10 D 阶段（**已完成**）：参数重标定与 cost 模型 bug 发现

D 阶段跑 18 组（η × band × funding，3×3×2）真实数据搜索，1x 杠杆。

**D 阶段两个核心发现**：

**发现 1：3x 杠杆下参数救不了 sim 失真**
- D1 阶段（3x 杠杆，9 组 + 3 组）12 组全部破产（终值 ≤ 104）
- funding 接入 vs 不接入差别微小（mean sharpe 1.087 vs 1.083）
- η=0.20 + 小 band 触发了 p_short_all=1.0（Hedge 100% 做空），但 max_dd 溢出 1.1e27%
- **结论**：3x 杠杆 sim 失真下参数搜索没用，必须先修 sim

**发现 2：1x 杠杆下 cost 模型对早期 bar 计算异常**
- D2 阶段（1x 杠杆，18 组）跑出 sharpe 0.7-1.9 的"好"结果
- 但 cost_paid.sum() 9 亿 vs final_net 5.5 亿（cost > net_equity 的 1.6x）
- 5000 根跑：成本 595 亿 / 换手 79 万 = **7,545,571 bp**（正常应 ~30 bp）
- **根因**：bar 5000 时 BTCUSDT period_notional=14.7M USDT，bar 10000 时=4.6M（早期市场薄）
  → notional/pn 大 → impact 公式给出"冲击极大"
- 这是**CostModel 在早期市场薄、notional 大的情况下虚高冲击成本**的 bug
- **D 阶段真正贡献**：不是找到好超参，而是**通过参数搜索暴露 sim bug**

**D 阶段 18 组 top-10 by sharpe**（1x 杠杆）：

| η | band | funding | sharpe | total_ret | p_short | p_long |
|---|---|---|---|---|---|---|
| 0.05 | 0.03 | Y | 1.924 | 5,544,682% | 0.000 | 0.995 |
| 0.05 | 0.03 | N | 1.879 | 4,201,493% | 0.000 | 0.989 |
| 0.20 | 0.10 | Y | 1.480 | 739,301% | 1.000 | 0.000 |
| 0.20 | 0.20 | N | 1.454 | 745,886% | 1.000 | 0.000 |
| 0.20 | 0.10 | N | 1.189 | 32,025% | 1.000 | 0.000 |
| 0.20 | 0.20 | Y | 1.160 | 35,813% | 1.000 | 0.000 |

**注意**：这些 "total_ret" 是 sim 计算的，**含 cost 模型 bug**，不可信。

**D 阶段没完成的事**：
- **修 CostModel**：早期 bar 的 impact 需要按 bar 时刻的实际市场规模校准（不能固定用早期 pn 当基准）
- 修完 cost 后**重跑 D 阶段**（应能看到正常的 1x buy_hold 终值 ~1.5M，而非 5.5 亿）
- 修完 cost 后**接 3x 杠杆 + 完整评估协议**（DSR / bootstrap CI）—— 这是真正判断"有边际"的关键

### 12.9 进入 C 之前必须解决的 B 阶段遗留问题

1. ~~**专家"满仓值"与 max_exposure 解耦**~~（**B' 阶段已完成**，见 §12.7.1）
2. **C 阶段（逐腿保证金/强平）必须先于"实盘用"**：B' 真实数据已证明 sim 失真（bar 1,420 触发破产后 agent 继续 3x 仓位交易到 -18，**实盘上不会发生**——3x 多头在 -40% 暴跌段必爆仓）
3. **D 阶段（参数重标定）必须做**：A' 已证明 funding 信号在 1h 时间尺度上被噪声过滤，需要 D 阶段重新选 `η` / `band` 才能让 funding 可见

---

## 十三、合规

- 只读公开行情，**不接账号、不存 key、不碰下单接口**（代码级白名单强制，
  非白名单请求抛 `ComplianceError`）
- 不涉及虚拟货币兑换/交易/承销，不触及 2026-02-06 八部委 42 号文禁止的境内业务活动
