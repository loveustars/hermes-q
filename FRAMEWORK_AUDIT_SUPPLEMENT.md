# FRAMEWORK_AUDIT_SUPPLEMENT.md —— 补充说明 + 仓库导览

> **关系声明**：本文件是 `FRAMEWORK_AUDIT.md`（2026-09-15 独立审计，465 行）的**补充**，
> 不是替代，也不是覆盖。所有"该审计已覆盖"的结论，在本文只**索引**，不重复论证。
> 新增内容是审计**没单独拎出来**的两块：**性能瓶颈**（cProfile 实测）、
> **实时可行性**（在线交易的额外风险）。
> 末尾附了一份**仓库导览**，给第一次打开这个项目的人用。

---

## 0. 阅读路径

| 你想看什么 | 去哪 |
|---|---|
| 学习体的终值/回撤/池子无效等**效果缺陷** | `FRAMEWORK_AUDIT.md` §1（问题一二三）+ §2（既有结论的反向解读） |
| 强平通道的解析式、数值例、路径定位 | `FRAMEWORK_AUDIT.md` 附录 A |
| 评估装置的功效/峰度豁免/无种子 | `FRAMEWORK_AUDIT.md` §1（问题三）+ 附录 B |
| 训练主循环**慢在哪、为什么慢** | **本文 §1** |
| 把这套产物**接去实盘**会怎样 | **本文 §2** |
| 上线前**必须**先做什么 | **本文 §3** |
| 这个目录里每样东西**是什么、怎么用** | **本文 §4** |
| 我没把握的部分 | **本文 §5** |

一句话总判断（与审计同方向，但加了一层实时结论）：

> **这套框架训练出的产物即使修掉记账 bug、扩到双腿 carry，实时交易也**不会**"稳定增长"。
> 它目前产出的最优组（η=0.20 / band=0.20）= 0.57× 市场暴露 + 年化 6% 成本 + −75.5% 回撤
> + 137 次强平；把"输给被动"这件事拆开看，三块都是路径依赖的负贡献、没有一个是 alpha。**
> **结构性的缺口**是：池子里没有正期望择时规则（事后最优 = 买入持有），
> 评估协议对真 +40%/年 alpha 的功效只有 3%，引擎无法表达 delta 中性的双腿 carry。
> **修这三件事不会让当前的最优组变赚钱，只会让我们终于有能力判断"赚钱还是亏钱"。**

---

## 1. 性能瓶颈（审计未单独覆盖，本文新做）

### 1.1 实测数字

机器：i9-13900H / Python 3.10 / numpy 2.2.6 / pandas 2.3.3。
全窗口：77,503 根小时线 × 3 标的（BTC / ETH / BNB，交集），2017-11-06 ~ 2026-09-14。
**一次端到端学习体全窗口仿真 = 72.5 秒**（与 `scripts/d_param_search.py` 顶部注释一致，
与 `FRAMEWORK_AUDIT.md` 附录 B 的 72 秒一致，三处数据互相印证）。

cProfile（按累计时间排序，前 10）：

| 排名 | 函数 | cumtime | 占比 | 调用次数 | 说明 |
|---|---|---|---|---|---|
| 1 | `SimExchange.run` | 139.6 s | 100% | 1 | Python 单线程逐 bar 循环，77k 次 |
| 2 | `HedgeEnsemble.decide` | 125.4 s | 89.8% | 77,202 | **每根 bar 调一次** |
| 3 | `[ex.weights(view, ...) for ex in self.experts]` | 105.1 s | 75.3% | 77,202 | listcomp，**每根 bar 重建全部 18 个 expert 的权重矩阵** |
| 4 | `MarketView.returns` | 93.0 s | 66.6% | 2,776,752 | 反复切片 pandas 取窗口 |
| 5 | `MarketView.window` | 81.1 s | 58.1% | 1,619,982 | 同上 |
| 6 | `pandas._getitem_axis` | 63.3 s | 45.3% | 2,513,428 | pandas 切片代价 |
| 7 | `MomentumExpert.weights`（`online.py:78`） | 58.3 s | 41.8% | 308,808 | 7 个动量 expert × 77k bar |
| 8 | `ShortMomentumExpert.weights`（`online.py:120`） | 42.8 s | 30.7% | 463,212 | 同样模式 |
| 9 | `_update_experts` | 12.9 s | 9.2% | 77,202 | Hedge 乘法更新，**不热** |
| 10 | `isinstance` | 8.4 s | 6.0% | 46,759,495 | pandas 内部类型检查，伴生开销 |

### 1.2 根因一句话

> **每根 bar 调 18 次 `expert.weights()`，每次都重新切片 pandas 取窗口。**

具体地：

- `MomentumExpert.weights` 和 `ShortMomentumExpert.weights`（共 13 个 expert）
  拿的是 `view.returns(s, n)` 的 close 切片。
- 对同一根 bar，`close[t-lookback:t]` 这种切片完全可以**预计算一次**
  （甚至把 18 个 expert 的最大 lookback 一次切出去，expert 在 numpy 上做索引）。
- 加上 `online.py:255` 的 listcomp 没有预分配的 `(K, S)` 输出缓冲，每次重新构造 ndarray。

### 1.3 "看起来重、实际不重" 的反直觉点

| 函数 | 看似重 | 实际 | 为什么 |
|---|---|---|---|
| `_update_experts`（`online.py:195`） | 名字像训练核心 | 12.9 s / 9.2% | 只是 K=18 的向量运算，K 小 |
| 评估协议 bootstrap / DSR / PBO | 数学吓人 | `bootstrap_alpha(2000, T=77k)` = 0.10 s | 在 numpy 上都是毫秒级 |
| `KURT_MAX_WELL_BEHAVED=10` 峰度豁免的"重计算" | 听起来要算四阶矩 | 0.00 s | 一次性，无迭代 |
| `_sanitize`（`exchange.py:169`） | 每根 bar 都过 | < 1% | 纯 Python 字典操作 |

**结论：评估侧永远不是瓶颈。训练/仿真是。**

### 1.4 多组 / 多种子的代价估算

`d_param_search.py` 是 **3×3×2 = 18 组纯串行 `for`**，**没有 `multiprocess`/`joblib`/`Pool`**（全仓库 grep 零引用），
所以 18 组 = 18 × 72 s ≈ **21.5 分钟**。

如果按项目自己的"六条铁律"中的第 6 条（≥10 种子 + 报分布）补上种子分布：

| 实验 | 单次耗时 | 组数 | 串行总计 | 并行上限（16 线程） |
|---|---|---|---|---|
| 当前 D 阶段（已完成） | 72 s | 18 | 21.5 min | ~1.5 min |
| D 阶段 + 10 种子 | 72 s | 180 | **3.6 h** | ~15 min |
| 3x 杠杆 + 10 种子 | 72 s | 180 | 3.6 h | ~15 min |
| 5-fold walk-forward + 10 种子 | 72 s | 900 | **18 h** | ~75 min |

**重要现状**：`exchange.py:48` 的 `latency_jitter_seed` **只**用于延迟抖动（jitter），
**不扰动策略**——意味着即使你打开 `latency_jitter_mean > 0`，**也没有"种子"机制
能让你做策略随机性扫描**。要做这件事，必须新加一个"策略种子接口"（目前是 0 基建）。

### 1.5 优化 ROI 排序（如果未来要动这块）

| 优先级 | 动作 | 估计收益 | 风险 |
|---|---|---|---|
| ★★★ | `d_param_search` 改 `joblib.Parallel(n_jobs=-1)` | 21.5 min → ~1.5 min（10×） | 低；不改变语义 |
| ★★★ | 把 `expert.weights` 的 pandas 切片换成 numpy 滚动视图 | 单次 72 s → ~30 s（2.4×） | 中；要重写 expert 接口 |
| ★★ | 加策略种子接口（HedgeEnsemble + RandomWeights 各加一个 `seed`） | 18×10 种子 = 3.6 h → 同上但可分批 | 低 |
| ★★ | 给 evaluation 加进度条（tqdm） | 不提速，只是不再瞎等 | 0 |
| ★ | `_update_experts` 向量化 K=18 | 12.9 s → 2 s | 极低；K 太小意义有限 |

---

## 2. 实时可行性（审计未涉及，本文新做）

### 2.1 学习体最优产物的"真实状态"

D 阶段（2026-09-15，18 组搜索）最优组：`η=0.20 / band=0.20 / funding=ON`。

| 指标 | 值 | 解读 |
|---|---|---|
| 终值（8.8 年 / 10k 起） | 200,595（≈ 20×） | **这个数里 BNB 贡献了 425 倍的一次性事件** |
| Sharpe | 0.838 | 看起来"还行" |
| **最大回撤** | **−75.5%** | 实盘里单次回撤过 50% 就要回答"产品还能不能继续"的问题 |
| 交易笔数 / 总成本 | 5,327 / 91,365 | **成本 = 初始本金 9.1 倍**；年化成本 ≈ 平均权益的 6% |
| β / R²（vs 等权基准） | 0.568 / 0.390 | 学习体≈市场暴露的 57% |
| `mean_gross` | 0.650 | **目标 1.0、实际只用 0.65 ⇒ 35% 的暴露被浪费** |
| 终态 `p_short_all` | 1.000 | 收敛到一个单点 on/off 规则，**没有梯度** |

**这三条合起来的意思**：8.8 年 20 倍 = 0.57× 市场暴露 × 极低成本路径（一次性 BNB）× 极低回撤成本路径。
**不是 alpha**。

### 2.2 学习体的"亏损三件套"（审计 §2 反向解读）

学习体净终值 ≈ 被动等权买入持有（1,525,547）× 0.65（敞口稀释）× e^(−成本/平均权益) × 强平通道（≈ −38%）
≈ 1.5M × 0.65 × 0.55 × 0.62 ≈ **200k**（实测吻合）。

| 成分 | 量级 | 方向 | 路径依赖？ |
|---|---|---|---|
| 敞口稀释（mean_gross=0.65） | −35% 暴露 | 负 | 是（取决于市场涨幅） |
| 交易成本（91,365 / 平均权益 ≈ 年化 6%） | 持续侵蚀 | 负 | 是（取决于换手） |
| 强平通道（被强平 137~216 次 vs 被动 2~5 次） | 选择性惩罚 | 负 | 是（取决于波动路径） |

**核心问题：三块都是路径依赖的**。这意味着"未来"曲线长什么样，**不取决于策略有多聪明，
取决于下一段路和过去那段路有多像**。BNB 425 倍是一次性事件，没有人会用它外推下一段。

### 2.3 评估协议对"实时"的判据能力

| 检验 | 结果 | 实时含义 |
|---|---|---|
| 真 +40%/年 alpha，n_trials=18，判"有边际"概率 | **3%** | 即使注入 +40%/年真信号，**97% 概率被判为"无边际"** |
| 唯一生效判据 `alpha_ci` | 无多重试验校正 | 学习体判无边际、`long_all`（池内最优）也判无边际 ⇒ **协议没有区分力** |
| DSR / alpha_p 峰度豁免阈值 | `KURT_MAX_WELL_BEHAVED=10` | 学习体峰度 33.5 ⇒ DSR 被豁免 ⇒ 唯一判据失败 |
| 18 组网格对 band 单调 | η=0.20 / 0.05 组对 band=0.03/0.10/0.20 单调递增 | **网格在选"少交易"，不是在选信号** |

**结论**：实时里你**没有任何工具**能判断"这个策略是好的还是又一次过拟合"。
当前协议对好策略和坏策略给同样的判定。

### 2.4 实时会引入 4 个回测没建模的东西

| 风险 | 回测怎么处理 | 实盘会怎样 |
|---|---|---|
| **执行延迟** | `latency_bars` 1~N bars 离散化 | 实盘会有分笔撮合、撤单重挂、滑点非平稳；框架**没有建模"挂单被吃不到"** |
| **资金费逐根结算路径** | 按 close × rate 直接扣 | 实盘 funding 是 8h 结算、跨时区；**同一根 bar 内 open/high/low 触发的资金费方向不同**，框架没区分 |
| **破产 vs 追保** | `bankrupt_floor=0.01 × initial_cash` 触发即出局 | 实盘你会**追加保证金**而不是出局；`margin.topup_to()` 与"破产出局"分离，**没有机制保证实盘你选择追保而不是被自动强平** |
| **强平通道语义** | 1x 凭空 −44.4% 强平 | 实盘 Binance BTC 永续 1x 全仓强平距离是 ~−90%（维持 0.4~0.5%），**不是 −44.4%** ⇒ 两种情况下回测曲线都不能直接外推 |

### 2.5 唯一可以"上线"的部分：前向纸面交易（`LIVE.md`）

**重要区分**：`runs/live/` 下跑的那个 carry 纸面交易**不是**训练框架的产物。
详见 `LIVE.md §1`：

- `src/live/paper.py` **只 import 标准库**，连 `src/sim/` 都不 import，对 `MarginBook` 的引用数为 **0**
- 它有独立的三桶记账（spot / margin / reserve），与训练框架的 `MarginBook` 是**两套独立实现**
- 配置走的是 M10 funding carry（M10 网格最优 + G3 封存段判定"有边际"），**不是** D 阶段的学习体
- 名义 0.4×、现货多 + 永续空 delta 中性

**所以这份纸面交易不受 2026-09-15 那个 −44.4% 强平 bug 影响**，跑出来的数字是另一条线的。
目前这条线是项目里**唯一**可以对外讲"在跑、未来不可知"的实证。

---

## 3. 上线前的硬约束（如果要真的接去实盘，按顺序）

按"投资额 = 改对一件事 vs 改错一件事"排序。**未做之前不要上线**。

| 序 | 必做 | 难度 | 当前状态 |
|---|---|---|---|
| 0 | **跑 `scripts/pool_ceiling.py`** | 低 | **脚本已写好（671 行），未跑过**。输出"池内最优成员"一行数字 + 学习体在第几名 |
| 1 | **保证金参数修正**：把 D 阶段硬编码的 `k=0.5` 改成 `k=1/L`（一行参数；审计附录 A 已验证 `only_BTCUSDT` 改后 101,251、0 次强平） | 极低 | **未做** |
| 2 | **结构性修补**：`margin_cash` 从来没被真正借记过，要让 agent 能看到强平/权益（当前是开环的）+ 删掉 per-leg 逻辑子账户 | 中 | **未做** |
| 3 | **池子扩到含双腿 carry**：需新加 expert + 引擎支持双腿头寸（`exchange.py:199` 单净头寸必须改） | 高 | **未做** |
| 4 | **加 walk-forward holdout + ≥10 种子分布**：基础设施 ≈ 0；要做的话先加策略种子接口（§1.4） | 高 | **未做** |
| 5 | **重写评估协议**：当前对 +40%/年 alpha 功效只有 3% ⇒ **等于没评估**。需要换协议或承认"当前装置无法判定边际" | 高 | **未做** |
| 6 | **实盘撮合层**：建一个事件驱动的撮合层（分笔撮合、撤单重挂、partial fill），目前 0 | 高 | **未做** |
| 7 | **实盘 funding 结算层**：把当前"按 close × rate 直接扣"改成"按 8h 边界、按标记价"，目前 0 | 高 | **未做** |

**如果只做一件**：做 §3 第 0 行（跑 `pool_ceiling.py`）。它把"学习体在池内第几名"从看不见变成一行数字，
**所有后续投入是否值得，都取决于这一行**。如果学习体不在池内前 30%，按项目自己的 G2 淘汰门
（PLAN §1 §6）就该停止参数调整、转信号源研究。脚本作者已在文件 docstring 里把这件事写明白。

---

## 4. 仓库导览（第一次打开这个项目的人看这一节）

### 4.1 一句话定位

**Hermes-Q** = 资产无关的金融时序自学习框架（crypto 是第一个插件），研究用途，只读公开行情。
**目标不是"造一个赚钱 agent"，是造一套能证伪任何策略的评估装置 + 通用框架**（PLAN §0）。

### 4.2 顶层结构

```
/home/nick/workspace/quant/
├── PLAN.md          ← 项目计划（80 KB）：目标 / 铁律 / 执行模型 / 路线图
├── WORK_LOG.md      ← 跨 session 交接（95 KB）：每个里程碑的实测记录
├── LIVE.md          ← 前向纸面交易状态（13 KB）：当前唯一"未来不可知"的实证
├── FRAMEWORK_AUDIT.md     ← 2026-09-15 独立审计（37 KB）：3 大问题 + 证据索引
├── FRAMEWORK_AUDIT_SUPPLEMENT.md  ← 本文件（性能瓶颈 + 实时可行性 + 仓库导览）
│
├── configs/         ← 冻结配置（base.json / spreads.json）。任何变更必须新建文件，禁止就地修改
├── data/            ← 数据层（manifest.json + raw/ 原始 CSV，CSV 不入库，按需重放）
├── src/             ← 代码主体（按职责切分）
│   ├── agents/      ← 学习回路（HedgeEnsemble + 18 个 Expert + baseline）
│   ├── sim/         ← 仿真引擎（SimExchange / MarginBook / CostModel / FundingTable / carry）
│   ├── eval/        ← 评估装置（protocol / metrics / benchmark / holdout）
│   ├── env/         ← MarketView：因果封印的视图（策略只能通过它访问 t 之前的数据）
│   ├── data/        ← 数据加载 + 质量校验
│   ├── live/        ← 纸面交易实现（与 sim/ 完全隔离）
│   ├── registry/    ← Run 注册（每次实验写 config.json + meta.json）
│   └── config.py    ← 配置加载入口
├── scripts/         ← 实验脚本（M1~M11 + A/B/C/D 阶段 + 工具）
├── tests/           ← 单元测试（11 个测试文件，pytest 全绿 1.23s）
└── runs/            ← 实验产物（每个 run 一个目录，注册过的含 config/meta，未注册的不入 git）
```

### 4.3 关键路径速查

| 想做什么 | 去哪 |
|---|---|
| 跑回测 | `scripts/m5_online.py`（学习体单跑）/ `scripts/d_param_search.py`（D 阶段 18 组） |
| 跑评估协议 | `src/eval/protocol.py::evaluate_strategy`（alpha_ci / DSR / alpha_p / pbo_cscv） |
| 看池子上界 | `scripts/pool_ceiling.py`（**已写好、未跑过**） |
| 看 carry 单独仿真 | `src/sim/carry.py` + `scripts/m10_carry_grid.py` |
| 看前向纸面交易状态 | `runs/live/<SYM>.json` + `LIVE.md` |
| 加新 expert | `src/agents/online.py` 继承 `Expert` 基类，在 `default_experts()` 注册 |
| 改配置 | **新建 `configs/<name>.json`**，禁止改 `base.json` |
| 跑测试 | `python3 -m pytest tests/ -q`（158 passed in 1.23s） |
| 加实验 | 必须通过 `src/registry/runs.py::Run` 注册；写 `config.json + meta.json`，否则不入 git |

### 4.4 三个最容易踩的坑

1. **不要改 `configs/base.json`**——它被冻结（"universe.mode: FROZEN"），改了就破坏样本外可比性。
2. **不要写裸 `runs/<date>_xxx/`**——`.gitignore` 已经把这些排除了。正规产物走 `Run` 注册（`src/registry/runs.py`），
   自动写 `config.json + meta.json`，git 会收录。**这是项目"证据可复现"的底线**（`.gitignore` 注释里写明）。
3. **不要把 D 阶段的学习体当 alpha 上线**——见 §2。

### 4.5 测试与质量门

```
python3 -m pytest tests/ -q
# → 158 passed in 1.23s
```

测试覆盖：因果、数据质量、资金费延迟、MarginBook、杠杆语义、协议、模拟守恒、试验计数可复现、
波动归因、纸面交易、agent 做空能力。

---

## 5. 不确定清单（与审计 §4 平行，仅列新增项）

| # | 不确定 | 把握 |
|---|---|---|
| 1 | 优化 §1.5 的 ROI 数字是按"pandas 切片 → numpy 滚动视图"估的，没实测过。真实节省可能在 1.5×~3× 之间 | 五成 |
| 2 | "实时 funding 8h 结算路径不同"是我推断的，没对照过 Binance 真实 funding 序列 | 六成 |
| 3 | "−75.5% 回撤的产品在实盘会死"这条结论**方向**我有把握，**具体阈值**（50%? 60%?）取决于客户/合规/风险预算，**不在本框架能力范围内** | 七成方向，三成数字 |
| 4 | 审计里"学习体输出 = 暴露稀释 + 成本 + 强平"是净额分解，没单独分解"被停损实现的损失"vs"停损后以更好价重入的收益"。方向不确定 | 五成 |
| 5 | 成本模型 12bp/边 我没有独立校准；如果真实往返 6bp 不少倍数关系会变，但"池内最优 = 买入持有"这个结论不会变 | 五成 |

---

## 附录 A —— 文件间引用关系（避免重复造轮子）

| 本文件章节 | 主要引用 |
|---|---|
| §1（性能瓶颈） | `src/agents/online.py:78,120,195,240,255`、`src/env/market_view.py:87,91`、`src/sim/exchange.py:188` |
| §2.1（最优产物） | `runs/d_param_search/all_configs.csv`、`FRAMEWORK_AUDIT.md` §0 |
| §2.2（亏损三件套） | `FRAMEWORK_AUDIT.md` §2 反向解读 |
| §2.3（评估功效） | `src/eval/protocol.py:469`（KURT_MAX_WELL_BEHAVED）、`FRAMEWORK_AUDIT.md` §1 问题三 |
| §2.4（实时 4 风险） | `src/sim/exchange.py:288-345`（强平/破产路径）、`src/sim/funding.py` |
| §2.5（纸面交易独立） | `LIVE.md §1`（已写得很完整，本节只索引） |
| §3（上线硬约束） | `scripts/pool_ceiling.py` docstring、`FRAMEWORK_AUDIT.md` 附录 A（保证金修法） |
| §4（仓库导览） | `PLAN.md §1`（六条铁律）、`PLAN.md §0`（产出定位）、`.gitignore` |

## 附录 B —— 复现环境

Python 3.10 / numpy 2.2.6 / pandas 2.3.3 / i9-13900H。
全窗口 = 77,503 根（3 标的交集，2017-11-06 ~ 2026-09-14）。
**一次学习体全窗口仿真 = 72.5 秒**（本文与审计、与脚本顶部注释三处一致）。
本文与审计的全部实验合计约 50 分钟机时。

---

**最后一句话**（与审计同方向，但更直接）：

> **如果你今天只有 25 分钟**：跑 `scripts/pool_ceiling.py`。一行数字告诉你这个池子的天花板在哪。
> 如果天花板 = 买入持有（1.5M），那学习体最多 = 1.5M − 成本 − 强平 − 暴露稀释 ≈ 200k。
> 这是结构上限，**不是配置问题**。修保证金参数、扩池到双腿 carry、写 RL/LLM 反思体都改不了这个上限。
> 要改上限，必须**换信号源**——而项目自己验证过、唯一有边际的信号源是 funding carry（M10），
> 它目前**结构上不可表达**（见审计 §1.5）。这条线要不要继续、往哪走，看完那一行数字再决定。


